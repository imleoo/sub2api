# 上游同步执行方案：upstream/main 0.1.137 → zhiguofan

**生成时间**：2026-06-21
**状态**：方案待审，**未执行任何 git 操作**
**适用范围**：本次同步（zhiguofan 1.1.136 → 1.1.137），80 提交 / 1016 文件变更

> 本文档由 `/sync-upstream` 风险分析衍生，是一次性执行手册。完成本次同步后可归档。
> 风险依据：[`claudedocs/自定义开发功能列表.md`](自定义开发功能列表.md)（唯一源）。

---

## 0. 总体判断

| 维度 | 现状 |
|------|------|
| 待合并提交 | 80（main 与 zhiguofan 均落后上游 80） |
| 变更文件 | 1016 |
| maas-refactor 分支 | **不存在**，MAAS 已并入 zhiguofan，三分支模型简化为两分支 |
| 整体风险 | 🔴 高 |
| 最大工作量 | 功能 35 逆向链回归（~90 文件） |

**核心策略**：分阶段人工合并，每阶段过编译门禁、逐阶段提交。**不要一把梭跑 `sync_upstream_to_zhiguofan.sh`**——脚本对功能 35 大面积回归与 migration 碰撞无能为力，会留下混乱中间态。

---

## 1. 三大冲突点（合并前必读）

### 冲突点 A — 功能 35 逆向订阅链大面积回归 🔴🔴🔴
上游本次有 ~90 个 antigravity/oauth 文件变更，fork 已整链删除。`git merge` 会把它们**全部拉回**。

涉及文件族：
- `backend/internal/pkg/antigravity/*`（13 文件）
- `backend/internal/pkg/{oauth,geminicli,openai}/oauth*.go`
- `backend/internal/service/antigravity_*.go`、`*_oauth_service*.go`、`oauth_*.go`
- `backend/internal/handler/admin/{antigravity,gemini,openai}_oauth_handler.go`
- `backend/internal/handler/auth_*_oauth*.go`
- `backend/internal/repository/{claude,openai,gemini}_oauth*.go`
- 前端 `composables/use{Antigravity,Gemini,OpenAI}OAuth.ts`、`OAuthAuthorizationFlow.vue`、`api/admin/antigravity.ts`

**处理**：合并完成后，严格按以下顺序重新删除：
1. [`claudedocs/逆向代码清理执行方案_v6.md`](逆向代码清理执行方案_v6.md)（方案）
2. [`claudedocs/逆向清理_执行报告_v6.md`](逆向清理_执行报告_v6.md)（已执行记录，按此复核删除清单）
3. [`claudedocs/逆向清理_后遗症收尾_follow-up.md`](逆向清理_后遗症收尾_follow-up.md)（收尾项）
4. 可直接调用 `reverse-cleanup-v6` skill 复跑

**门禁**（删干净的判定）：
```bash
rg -n "PlatformAntigravity|AccountTypeOAuth|AccountTypeSetupToken|IsOAuth|require_oauth_only|mcp_xml_inject|antigravity" \
  backend/internal frontend/src
# 期望：仅剩用户登录 OAuth 白名单（auth_*_oauth：dingtalk/wechat/linuxdo/oidc/email 这类“用户登录”属正常保留，
#       需区分“账号上游逆向 OAuth”才是要删的）
```
> ⚠️ 注意区分两类 OAuth：**用户登录** OAuth（dingtalk/wechat/linuxdo/oidc/email，**保留**）vs **账号上游逆向** OAuth（antigravity/gemini-cli/openai-codex token 逆向，**删除**）。`逆向清理_执行报告_v6.md` 已界定边界，以它为准。

### 冲突点 B — migration 同号并存（风险已下调为 🟡）
> 经 git 核实（2026-06-21）：上游最高迁移仅到 **153**，**不存在** 160/161（fork 的 160/161 是逆向清理独有，上游不会带回）。本次实际只带入 4 个上游新迁移，均与 fork 同号文件**异名并存**，**无需重编号**。

上游本次新增（zhiguofan 没有的全部）：
- `151_account_autopause_expiry_index_notx.sql`
- `151_channel_monitor_jitter.sql`
- `152_scheduler_outbox_dedup_key.sql`
- `153_scheduler_outbox_pending_dedup_key_index_notx.sql`

zhiguofan 同号文件（异名，可并存）：`151_seed_lingjing_models.sql`、`152_update_lingjing_prices.sql`、`153_model_pricing_pricing_unit.sql`。

**现状**：`migrations_runner.go` 按文件名排序容忍同号共存（fork 本就有 `149_proxy_expiry_fallback.sql` + `149_users_add_phone.sql` 并存）。因此上述 4 个上游新迁移直接并入即可，无碰撞。

**处理**：合并后仅需确认 outbox/autopause 系列与 fork 调度（`scheduler_snapshot_service` / lingjing）无依赖顺序问题：
```bash
ls backend/migrations/*.sql | sed 's:.*/::' | sort   # 复查，确认无真正重名（同名不同内容）
```

### 冲突点 C — 兜底定价 vs SSOT 架构 🔴
上游多个 `feat(billing): add ... fallback pricing`（GLM/Kimi/MiniMax、DeepSeek V4、kimi-for-coding、doubao-embedding-vision）往 `billing_service.go` 重新引入 `fallbackPrices`。fork 功能 26 已**彻底删除该字段**，计费走 catalog → aliasIdx → matchByModelFamily。

**处理**：
- 合并时**丢弃**上游对 `fallbackPrices` 的新增（保持 SSOT）。
- 但上游新增的国产模型价格**不能丢**——按功能 34「seed 绝对不内置」：**不要**写进 `BootstrapPricingSeeds()`（seed 只保 16 条 Claude/GPT/Gemini 兜底），改以运营模板 `data/model_pricing.json`（冷启动兜底）或 `sync-maas` 上传吸收价格数值。
- ⚠️ fork 已在 `data/model_pricing.json` 放了 deepseek 参考价（commit eb6cf4ae）；吸收上游 DeepSeek V4 等价格时**注意去重**，勿与现有条目重复。
- 验证：`grep -n "fallbackPrices" backend/internal/service/billing_service.go` 应**无输出**。

---

## 2. 分阶段执行步骤

### 阶段 0 — 前置快照
```bash
cd /Users/leoobai/jiwu-project/SubPanel
git status                      # 必须 clean
git fetch upstream --quiet
git branch backup/zhiguofan-pre-0.1.137 zhiguofan   # 回滚保险点
```

### 阶段 1 — main fast-forward（无冲突）
```bash
git checkout main
git merge upstream/main --ff-only
git show main:backend/cmd/server/VERSION    # 期望 0.1.137
git push origin main
```

### 阶段 2 — zhiguofan 合并 main（冲突主战场）
```bash
git checkout zhiguofan
git merge main --no-edit        # 预期大量冲突，不要慌
git diff --name-only --diff-filter=U | sort   # 列出所有冲突文件
```
按以下优先级解决冲突（**保 fork 实现**，吸收上游有价值变更）：

1. **后端核心服务**（逐文件人工）
   - `billing_service.go` / `pricing_service.go` → 见冲突点 C，保 catalog SSOT，吸收上游新模型价格为 seed
   - `gateway_service.go` / `openai_gateway_service.go` → 保 masking + `ApplyUpstreamCostSnapshot` + lingjing + generic；吸收上游 thinking 过滤、失败转移、zstd 解压
   - `domain/protocol.go` / `domain/constants.go` → 保功能 16 协议常量；核对上游对 constants 的改动
   - `config/config.go` → 保 `ProtocolBucketEnabled`、`GenericRuntimeEnabled` **两字段**（注：`USAGE_UPSTREAM_COST_ENABLED` 非 config 字段，仅注释；快照由 `ApplyUpstreamCostSnapshot` 命中 provider_pricing 驱动）
   - `routes/gateway.go` → 保 `getGroupInboundProtocol`、`/lingjing/v1/video/*`、协议分流 switch
   - `routes/admin.go` → 保 sync-maas、`provider-pricings`（复数）路由
   - `scheduler_snapshot_service.go` / `account.go` → 保 PlatformLingjing、masking、双桶

2. **Handler 层**
   - `setting_handler.go` → 保功能 5/6/29/30/31 全部字段
   - `auth_handler.go` / `auth_service.go` / `user_handler.go` → 保功能 13 username 必填

3. **Ent schema**（保 fork 新增列/文件）
   - `usage_log.go`（9 列）、`user.go`（phone）、`model_pricing.go`（pricing_unit+tier_pricing）、`group.go`/`account.go`（协议字段）、新增 schema 文件

4. **前端**（保 fork 功能）
   - `router/index.ts` → 保 `/models`；`/admin/model-discounts` 仅为**重定向**到 `/admin/model-pricings`（独立折扣页已删，勿恢复）。注：provider-pricing **无前端路由**（仅后端 admin API），不要据上游恢复前端入口
   - `types/index.ts` → `'lingjing'`/`'generic'`，**不要**恢复 `'antigravity'`/oauth 类型（功能 35）
   - `platformColors.ts` → lingjing 条目
   - `ModelsView.vue`、`CreateAccountModal.vue`、`EditAccountModal.vue` → 保白名单/lingjing/无 OAuth 入口
   - `i18n/locales/zh.ts` & `en.ts` → 保所有 fork 键

### 阶段 3 — 功能 35 逆向链重新删除
冲突点 A。合并解决后，调用 `reverse-cleanup-v6` skill 或按 `逆向清理_执行报告_v6.md` 手动删除，过 rg 门禁。

### 阶段 4 — migration 复查（无需重编号）
冲突点 B。本次上游最高迁移仅 153、无 160/161，4 个新迁移与 fork 同号异名并存，**无需重编号**。仅复查无真正重名 + outbox/autopause 与 fork 调度无依赖序问题。

### 阶段 5 — workflow 触发器还原（功能 9）
```bash
for f in .github/workflows/*.yml; do grep -A3 "^on:" "$f"; echo "--- $f"; done
# 把 6 个文件的 on: 全部还原为仅 workflow_dispatch:
```

### 阶段 6 — 版本号 + 代码生成
```bash
echo "1.1.137" > backend/cmd/server/VERSION      # 主号必须为 1
cd backend
go generate ./ent            # schema 有变更，必跑
go generate ./cmd/server     # wire 有变更；若无网拉 wire 工具，手动同步 wire_gen.go
cd ..
```

### 阶段 7 — 编译 + 验证门禁
```bash
cd backend && go build ./cmd/server/
go test -tags=unit ./internal/service ./internal/repository ./internal/server ./internal/handler/...
cd ../frontend
pnpm install                 # 若 package.json 有变更
pnpm exec vitest run src/views/user/__tests__/ModelsView.spec.ts src/i18n/__tests__/usageServiceTierLocales.spec.ts
pnpm exec vitest run src/views/admin/__tests__/SettingsView.spec.ts src/composables/__tests__/usePersistedPageSize.spec.ts
pnpm run lint:check
cd ../backend && golangci-lint run ./...
```

### 阶段 8 — fork 功能守护门禁
```bash
cd /Users/leoobai/jiwu-project/SubPanel
grep -q '/lingjing/v1/video' backend/internal/server/routes/gateway.go && echo OK || echo MISSING
grep -q 'getGroupInboundProtocol' backend/internal/server/routes/gateway.go && echo OK || echo MISSING
grep -q 'ProtocolBucketEnabled' backend/internal/config/config.go && echo OK || echo MISSING
grep -q 'upstream_total_cost' backend/ent/schema/usage_log.go && echo OK || echo MISSING
grep -q 'ApplyUpstreamCostSnapshot' backend/internal/service/gateway_service.go && echo OK || echo MISSING
grep -n 'fallbackPrices' backend/internal/service/billing_service.go && echo "BAD: fallbackPrices 残留" || echo "OK: 无 fallbackPrices"
# 功能 35 门禁
rg -n "PlatformAntigravity|AccountTypeOAuth|require_oauth_only|mcp_xml_inject" backend/internal frontend/src
test -L AGENTS.md && readlink AGENTS.md   # 期望 CLAUDE.md
```

### 阶段 9 — 提交
```bash
git add -A
git commit    # 描述本次同步范围、解决的冲突、重新删除的功能35、migration 重编号
# push 由用户决定时机
```

---

## 3. 回滚

任意阶段失败：
```bash
git merge --abort                              # 阶段 2 合并中途
git reset --hard backup/zhiguofan-pre-0.1.137  # 已提交后整体回滚
```

---

## 4. 上游本次值得吸收的有价值变更（勿误删）

- 国产模型定价（GLM/Kimi/MiniMax/DeepSeek V4/doubao-embedding）→ 以 seed/catalog 吸收
- thinking/reasoning 协议感知过滤（`6baf00d7`、`142d8c36`、`56c6325d`）
- scheduler outbox 去重一致性修复（一串 fix）
- 网关失败转移：non-JSON 2xx（`ab9987b2`）、zstd 解压（`c1c28ac7`）、SSE event:error body 保留（`6c7203d8`）
- OpenAI 配额查询/重置、/responses 探测工具校验
- account list 显示 account id、渠道监控抖动配置

---

## 5. 预估

- 阶段 1：分钟级
- 阶段 2：冲突主战场，**数小时**（取决于冲突复杂度）
- 阶段 3（功能 35 重删）：1-2 小时（有 skill 与报告可复用）
- 阶段 4-8：1-2 小时
- 总计：半天到一天人工

> 建议挑整块时间执行，避免中断留下半合并状态。
</content>
</invoke>
