# 待办任务列表（zhiguofan 分支）

> 跨会话的工程待办登记处。每条任务标注状态、前置条件、验收门禁、关联文档。
> 状态：⬜ 待办 ｜ 🔄 进行中 ｜ ✅ 已完成 ｜ ⏸ 阻塞

---

## ✅ T1 · 逆向清理后遗症 — 剩余三类（已完成）

**状态**：✅ 已完成（2026-06-16，分支 `feature/reverse-cleanup-v6`，三类逐类独立提交）
**登记日期**：2026-06-15 ｜ **完成日期**：2026-06-16
**清单来源**：`claudedocs/逆向清理_后遗症收尾_follow-up.md`（含每项 file/符号、改动顺序、验收命令）
**背景**：`../自定义开发功能列表.md` 功能 35。逆向订阅主链已删并提交；剩余三类为 inert 死代码。
**完成前提**：Docker daemon 恢复可达 + golangci-lint v2.9.0 在位 → 三类均跑完整门禁（unit/integration/e2e-compile/lint）。

**子任务**（按风险从低到高执行，逐类提交）：

- [x] **类 3（🟢 低风险）** — commit `dfe1fe40`：清除 openai_*.go 等 unused 孤儿函数海（含 P7 遗留的 hotpath/openai-images 测试 stub）。`golangci-lint unused` 清零。
- [x] **类 2（🟡 schema）** — commit `8355b78f`：删 `RequirePrivacySet`/`IsPrivacySet` 死调度门控。涉及 service/admin/dto/handler/repository/ent schema 全链 + 前端 GroupsView UI/types + 新增迁移 `161_drop_group_require_privacy_set.sql`。`go generate ./ent` 已提交生成代码。
- [x] **类 1（🔴 热路径）** — commit `8327fcd0`：删网关 `tokenType=="oauth"` 10 处死分支 + 去 compute 函数失活形参 + 级联删 `getBetaHeader`/`applyClaude*MimicHeaders`/`mergeAnthropicBeta*` + 删 5 个 constants OAuth 符号（KEEP 符号未动）+ 清 `ctFingerprint`/`ctEnableFP` 死块。oauth 测试用例改写为 API-key 路径覆盖。

**验收结果**：build/vet 绿 · `go test -tags=unit ./internal/...` 通过 · e2e 编译通过 · server/routes+middleware+repository integration(Docker) 通过 · `golangci-lint unused` 清零 · `rg 'tokenType == "oauth"|RequirePrivacySet|IsPrivacySet'` 全清零 · 前端 typecheck/lint/GroupsView 测试通过。

**遗留（需 live 环境，已与用户确认自动门禁达标即提交）**：
- 类 1 的「真实 apikey/vertex 转发冒烟」需 live 上游凭证，留待手动补做。
- 预存失败 `repository TestListWithFilters/filter_by_type`（账号类型过滤，与本任务无关，stage A 基线同样失败）。

**关联文档**：`逆向清理_后遗症收尾_follow-up.md`、`逆向代码清理执行方案_v6.md`、`逆向清理_执行报告_v6.md`、`../自定义开发功能列表.md`（功能 35）。

---

## ⬜ T2 · Gemini 生图计费：未产出图片场景仍计费 1 张图（bug，未修复）

**状态**：⬜ 待办（问题已定位、已补测试复现，尚未修复）
**登记日期**：2026-07-01
**发现路径**：用户询问 e2e 是否覆盖生图模型计费 → 排查 `ModelWhitelistSelector.vue` 白名单选择器 bug 过程中顺带发现。

**根因**：`backend/internal/service/gemini_messages_compat_service.go` 的 `Forward()`（约 L989-992）、`ForwardNative()`（约 L1462-1465），以及 `gemini_chat_completions_compat_service.go` 的 `forwardClaudeBodyAsChatCompletions()`（约 L263-266），三处统计 `imageCount` 的逻辑完全相同：

```go
imageCount := 0
if isImageGenerationModel(originalModel) {
    imageCount = 1
}
```

只要请求模型名命中 `isImageGenerationModel()`（`gateway_temp_unschedule.go:40-51`，白名单 `gemini-2.5-flash-image*`/`gemini-3-pro-image*`/`gemini-3.1-flash-image*`），**无论上游实际返回了什么都硬编码记 1 张图**——代码完全没有解析响应体里 `inlineData`（图片 part）的数量，也没看 `finishReason`。

**影响**：Gemini 生图模型对涉及人物、版权、暴力等内容有安全策略，经常返回 HTTP 200 但 `finishReason=SAFETY`、无 `inlineData`（请求"成功"但没图）；也可能出现模型被要求"只回复文本不生成图片"而真的照做的场景。这两类情况下用户会被**错误扣费/扣配额，但实际没拿到任何图片产出**。

**不受影响的部分**：计费公式本身（`billing_service.go:876 CalculateImageCost()` = 单价 × 数量）与定价数据（`model_pricings.output_cost_per_image`）均正确，问题仅在于喂给公式的 `imageCount` 这个数字来源不对。对比之下 OpenAI 生图路径（`openai_images.go:654-712`）是正确实现——会解析响应里的实际图片数覆盖默认值，Gemini 这边是退化实现，两条链路不一致。

**已补充的测试（用于锁定当前行为、验证修复）**：
- 单测（mock 上游，确定性，3 处代码路径各一个）：
  - `TestGeminiMessagesCompatServiceForward_SafetyBlockedImageStillBillsOneImage`
  - `TestGeminiMessagesCompatServiceForwardNative_SafetyBlockedImageStillBillsOneImage`
  - `TestGeminiForwardAsChatCompletions_SafetyBlockedImageStillBillsOneImage`
  均在 `backend/internal/service/gemini_messages_compat_service_test.go`。断言的是【当前】（有 bug 的）行为（`ImageCount==1`），PASS = 复现成功；**修复后需要把这三处断言改成 `require.Equal(t, 0, ...)`**。
- e2e（真实上游，`backend/internal/integration/e2e_full_gemini_image_billing_test.go`）：
  - `TestE2EFull_GeminiImageBillingOnNoImageProduced`：请求生图模型但 prompt 明确要求"只回复文本不要生成图片"，若响应确无 `inlineData` 仍断言余额下降达到图片计价量级（≥$0.01），证明零产出仍被计费；若上游仍产出了图片则 `t.Skip`（live 行为不可控，不视为失败）。2026-07-01 已用真实 Gemini 生图账号验证跑通，见执行记录。

**修复方向（尚未实施）**：三处硬编码改为真正解析响应体统计 `inlineData`/图片 part 数量；`finishReason=SAFETY` 或无图片 part 时记 0。三个单测的断言需同步改为 `0`。

**关联文档**：`../自定义开发功能列表.md`（建议后续补录一条功能号，目前暂未占号）。
