# 待办任务列表（zhiguofan 分支）

> 跨会话的工程待办登记处。每条任务标注状态、前置条件、验收门禁、关联文档。
> 状态：⬜ 待办 ｜ 🔄 进行中 ｜ ✅ 已完成 ｜ ⏸ 阻塞

---

## ⬜ T1 · 逆向清理后遗症 — 剩余三类（交给带集成测试的专项执行）

**状态**：⬜ 待办（已排期，等待具备集成测试环境的专项）
**登记日期**：2026-06-15
**清单来源**：`claudedocs/逆向清理_后遗症收尾_follow-up.md`（含每项 file/符号、改动顺序、验收命令）
**背景**：`claudedocs/自定义开发功能列表.md` 功能 35。逆向订阅主链已删并提交（`feature/reverse-cleanup-v6`，6 commits）；剩余为 inert 死代码，因需集成测试 / schema 迁移 / 全量 lint 而未在本轮做完。

**为何必须由「带集成测试」的专项做**：
- 本环境 **Docker daemon 不可达**（`docker ps` 失败，Docker Desktop 未运行），testcontainers 起不来，`go test -tags=integration/e2e` 无法运行。
- 剩余「类 1」改动网关核心热路径（鉴权头拼装），**unit 测试兜不住转发行为正确性**——必须在有 Docker 的环境跑 integration/e2e + 真实 apikey/vertex 转发冒烟。

**子任务**（按风险从低到高执行，逐类提交）：

- [ ] **类 3（🟢 低风险）**：删 openai_*.go unused 孤儿函数海。用 `golangci-lint run ./...` 的 `unused` 一次性扫全（已实测零调用方清单见 follow-up 文档），逐文件删，删一批重跑 lint 捕级联。验收：`golangci-lint run` unused 清零 + `go build/vet/test -tags=unit`。
- [ ] **类 2（🟡 schema）**：删 `RequirePrivacySet`/`IsPrivacySet` 死功能。改 `ent/schema/group.go` → `go generate ./ent` → 补迁移 `XXX_drop_group_require_privacy_set.sql` → 删 5 处调用点 + 前端开关。验收：`rg 'RequirePrivacySet|IsPrivacySet'` 清零 + `go generate` + build。
- [ ] **类 1（🔴 热路径，必须 e2e）**：删网关 `tokenType=="oauth"` 10 处死分支 + unwind `tokenType`/`mimicClaudeCode` 形参 + 删 constants.go 的 `BetaOAuth`/`FullClaudeCodeMimicryBetas`/`DefaultBetaHeader`/`HaikuBetaHeader`/`CountTokensBetaHeader`（KEEP 符号见 v6 方案 §9.1，勿误删）。验收：`go test -tags=e2e ./internal/integration` + 真实 apikey/vertex 转发冒烟 + `rg 'tokenType == "oauth"'` 清零。

**总验收门禁**（专项完成后）：
```
go build ./... && go vet ./...
go test -tags=unit ./internal/...
go test -tags=integration ./internal/repository ./internal/integration   # 需 Docker
go test -tags=e2e ./internal/integration                                 # 类 1 必跑
golangci-lint run ./...                                                  # unused 清零
cd frontend && pnpm run typecheck && pnpm run lint:check && pnpm test:run
rg -n 'tokenType == "oauth"|RequirePrivacySet|IsPrivacySet' backend/internal   # 清零
```

**关联文档**：`逆向清理_后遗症收尾_follow-up.md`、`逆向代码清理执行方案_v6.md`、`逆向清理_执行报告_v6.md`、`自定义开发功能列表.md`（功能 35）。
