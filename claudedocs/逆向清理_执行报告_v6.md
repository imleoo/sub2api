# 逆向代码清理 v6 — 执行验收报告

> 生成时间：2026-06-14
> 执行范围：方案 P2–P7 + 前端（PF），自动顺序执行、编译门禁、逐阶段提交、失败即停
> 本报告依据编排器返回的结构化结果，并结合执行结束时仓库实际状态复核

---

## 1. 总览

| 项 | 值 |
|----|----|
| 计划阶段 | P2 → P3 → P4 → P5 → P6 → P7 → PF（前端） |
| 实际完成（已提交且 gate 绿） | **P2、P3、P4、P5** |
| 中途停止于 | **P6**（antigravity 逆向链删除） |
| P6 状态 | 工作区已大量改动但**未提交**，单元测试 gate **红**（详见 §4） |
| 当前 git HEAD | `30e5892f`（P5 提交） |
| 后续阶段 | P7、PF（前端集中改动）**未开始** |

### 已提交的阶段链（`git log --oneline`）

```
30e5892f refactor(P5): 删除 Claude OAuth 与共享 token 刷新设施     ← 当前 HEAD
5d071f64 refactor(P4): 删除 Gemini OAuth/CodeAssist 逆向链
1648f93d refactor(P3): 删除 OpenAI/Codex 逆向链
c30ea59d refactor(P2): 删除后端逆向网关路由与账号 OAuth 入口
639e4c85 refactor(P1): 解耦正规路径对逆向 OAuth 的反向依赖          ← 本次执行前已存在的基线
eb32a481 docs: 逆向清理 v6 方案基线 (P0)
```

> P0（方案基线）与 P1（反向依赖解耦）在本次自动执行启动前即已就绪。本次自动执行交付了 **P2–P5 共 4 个提交**，并在 P6 工作区改动后因测试 gate 未过而停止。

### 净删除规模（已提交部分，P2–P5）

- 整删纯逆向源/测试文件：**约 50 个**（OpenAI/Codex、Gemini OAuth/CodeAssist、Claude OAuth + 共享 token 刷新设施及其测试）
- 局部切除逆向分支并编辑：**约 60 个**（网关服务、handler、wire、pkg/* 常量、测试桩）

---

## 2. 逐阶段明细

### P2 — 关闭后端逆向网关路由与账号 OAuth 入口 ✅

| 项 | 内容 |
|----|------|
| 状态 | green |
| commit | `c30ea59d` |

**做了什么**
- 删除 `routes/gateway.go` 的 antigravity 路由组（`/antigravity/models`、`/antigravity/v1`、`/antigravity/v1beta`，原 232–263 行）；保留 Lingjing（220–230）、`/v1`、Gemini `v1beta` 正规路由。
- 删除 `routes/admin.go` 的 Codex import 路由（`/import/codex-session`）、账号 `:id/refresh`、`apply-oauth-credentials`、`set-privacy` 路由，并清理两条「已移除」残留注释；保留普通账号 CRUD/test/stats 全部路由。
- `routes/auth.go` 完全未动。
- 顺带修复 P1 遗留的 `NewOpenAIGatewayService` 测试桩签名不一致（两处删除已移除的 `OpenAITokenProvider` nil 实参），否则 `go vet` 不过。

**改了哪些文件**
- `backend/internal/server/routes/gateway.go`
- `backend/internal/server/routes/admin.go`
- `backend/internal/service/openai_ws_protocol_forward_test.go`
- `backend/internal/handler/openai_gateway_handler_test.go`
- `backend/internal/service/openai_gateway_record_usage_test.go`

**删了哪些文件**：无

**gate 结果**
- `go build ./...` 全绿；`go vet ./...` 全绿（修复测试桩后通过）。
- `go test -tags=unit ./internal/server ./internal/handler/...` 全部 ok（server 1.228s 强制刷新通过，handler 22.875s）。
- grep 验证 `routes/gateway.go`、`routes/admin.go` 已无 antigravity / apply-oauth / import-codex / `:id`-refresh / set-privacy 残留。

**偏离与理由**
1. 未删除 handler 方法 `refreshSingleAccount`/`Refresh` 本体——本阶段不强制删 handler 函数本体，且 `refreshSingleAccount` 仍被保留的 `BatchRefresh`（line 1253）调用，整删会破坏正规批量刷新，留待后续阶段。
2. 额外修复 P1 遗留的两处 `NewOpenAIGatewayService` 测试桩签名——这是达成 P2 gate「go vet 全绿」的必要前置，非新增清理范围。
3. 未提交 `.claude/workflows/` 未跟踪目录（编排产物，非业务代码）。

---

### P3 — 删除 OpenAI/Codex 逆向链 ✅

| 项 | 内容 |
|----|------|
| 状态 | green |
| commit | `1648f93d` |

**做了什么**
- 整删纯逆向文件：openai_oauth_service（service + repo）、openai_token_provider、openai_privacy_service、`pkg/openai/oauth`、admin/openai_oauth_handler、account_codex_import、openai_codex_instructions_template。
- 在 openai_gateway_service / messages / chat_completions / codex_transform / images / fastpath 及 handler 层切除所有 OpenAI OAuth / Codex 分支（GetAccessToken OAuth case、codex transform 函数簇、ChatGPT internal URL/header、passthrough OAuth body、429 storm/ShouldStop、SSE OAuth 启发、overrideBrowserUserAgent），并清除产生的孤儿函数。
- admin_service 删 OpenAI 隐私方法 + privacyClientFactory；token_refresh_service 删 ensureOpenAIPrivacy；account.go `SupportsOpenAIImageCapability` 改为仅 apikey、`IsPrivacySet` 去 OpenAI 分支。
- Wire 去 4 个 OpenAI OAuth provider/factory，`go generate ./cmd/server` 重生成 `wire_gen.go`。
- 测试删 OpenAI/Codex OAuth 用例；generic 流式/失败转移/紧凑映射用例迁移到 apikey fixture。

**删了哪些文件**（18 个）
```
service/openai_oauth_service.go               service/openai_token_provider.go
service/openai_privacy_service.go             service/openai_codex_instructions_template.go
repository/openai_oauth_service.go            pkg/openai/oauth.go
handler/admin/openai_oauth_handler.go         handler/admin/account_codex_import.go
handler/admin/account_codex_import_test.go    pkg/openai/oauth_test.go
repository/openai_oauth_service_test.go        service/openai_oauth_service_auth_url_test.go
service/openai_oauth_service_refresh_test.go   service/openai_oauth_service_state_test.go
service/openai_token_provider_test.go          service/openai_passthrough_normalization_test.go
service/openai_privacy_retry_test.go           service/openai_subscription_test.go
```

**改了哪些文件**（约 38 个）：wire.go（cmd/server + service + repository）、wire_gen.go、req_client_pool.go、openai_gateway_service.go、openai_gateway_messages.go、openai_gateway_chat_completions.go、openai_codex_transform.go、openai_account_runtime_block_fastpath.go、openai_images.go、openai_images_responses.go、account.go、admin_service.go、token_refresh_service.go、token_refresher.go、oauth_service.go、handler/openai_gateway_handler.go、handler/openai_chat_completions.go、handler/openai_images.go、handler/admin/account_handler.go、handler/admin/account_data.go，及一批对应 `_test.go` 与 `server/api_contract_test.go`。

**gate 结果**
- `go build ./...`、`go vet ./...` 全绿。
- `go test -tags=unit ./internal/service ./internal/handler/...` 全部 ok（另验证 repository/server/domain/model 亦 ok）。
- wire 通过 `go generate ./cmd/server` 成功重生成。

**偏离与理由**
1. `token_refresher.go` 未整删（方案 §3.3 要求 P3 整删）：其 `TokenRefresher` 接口与 `ClaudeTokenRefresher` 仍被 P5 的 oauth_refresh_api.go / token_refresh_service.go 引用，整删会断 P5 文件编译；本阶段只删 `OpenAITokenRefresher`，整文件随 P5 删除。
2. `UpdateCodexUsageSnapshotFromHeaders` 及 `updateCodexUsageSnapshot`/`ParseCodexRateLimitHeaders` 保留：不在 P3 整删清单，且后两者仍被非 OAuth-gated 处调用（apikey 下为无害空操作）。
3. `gateway_oauth_metadata_test.go` 保留至 P5（被测函数 `buildOAuthMetadataUserID` 在 P5 的 gateway_service.go）。
4. 删除一个 P1 已破坏的 Gemini-OAuth 测试 `TestGeminiForwardAsChatCompletions_OAuthRoutesToGeminiAndReturnsChatFormat`（属 P4 范围，但在 P2 基线即已 FAIL，为达 P3 gate 绿一并清除）。
5. account.go 的 OpenAI OAuth getters/Extra 字段键与若干注释残留留待 P5/P7 统一收敛（无 Go 符号悬空，gate 全绿）。

---

### P4 — 删除 Gemini OAuth/CodeAssist 逆向链（仅后端）✅

| 项 | 内容 |
|----|------|
| 状态 | green |
| commit | `5d071f64` |

**做了什么**
- 整删 8 个纯逆向文件：service/gemini_oauth*.go、gemini_token_refresher.go、geminicli_codeassist.go；repository/gemini_oauth_client.go、geminicli_codeassist_client.go、gemini_drive_client.go；handler/admin/gemini_oauth_handler.go（含同名 `_test.go`）。
- `pkg/geminicli` 局部拆分：保留 `AIStudioBaseURL`/`DefaultModels`/`DefaultTestModel`，删 oauth.go / codeassist_types.go / drive_client.go / token_types.go / sanitize.go 及 constants 中的 CLI UA + cloudcode-pa + OAuth 常量。
- `gemini_token_provider.go` 去除死 OAuth token flow 与 refreshAPI/executor 字段，仅留 service_account 路径。
- 切除三个 compat/test 服务（gemini_messages_compat、gemini_chat_completions_compat、account_test_service）中引用 `GeminiCliBaseURL`/`GeminiCLIUserAgent` 的 OAuth token flow 分支，保留 AI Studio/apikey/service_account/Vertex。
- **新建 `gemini_tier.go`** 承接仍被 gemini_quota.go 复用的 Gemini 配额 tier 常量与 `canonicalGeminiTierID`/`canonicalGeminiTierIDForOAuthType`（平台无关的 tier 归一化）。
- wire 清理 `NewGeminiOAuthClient`/`NewGeminiCliCodeAssistClient`/`NewGeminiDriveClient`（repository）与 `NewGeminiOAuthService`（service）；必保 `NewGeminiTokenCache`/`NewRefreshTokenCache`。
- 删逆向测试 `TestCreateGeminiReqClient_ForceHTTP2Disabled`，保留通用 req-client pool 覆盖。
- `wire_gen.go` 已由 P1/P3 清理，本阶段无需改动。

**删了哪些文件**（16 个）
```
service/gemini_oauth.go                  service/gemini_oauth_service.go
service/gemini_oauth_service_test.go     service/gemini_token_refresher.go
service/geminicli_codeassist.go          repository/gemini_oauth_client.go
repository/geminicli_codeassist_client.go repository/gemini_drive_client.go
handler/admin/gemini_oauth_handler.go    pkg/geminicli/oauth.go
pkg/geminicli/oauth_test.go              pkg/geminicli/drive_client.go
pkg/geminicli/drive_client_test.go       pkg/geminicli/codeassist_types.go
pkg/geminicli/token_types.go             pkg/geminicli/sanitize.go
```

**改了哪些文件**（9 个）：pkg/geminicli/constants.go、repository/wire.go、repository/req_client_pool_test.go、service/wire.go、service/gemini_token_provider.go、service/gemini_messages_compat_service.go、service/gemini_chat_completions_compat_service.go、service/account_test_service.go、service/gemini_tier.go（新建）。

**gate 结果**
- `go build ./...`、`go vet ./...` 全绿（含修复 req_client_pool_test.go 引用已删 `createGeminiReqClient`）。
- `go test -tags=unit ./internal/service ./internal/repository ./internal/handler/...` 全部 ok（service 84.4s、repository 5.5s、handler 24.4s）。
- cmd/server 单独 build 通过，确认 wire 一致。
- 残留逆向符号扫描（GeminiOAuthService / GeminiOAuthClient / GeminiCliCodeAssistClient / GeminiTokenRefresher / GeminiDriveClient / geminicli.GeminiCliBaseURL / GeminiCLIUserAgent / SessionStore / OAuthConfig / DriveClient / TokenResponse / fetchProjectID）非测试代码 **NONE**。
- 前端 typecheck 未跑（本阶段限定仅后端）。

**偏离与理由**
1. `NewClaudeUsageFetcher` 未从 repository/wire.go 删除：仍被 `NewAccountUsageService` 构造器参数 `ClaudeUsageFetcher` 强依赖（account_usage_service.go 的 Claude OAuth usage 分支属 P5），现在删会断 wire 图，保留待 P5/P6。
2. **新建 gemini_tier.go**：gemini_quota.go（正规配额服务）依赖原定义在已删 gemini_oauth_service.go 的 `GeminiTier*` 常量 + `canonicalGeminiTierID*`，属平台无关 tier 归一化逻辑，迁出而非删除，符合「先迁移共享符号再删逆向文件」原则。
3. 删 pkg/geminicli/sanitize.go（方案未点名）：其 `SanitizeBodyForLogs` 唯一调用方是被删的 OAuth 文件，已成孤儿。
4. gemini compat 服务中残留的 `account.Type==AccountTypeOAuth` 行为型条件分支未全删——对存活类型恒为 false 且不阻断编译，`AccountTypeOAuth` 常量在 P7 删除时会被编译器强制清理。

---

### P5 — 删除 Claude OAuth 与共享 token 刷新设施 ✅

| 项 | 内容 |
|----|------|
| 状态 | green |
| commit | `30e5892f`（当前 HEAD）|

**做了什么**
- 整删纯逆向文件：repository/claude_oauth_service.go、service/oauth_service.go、token_refresh_service.go、oauth_refresh_api.go、token_cache_key.go、token_refresher.go，以及随刷新链失孤的 refresh_policy.go（`errRefreshSkipped` 定义在被删的 token_refresh_service.go，全部符号已无外部引用）。
- 共享工具 `MergeCredentials`（被 P6 antigravity_token_refresher 复用）迁出到新文件 `credentials_merge.go`。
- `claude_token_provider.go` 去掉死的 OAuth refresh 字段（refreshAPI/executor/refreshPolicy）与 `SetRefreshAPI`/`SetRefreshPolicy`，仅保留 service_account token。
- `pkg/oauth` 按 §2.3 只删 Claude 逆向专属 oauth.go（ClientID/AuthorizeURL/SessionStore/OAuthSession/TokenResponse/BuildAuthorizationURL，零外部引用）+ oauth_test.go；**保留 pkce.go**（GenerateState/GenerateCodeVerifier/GenerateCodeChallenge 仍被 6 个用户登录 auth_*oauth.go 引用，已确认）。
- `token_cache_invalidator.go` 按 §8.4 推荐方案缩小为正规缓存失效器：保留 `TokenCacheInvalidator` 接口与全部注入契约（RateLimitService/AccountHandler/Wire 不动），去掉 OAuth-only 守卫 + OpenAI/Claude/Antigravity 分支仅留 Gemini，通用函数 `CheckTokenVersion` 迁到 token_version.go。
- handler/dto/mappers.go 删 `IsAnthropicOAuthOrSetupToken` 门控的 5h 窗口/会话/TLS 伪装映射块。
- pkg/claude/constants.go 仅按符号删除确认零引用的死 OAuth 头（MessageBetaHeaderNoTools/WithTools、BetaContext1M）。
- cmd/server/wire.go 与 wire_gen.go（两处 provideCleanup 定义）同步移除 `TokenRefreshService` cleanup 入参与 Stop 步骤；service/wire.go ProviderSet 移除 NewOAuthService/ProvideOAuthRefreshAPI/ProvideTokenRefreshService。
- 删除/修订相关 `_test.go`（claude_token_provider_test 重写为仅测 service_account 守卫，token_cache_invalidator_test 删 OpenAI/Claude/Antigravity 用例，wire_gen_test 去 tokenRefreshSvc）。

**删了哪些文件**（15 个）
```
repository/claude_oauth_service.go        repository/claude_oauth_service_test.go
service/oauth_service.go                  service/oauth_service_test.go
service/token_refresh_service.go          service/token_refresh_service_test.go
service/oauth_refresh_api.go              service/oauth_refresh_api_test.go
service/token_cache_key.go                service/token_cache_key_test.go
service/token_refresher.go                service/token_refresher_test.go
service/refresh_policy.go                 pkg/oauth/oauth.go
pkg/oauth/oauth_test.go
```

**改了哪些文件**（12 个）：service/claude_token_provider.go（+test）、service/token_cache_invalidator.go（+test）、service/token_version.go、service/credentials_merge.go（新建）、service/wire.go、handler/dto/mappers.go、pkg/claude/constants.go、cmd/server/wire.go、cmd/server/wire_gen.go、cmd/server/wire_gen_test.go。

**gate 结果**：build / vet / `go test -tags=unit` 全绿（P5 提交时）。

**偏离与理由**
1. `repository/claude_usage_service.go` 未删（P5 提示列入整删）：删它需移除 `NewClaudeUsageFetcher` 并改 `NewAccountUsageService` 构造器签名（连带去掉 `*AntigravityQuotaFetcher`），而该构造器改签名归到 P6/§3.5。为不做半截构造器改动而推迟到 P6。当前文件零编译影响。
2. pkg/claude/constants.go 仅删 3 个确认零引用死符号；其余 P5 点名的 Beta* 符号（BetaOAuth/BetaTokenCounting/DefaultBetaHeader/HaikuBetaHeader/CountTokensBetaHeader/FullClaudeCodeMimicryBetas/BetaFastMode/PromptCachingScope/Effort/RedactThinking/ContextManagement/ExtendedCacheTTL）仍被 gateway_service.go（OAuth mimic 块）、upstream_models.go、account_test_service.go、openai_gateway_messages.go 引用，严格遵守 §9.1 铁律「删 Beta* 前命中须全部落在已删除的 OAuth/mimic 分支」，推迟到删 gateway_service.go OAuth mimic 的阶段。
3. gateway_service.go:3993 残留一句提及已删 `TokenRefreshService` 的注释（位于 `getOAuthToken` OAuth 分支内）——该函数属 gateway OAuth 清理范围（非 P5），为守阶段边界未改，非编译/vet 问题。

---

### P6 — 删除 Antigravity 逆向链 ⛔ 未完成（停止点）

| 项 | 内容 |
|----|------|
| 状态 | **失败 / 未提交** |
| commit | 无（工作区改动未提交，HEAD 仍为 P5 的 `30e5892f`）|

P6 在自动执行中已对工作区做了大量删除/编辑（约 40+ 个 antigravity 文件被删，约 60 个文件被改），但**未达成单元测试 gate，未提交**，编排器在此处停止。详见 §4「中途停止 — 剩余报错与接手步骤」。

---

### P7 / PF（前端） — 未开始

P7（统一收敛 `AccountTypeOAuth` / Beta* 死符号 / DROP COLUMN 迁移等）与前端集中改动 PF 均未启动。

---

## 3. 已知偏离与待拍板项处置说明

本次执行遵循方案默认/推荐口径，关键决策记录如下：

| 议题 | 本次处置 | 依据 |
|------|----------|------|
| 前端改动时机 | **集中到 PF 阶段**统一执行（尚未到达）。前端 typecheck 与后端编译相互独立，集中可避免逐阶段半破坏 import | 执行约定 |
| Lingjing | **保留**（routes/gateway.go 220–230 正规路由未动）| §8 默认 |
| 面板用量（promptanalytics 之外的用量面板）| **保留** | §8 默认 |
| token_cache_invalidator | **缩小为正规缓存失效器**（保留接口与注入契约，仅留 Gemini 分支）| §8.4 推荐 |
| require_oauth_only | 计划**删除**（属 P6/P7 范围，未到达）| §8 默认 |
| mcp_xml_inject | 计划**删除**（属 P6/P7 范围，未到达）| §8 默认 |
| DROP COLUMN 迁移 | 计划采用**方案 A**（属 P7 范围，未到达）| §8 默认 |

> 注：require_oauth_only / mcp_xml_inject / DROP COLUMN 的实际删除发生在 P6/P7，本次因停在 P6 尚未落地，上表记录的是既定口径。

阶段内的逐条技术性偏离已在 §2 各阶段「偏离与理由」中列出，核心模式有三类：
- **共享符号先迁移再删**（gemini_tier.go、credentials_merge.go），保证正规服务继续编译；
- **跨阶段耦合推迟**（token_refresher.go、claude_usage_service.go、Beta* 常量随其唯一引用者所在阶段一并删），守住「删前命中须全部落在已删分支」铁律；
- **gate 必要前置修复**（P1 遗留测试桩签名）一并纳入提交。

---

## 4. 中途停止 — 剩余报错与人工接手步骤

### 4.1 停止点诊断

执行结束时仓库实测（HEAD=`30e5892f`，工作区含未提交的 P6 改动）：

- `go build ./...` → **绿**
- `go vet ./...` → **绿**
- `go test -tags=unit ./internal/service ./internal/handler/...` → **红**

P6 的逆向源码删除本身已编译通过，但**一批单元测试仍以「四平台 / 含 antigravity」为断言基线**，删除 antigravity 后断言落空。这是删除遗留的测试断言未同步，**非源码编译错误**。

### 4.2 剩余失败用例清单

`internal/service`：
```
TestAdminService_BulkUpdateAccounts_MixedChannelPreCheckBlocksOnExistingConflict
TestGeminiMessagesCompatService_SelectAccountForModelWithExclusions_StickyMixedScheduling
TestGeminiMessagesCompatService_isModelSupportedByAccount
TestRateLimitService_HandleUpstreamError_OAuth401SetsTempUnschedulable
TestPlatformToProtocol
TestGetDefaultPlatformQuotas_ReturnsFourPlatforms
```

`internal/handler`：
```
TestHandleFailoverError_BasicSwitch
TestHandleFailoverError_ContextCanceled
TestHandleFailoverError_IntegrationScenario
```

`internal/handler/admin`：
```
TestSyncPricingModels_ValidPlatform_EmptyService
TestUpdateUserPlatformQuotas_Success
```

代表性断言（说明根因）：
- `TestUpdateUserPlatformQuotas_Success`：`expected 4 cache delete calls, got 3: [anthropic openai gemini]` —— token 缓存失效器已不再覆盖 antigravity（符合预期），测试断言需从 4 改为 3 并去掉 antigravity 期望项。
- `TestGetDefaultPlatformQuotas_ReturnsFourPlatforms` / `TestPlatformToProtocol`：默认平台集合 / 协议映射不再含 antigravity，需把「四平台」断言改为存活平台集合。

### 4.3 建议的人工接手步骤

1. **同步测试断言**（不是改源码）：逐个把上述失败用例中的 antigravity / 「四平台」期望改为存活平台集合（anthropic / openai / gemini）。
   - `TestGetDefaultPlatformQuotas_ReturnsFourPlatforms` 应改名/改断言为三平台（或方案规定的存活集合）。
   - `TestUpdateUserPlatformQuotas_Success` 的 cache delete 期望 4→3，移除 antigravity 项。
   - `TestPlatformToProtocol` 去掉 antigravity → 协议的映射断言。
   - failover / gemini compat / ratelimit / bulk-update / sync-pricing 用例：检查其 fixture 是否构造了 antigravity 账号或断言 antigravity 分支，按存活平台重建 fixture。
2. **复跑 P6 gate**：`cd backend && go build ./... && go vet ./... && go test -tags=unit ./internal/service ./internal/handler/...` 直到全绿。
3. **核对 P5 推迟项是否归入 P6**：claude_usage_service.go 整删 + `NewAccountUsageService` 构造器去掉 `ClaudeUsageFetcher`/`*AntigravityQuotaFetcher` 参数（§3.5），并 `go generate ./cmd/server` 重生成 wire。
4. **提交 P6**：`refactor(P6): 删除 Antigravity 逆向链`（消息尾部按规范加 Co-Authored-By）。
5. **继续 P7**：统一收敛 `AccountTypeOAuth` 常量、pkg/claude 剩余 Beta* 死符号、gateway_service.go OAuth mimic 块、require_oauth_only / mcp_xml_inject 删除、DROP COLUMN 迁移（方案 A）。
6. **最后 PF（前端）**：集中执行前端 Gemini/OAuth 收敛与 typecheck/vitest/lint。

> 风险提示：当前工作区是**未提交的半成品 P6**。若需暂停或交接，建议先 `git stash` 或在 feature 分支临时 commit（标注 WIP），避免与后续操作混淆。**不要在此状态直接推送。**

---

## 5. 验收建议命令

### 后端
```bash
cd /Users/leoobai/jiwu-project/SubPanel/backend
go build ./... && go vet ./... && go test -tags=unit ./...
```

### 前端（PF 完成后再执行）
```bash
cd /Users/leoobai/jiwu-project/SubPanel/frontend
pnpm run typecheck && pnpm exec vitest run && pnpm run lint:check
```

> 当前（停在 P6、未修测试断言）执行后端命令会在 `go test` 阶段红；build/vet 绿。前端命令在 PF 完成前不具备验收意义。

---

## 6. 结论

- **P2–P5 已干净落地**（4 个提交，逐阶段 build/vet/unit-test gate 全绿），完成了后端逆向网关路由关闭、OpenAI/Codex、Gemini OAuth/CodeAssist、Claude OAuth + 共享 token 刷新设施的删除。
- **P6（Antigravity）源码删除已完成但测试断言未同步**，单元测试 gate 红，编排器按「失败即停」中止，改动未提交。
- 接手只需**同步约 11 个测试用例的平台断言**（4→3 / 去 antigravity），即可让 P6 gate 转绿并提交，随后推进 P7 与前端 PF。

---

## 续跑 P7+PF

> 本小节为「逆向清理 v6」P7+PF 续跑结果，追加于原报告之后，不覆盖前文。续跑停止点：**PF（前端）未执行**。

### 当前 git HEAD（最近 15 条）

```text
02255e71 refactor(P7): 收敛账号类型/Wire/Ent schema 与 require_oauth_only/mcp_xml_inject 等死功能
c7b84e75 chore: 逆向清理 workflow 编排脚本
4db57b3b refactor(P6): 拆分 pkg/antigravity 共享类型并删除 antigravity 整平台
30e5892f refactor(P5): 删除 Claude OAuth 与共享 token 刷新设施
5d071f64 refactor(P4): 删除 Gemini OAuth/CodeAssist 逆向链
1648f93d refactor(P3): 删除 OpenAI/Codex 逆向链
c30ea59d refactor(P2): 删除后端逆向网关路由与账号 OAuth 入口
639e4c85 refactor(P1): 解耦正规路径对逆向 OAuth 的反向依赖
eb32a481 docs: 逆向清理 v6 方案基线 (P0)
f45abfce Implement code changes to enhance functionality and improve performance
6102d34c docs: 自定义功能列表瘦身与事实修正
e788e68a refactor: 优化多个服务中的扫描器缓冲区管理，减少内存分配并提高性能
c4c72614 fix: 更新代码修复报告，调整版本信息并记录修复进度
296e616a fix: 修复支付配置突变、空平台落库与更新校验后置
ee7555f4 Update model pricing hash and add AI solution assistance document
```

> 注：原报告第 6 节停在「P6 未提交」状态。续跑期间 P6 已落地为提交 `4db57b3b`，P7 已落地为提交 `02255e71`（当前 HEAD）。

---

### 阶段 P7 — 后端枚举/Ent/Wire/残留收敛

| 项 | 内容 |
|----|------|
| **状态** | 🟢 green（已达绿并提交） |
| **commit** | `02255e7126aebeeb28f5347423f70f32a63a24e4` |

**概述**

接手时上一个 agent 已完成绝大部分清理（枚举常量、Ent schema 字段删除、迁移 160、wire 重生成、绝大多数引用收敛）但**未达绿**。本轮完成的剩余收尾：

1. **残留注释/示例去措辞**：残留扫描中 6 个非测试源文件的 antigravity 注释/示例（`group.go`、`scheduler_snapshot_service.go`、`dto/types.go` ×2、`gateway_handler.go`、`gemini_v1beta_handler.go`）改为去 antigravity 措辞。
2. **测试契约收敛**：
   - `gateway_fork12_guard_test` 的 `ForcePlatform` 期望从 `>=3` 改为 `>=1`（antigravity 专用路由已移除，仅剩 lingjing）；
   - `gateway_protocol_routing_unit_test` 删除 2 个 antigravity 平台用例；
   - `error_passthrough_service_test` 三处 antigravity 平台断言改为 gemini（保留多平台匹配覆盖）；
   - `auth_service_platform_quota_test` 删 antigravity 配额键、记录数 4→3；
   - `setting`/`model_rate_limit`/`openai_upstream_transport`/`model_not_found` 测试的 antigravity 注释/字符串/函数名去除。
3. **修复上一个 agent 留下的 3 个失败单测**：
   - `TestShouldRefreshOpenAICodexSnapshot`（删 OAuth-gated 的 stale ws 快照断言）；
   - `TestGatewayCacheTTLGlobalSetting_TargetResolution`（cache TTL override 整体为 OAuth/SetupToken-only 逆向功能，账号级 override 断言改为恒 false）；
   - `TestOpenAIGatewayService_ProxyResponsesWebSocketFromClient_PassthroughHeadersUsePromptCacheAndTurnState`（session 隔离 `isolateOpenAISessionID` 原为 OAuth-only，apikey 路径用原始 key，断言改为 raw `"pcache_passthrough"`）。
4. **清理** `git diff --check` 报出的 4 个 EOF 多余空行。

`go generate ./ent` 与 `go generate ./cmd/server` 均离线成功且无新增 diff。迁移 `160_drop_group_oauth_only_and_mcp_xml_inject.sql` 已存在（方案 A：DROP COLUMN）。

**本轮删除的文件**：无（`filesDeleted` 为空）。

> 整删的测试文件（`gateway_handler_warmup_intercept_unit_test.go`、`account_codex_cli_only_allowed_clients_test.go`）、Ent schema 改动、迁移 160、wire 重生成均由上一个 agent 完成，故本轮删除清单为空。

**本轮编辑的文件**

```text
backend/internal/service/group.go
backend/internal/service/scheduler_snapshot_service.go
backend/internal/handler/dto/types.go
backend/internal/handler/gateway_handler.go
backend/internal/handler/gemini_v1beta_handler.go
backend/internal/server/routes/gateway_fork12_guard_test.go
backend/internal/server/routes/gateway_protocol_routing_unit_test.go
backend/internal/service/error_passthrough_service_test.go
backend/internal/service/auth_service_platform_quota_test.go
backend/internal/service/setting_service_platform_quota_test.go
backend/internal/service/model_rate_limit_test.go
backend/internal/service/openai_upstream_transport_error_handle_test.go
backend/internal/service/model_not_found_error_test.go
backend/internal/service/account_usage_service_test.go
backend/internal/service/gateway_body_order_test.go
backend/internal/service/openai_ws_forwarder_ingress_session_test.go
backend/internal/integration/e2e_gateway_test.go
backend/internal/repository/simple_mode_default_groups_integration_test.go
backend/internal/service/gateway_account_selection_test.go
backend/internal/service/ratelimit_service_401_test.go
```

**Gate 结果**

| Gate | 结果 |
|------|------|
| `go generate ./ent` | OK（无 diff） |
| `go generate ./cmd/server` | OK（离线成功，`wire_gen` 无 diff） |
| `go build ./...` | OK |
| `go vet ./...` | OK |
| `go test -tags=unit ./internal/service ./internal/repository ./internal/server ./internal/handler/...` | ALL ok |
| `go vet -tags=integration ./...` | OK |
| `go test -tags=integration -run xxx_NONE ./internal/repository` | ok（no tests）；`internal/integration` 用 e2e tag，integration tag 下 build constraints exclude 属预期 |
| `go test -tags=e2e -run xxx_NONE ./internal/integration ./internal/web` | OK |
| `git diff --check` | clean |
| 残留扫描 `rg 'AccountTypeOAuth\|AccountTypeSetupToken\|PlatformAntigravity\|require_oauth_only\|RequireOAuthOnly\|mcp_xml_inject\|MCPXMLInject\|ClearAntigravityQuotaScopes\|antigravity' backend/internal backend/cmd --glob '!**/auth_*oauth*.go'` | 0 命中 |

**偏离说明**

1. 本轮接手时绝大部分 P7 编辑已由上一个 agent 完成（含整删测试文件、Ent schema 改动、迁移 160、wire 重生成），故 `filesDeleted` 为空，`filesEdited` 仅列本轮实际改动文件。
2. 对 OAuth-only 逆向功能（cache TTL override、WS session 隔离、codex 快照 staleness 探测）采用上一个 agent 既定的「将方法收敛为恒 false 桩 + 同步修订/删除逆向断言」策略而非物理删除方法体——保持全分支一致性，未加回逆向代码、未跳过测试，删除的均为逆向功能断言。
3. 三处 antigravity 平台测试断言改为等价的 gemini 平台而非整删，以保留多平台匹配/配额的正规路径覆盖。
4. 前端 typecheck/vitest/lint 不在 P7 gate 范围内（P7 为后端最后一步），未执行。
5. 迁移文件采用方案 A（DROP COLUMN），上一个 agent 已创建，本轮仅核验内容正确。

**剩余报错**：无。

---

### 阶段 PF — 前端 Gemini/OAuth 收敛与门禁

| 项 | 内容 |
|----|------|
| **状态** | ⏳ 未执行（续跑停止点） |
| **commit** | — |

PF 阶段未执行。后端 P7 已达绿并提交（HEAD = `02255e71`），续跑在进入前端阶段前停止。

---

### 人工接手步骤（PF）

PF 为前端最后一步，后端已全绿，可独立推进：

1. **进入前端目录**：`cd /Users/leoobai/jiwu-project/SubPanel/frontend`
2. **收敛前端逆向引用**：集中清理前端 Gemini OAuth / antigravity 相关 UI、API 封装、i18n 文案与类型残留（账号类型选项、平台枚举、OAuth 授权入口等）。
3. **门禁三连**（依序，失败即停）：
   ```bash
   pnpm run typecheck
   pnpm exec vitest run
   pnpm run lint:check
   ```
   注意 Vitest 覆盖率门禁要求语句/分支/函数/行均达 80%（`frontend/vitest.config.ts`）；删除组件后同步删除/调整对应 `*.spec.ts`，勿因删测试拉低覆盖率。
4. **残留扫描**（前端）：`rg -i 'antigravity|oauth' frontend/src` 复核，确认仅剩正规路径所需引用。
5. **达绿后提交**：`git commit` 命名 `refactor(PF): 前端逆向 Gemini/OAuth 收敛`，保持逐阶段提交约定。
6. **全分支最低验证**：参照 `CLAUDE.md`「合并后最低验证」节，跑后端 unit + 前端指定 vitest + lint:check 复核。

---

## 复核与最终状态（2026-06-15，主线程接手完成 P6 收尾 + P7 + PF + 复核）

> 上文 P6「未完成」、P7/PF「未开始」为 workflow 阶段性记录，**已过时**。P6 测试契约、P7、PF 均已由主线程完成并提交；下为最终事实。

### 最终提交链
```
eb7b55bb refactor(P5-fix): 补删 P5 遗漏的 Claude OAuth 用量抓取逆向链
bf7c0a1f refactor(PF): 前端清理 antigravity/OAuth/Codex 残留 UI 与类型
02255e71 refactor(P7): 收敛账号类型/Wire/Ent schema 与 require_oauth_only/mcp_xml_inject 等死功能
c7b84e75 chore: 逆向清理 workflow 编排脚本
4db57b3b refactor(P6): 拆分 pkg/antigravity 共享类型并删除 antigravity 整平台
30e5892f refactor(P5): 删除 Claude OAuth 与共享 token 刷新设施
5d071f64 refactor(P4): 删除 Gemini OAuth/CodeAssist 逆向链
1648f93d refactor(P3): 删除 OpenAI/Codex 逆向链
c30ea59d refactor(P2): 删除后端逆向网关路由与账号 OAuth 入口
639e4c85 refactor(P1): 解耦正规路径对逆向 OAuth 的反向依赖
```

### 复核发现并修复的遗漏
1. **P5 漏删 Claude OAuth 用量抓取链**（已修，`eb7b55bb`）：`repository/claude_usage_service.go`（伪装 `claude-code/2.1.7` 抓 `api.anthropic.com/api/oauth/usage`）及 `account_usage_service.go` 的 `ClaudeUsageFetcher`/`fetchOAuthUsageRaw`/`buildUsageInfo`/`ClaudeUsageResponse` 等整条已无调用方的死链 + wire 注入。附录 A1 已列入但 P5 未执行。
2. **PF i18n 未清干净**（已补）：workflow PF agent 死于网络时，`en.ts` 仅部分清理、`zh.ts` 基本未动。已按 §5 白名单逐键删除 antigravity/setupToken/antigravityUserAgentVersion 等逆向键，保留社交登录 `oauthFlow.*`、Drive 备份 `googleDrive.*authorize*`、`auth.callback*`。
3. **PF 消费方未清**（已补）：CreateAccountModal/EditAccountModal/SettingsView/AccountsView/GroupsView 等引用了已删 composable/类型，已补全至 typecheck 绿。

### 最终验收结果（全部实跑）
| 维度 | 结果 |
|---|---|
| 后端 `go build ./...` / `go vet ./...` | ✅ |
| 后端单测（service/repository/server/handler/...） | ✅ 全过 |
| 集成编译 `go test -tags=integration -run NONE ./internal/repository` | ✅ |
| e2e 编译 `go test -tags=e2e -run NONE ./internal/integration` | ✅ |
| 后端逆向残留扫描 | ✅ 零命中 |
| 前端 `pnpm run typecheck` / `lint:check` | ✅（1 既有 warning） |
| 前端 `pnpm exec vitest run` | ⚠️ 744 通过 / **6 既有失败**（见下） |
| 前端逆向残留扫描 | ✅ 零命中 |

### ⚠️ 6 个前端既有失败（与本次清理无关）
`EmailVerifyView.spec.ts`(2) + `UsageView.spec.ts`(3) + `UsageTable.spec.ts`(1)，均为 image 计费 tooltip / 邮箱验证 pending 用例。**经核实在逆向清理起点 P0（`eb32a481`）即失败**，源自更早「图片按张计费」提交（`29918a0d`），属既有缺陷，**逆向清理引入零新增失败**。建议单独排查（疑似测试环境 i18n 渲染/异步时序，渲染出原始 key）。

### 必保项复核（§0.3）—— 全部存在/未误伤
- 用户登录 OAuth：`routes/auth.go`、`handler/auth_*oauth*.go`（14）、`pkg/oauth/pkce.go`（PKCE 已迁出，`oauth.go` 仅含逆向类型故整删，功能等价）✅
- 面板订阅/支付：`subscription_service.go`、`subscription_handler.go`、`internal/payment/`、前端 `payment/*` ✅
- 正规云接入：`claude_token_provider.go`、`vertex_service_account.go`、`gemini_token_cache.go`、`refresh_token_cache.go`、`PlatformLingjing` ✅
- Ent：`go generate ./ent` 已执行，生成代码已去 `require_oauth_only`/`mcp_xml_inject`；§8.6 方案A 迁移 `migrations/160_drop_group_oauth_only_and_mcp_xml_inject.sql` 已落地 ✅

### 结论
逆向清理 P1–P7 + 前端 PF + P5 补漏全部完成并提交；后端全维度绿、前端 typecheck/lint/残留绿。唯一遗留为 6 个**与清理无关的既有前端测试失败**，已定位、建议单独处理。
