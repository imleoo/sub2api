# zhiguofan 分支自定义功能列表

本文档记录 zhiguofan 分支相对于上游 `Wei-Shaw/sub2api` 的全部差异，用于同步上游代码时快速评估合并风险。

> **权威性说明**：本文档与 `CLAUDE.md` 保持一致，两者有冲突时以 **当前代码** 为准。
>
> **文档口径**：本文档只回答「fork 有什么、动了哪些文件、合并上游时要注意什么」；实现细节、开发过程、历史决策见 `CHANGELOG.md`、git 历史与 `claudedocs/` 对应文档。

---

## 快速风险评估表

同步上游后，按此表逐项 grep/检查，若文件被还原则需手动恢复。

> 同一文件只列一行；多个功能涉及同一文件时检查要点合并标注功能号。

| 风险等级 | 文件 / 位置 | 检查要点 |
|---------|-----------|---------|
| 🔴 高 | `backend/cmd/server/wire_gen.go` | adminUserHandler、NewSettingHandler 参数数量；lingjingGatewayService/lingjingPollRunner/lingjingHandler 注入；providerPricingHandler、endpointRepository、schedulerSnapshotService（含 dual-bucket cfg）注入；功能 24/34：`NewModelPricingHandler`（6 参，含 `accountTestService`+`modelRoutingService`）、`NewUsageHandler`（8 参，含 `modelRoutingService`）；功能 44（v2）：team/enterprise 系列 repository（member/invitation/activity_log/fund/enterprise_profile/department）、service（team/fund/auto_topup/enterprise/department）、handler（team/enterprise）及自动补给后台服务 cleanup 链注入；功能 45：statement/balance_snapshot repository 与 `BalanceSnapshotService` 月结后台服务（provideCleanup 形参 + stop 条目，`wire_gen_test.go` 调用同步加参）注入 |
| 🔴 高 | `backend/internal/server/routes/admin.go` | `provider-pricings` 路由组（**复数**，backend admin-API-only，无前端页面）；`model-pricings/sync-from-upstream`（功能 24）+ `sync-maas`（功能 34）；注：旧 `settings/model-discounts` 路由已随功能 24/34 移除（折扣并入 `model_pricings.discount_rate`）；功能 45：`users/:id/statement/export` 对账导出路由 |
| 🔴 高 | `backend/internal/server/routes/gateway.go` | masking 逻辑；`getGroupInboundProtocol` 函数及协议分流 switch；`/v1/images/generations` lingjing 平台判断；`/lingjing/v1/video/*` 路由组 |
| 🔴 高 | `backend/internal/domain/protocol.go` | 上游无此文件，确认未被删除/被 constants.go 覆盖；`InboundProtocol`/`OutboundProtocol` 常量集中、`ResolveInboundProtocol()` |
| 🔴 高 | `backend/ent/schema/usage_log.go` | P0-2 新增 9 列：`upstream_unit_price_*`、`upstream_total_cost`、`provider`、`pricing_source`、`async_task_id`、`cost_finalized_at`；合并后须重跑 `go generate ./ent` |
| 🔴 高 | `backend/internal/config/config.go` | `ProtocolBucketEnabled`（P5-6 回滚开关）、`Gateway.Scheduling.GenericRuntimeEnabled`（功能 25，env `GATEWAY_SCHEDULING_GENERIC_RUNTIME_ENABLED`）；上游改 config 结构时确认两字段未丢失。注：`USAGE_UPSTREAM_COST_ENABLED` **未实现为 config 字段/env**（仅注释提及），上游成本快照由 `ApplyUpstreamCostSnapshot` 命中 provider_pricing 与否驱动（功能 14） |
| 🔴 高 | `frontend/src/router/index.ts` | `/models`（`SupportedModels`）；`/admin/model-discounts` 现为**重定向**到 `/admin/model-pricings`（独立折扣页已删）。注：provider-pricing 无前端路由/页面（仅后端 admin API） |
| 🔴 高 | `backend/internal/service/billing_service.go` | 折扣以 inline `discountRate` 应用（读 `dbEntry.DiscountRate`，回退 `pricingService.GetDiscount(model)`；无独立 `applyDiscount()` 方法）；`GetModelPricing` 必须走 catalog 路径（不得重新引入 `fallbackPrices`）；功能 34：`CalculateSeedanceVideoCost` 必须走 `tier_pricing`（勿退回写死 ¥/秒） |
| 🔴 高 | `backend/internal/service/pricing_service.go` | `catalog`/`aliasIdx` 字段必须保留；不得重新引入 `pricingData`/`discounts`/`customPrices`；`GetCNYRate`、`ListAllModels`；功能 33/34：`ModelInfo.PricingUnit`/`OutputCostPerImage[Token]` 暴露，`ListEnabledCatalogModels` 按 mode 透传 image/video unit，灵境 seed/`matchLingjingModel` 等已删 |
| 🔴 高 | `backend/internal/service/model_catalog_service.go` | 上游无此文件（功能 26）；`UpsertModels` 是所有写路径的统一入口 |
| 🔴 高 | `backend/internal/service/model_catalog_seed.go` | 上游无此文件（功能 26）；`BootstrapPricingSeeds()` 16 条基础 seed 是计费安全的硬前置（灵境/豆包 seed 已被功能 34 删除） |
| 🔴 高 | `.github/workflows/*.yml` | 所有文件的 `on:` 必须为 `workflow_dispatch:` |
| 🔴 高 | 逆向订阅代码（antigravity / OAuth / Codex 整链） | 功能 35：本分支**已删除**整套订阅逆向；同步上游会**重新引入**，合并后须按 `逆向清理_执行报告_v6.md` + `逆向清理_后遗症收尾_follow-up.md` 重新删除。门禁：`rg -n "PlatformAntigravity\|AccountTypeOAuth\|AccountTypeSetupToken\|IsOAuth\|require_oauth_only\|mcp_xml_inject\|antigravity" backend/internal frontend/src` 仅剩用户登录 OAuth 白名单（合法保留点：`servertiming` 通用 host 分类、`admin_account.go` 遗留字段丢弃表、`admin.groups.platforms.antigravity`/`accounts.upstream.baseUrlHint` 兼容存量数据的 key）。2026-07-21 实测该门禁曾被遗漏执行、残留潜伏——已在 `script/pre_push_check.sh` 检查 5 自动化（推送范围新增行命中即拦截+人工确认），见 CLAUDE.md 已知陷阱「上游合并静默带回」条目 |
| 🔴 高 | `backend/internal/service/lingjing_gateway_service.go` | 功能 28：`lingjingModelCatalog` + `lingjingModelAliasToCanonical` + `lookupLingjingModel`；27 条模型路由是计费 + 网关分流唯一真源，与 migration 151/152 严格 1:1 |
| 🔴 高 | `backend/migrations/151_seed_lingjing_models.sql` / `152_update_lingjing_prices.sql` | 功能 28：模型 INSERT + 价格 UPDATE + 别名；model_id 写错计费 miss；价格单位错（per-image vs per-second）偏差 5-10× |
| 🔴 高 | `backend/ent/schema/user.go` | 功能 29：`phone` 字段；`go generate ./ent`，迁移 149 必跑 |
| 🔴 高 | `backend/internal/service/auth_phone.go` | 功能 29：上游无此文件；`NormalizePhone` E.164、`RegisterWithPhone`/`LoginWithPhone` |
| 🔴 高 | `backend/internal/service/sms_service.go` | 功能 29/30：`smsProvider` 接口 + `loadSmsClient` 三家分支（火山/腾讯/阿里） |
| 🔴 高 | `backend/internal/service/model_routing_service.go` `model_visibility.go` | 功能 34：上游无此两文件；可见模型口径唯一来源（`RoutableModelInfos`/`OperatorRoutableModelInfos`/`FilterVisibleModels`） |
| 🔴 高 | `backend/ent/schema/model_pricing.go` | 功能 33/34：`pricing_unit` enum 四值 + `tier_pricing` text 列；`go generate ./ent`，迁移 153/154/158 必跑 |
| 🟡 中 | `backend/internal/service/scheduler_snapshot_service.go` | L484/L786 平台列表含 `PlatformLingjing`；L650 `ProtocolBucketEnabled` 分支；`scheduler_dual_bucket.go` 双桶影子比较 |
| 🟡 中 | `backend/internal/service/gateway_service.go` | masking 调用分支；上游成本快照 `ApplyUpstreamCostSnapshot()` 调用**已移至 `gateway_usage_billing.go`**（上游 0.1.147 拆分，见功能 14）；功能 25：`listSchedulableAccounts` 包一层并入 generic（anthropic+gemini）、Forward generic→anthropic 直通、`buildUpstreamRequestAnthropicAPIKeyPassthrough` base_url 覆盖、`SetEndpointRepository`（flag 守卫）；功能 37：Forward 的 adapter gating（在 `response_masking` gate 之后、`anthropic_passthrough` 分支之前的 `InspectRequest` switch）+ `RequestRerouteError`/`invalidRequestFallbackGroupID` + 通用/直通两条流式路径的 `streamAdapter` 接管分支 |
| 🟡 中 | `backend/internal/service/openai_gateway_service.go` | `applyUpstreamCostSnapshot()` 调用；`SetLingjingService()`；功能 25：`listSchedulableAccounts` 并入 generic、eligibility generic 分支、token/base_url 覆盖、`SetEndpointRepository`（flag 守卫） |
| 🟡 中 | `backend/internal/handler/admin/provider_pricing_handler.go` | 上游无此文件；`NewProviderPricingHandler` 须在 wire_gen 注入 |
| 🟡 中 | `backend/internal/service/upstream_models.go` | 功能 24：`FetchModelsByConfig`（无账号按 base_url+key 拉取）；`upstreamModelEntry.owned_by`（newapi 兼容） |
| 🟡 中 | `backend/internal/handler/admin/model_pricing_handler.go` | 功能 24：`SyncFromUpstream` + `accountTestService`；功能 34：`List` 默认广场口径（`VisibleOnly=true`+`FilterVisibleModels`，不解析 `visible_only`）——**例外：`provider` 显式筛选、以及 `for_whitelist=true`（账号编辑弹窗的模型白名单选择器专用）均跳过广场过滤**；`SyncMaas`（替换三旧端点）、Create/Update 收 `pricing_unit` 四值 + `output_cost_per_image` |
| 🟡 中 | `backend/internal/service/openai_gateway_chat_completions_raw.go` | 功能 25：`forwardAsRawChatCompletions` 对 generic 用账号 key + 端点 base_url |
| 🟡 中 | `backend/internal/service/gemini_messages_compat_service.go` | 功能 25：`endpointRepo` + `SetEndpointRepository`；`ForwardNative` APIKey 路径对 generic 用端点 base_url |
| 🟡 中 | `frontend/src/views/user/ModelsView.vue` | 白名单过滤、图片模型、复制按钮 |
| 🟡 中 | `backend/internal/handler/usage_handler.go` | `ListModels` 完整性（功能 34 改走 `RoutableModelInfos`+`FilterVisibleModels`） |
| 🟡 中 | `backend/internal/handler/admin/user_handler.go` | `GetUserUsage` 调用真实 usageLogRepo；`CreateUserRequest.Username` 必填 |
| 🟡 中 | `backend/internal/handler/admin/setting_handler.go` | `GetModelDiscounts`/`UpdateModelDiscounts`、`CurrencyMode`/`CNYRate`/`UITheme`；功能 29/30/31：`PhoneRegisterEnabled`+`SmsProvider`+三家凭证+`PasswordLoginEnabled` |
| 🟡 中 | `frontend/src/i18n/locales/zh.ts` & `en.ts` | `models.*`、`nav.models`、`admin.accounts.providers.*`、`admin.dashboard.providers.*`、`auth.usernameLabel/usernameRequired` |
| 🟡 中 | `frontend/src/utils/platformColors.ts` | `lingjing` 颜色条目及 `isPlatform()` |
| 🟡 中 | `frontend/src/types/index.ts` | `GroupPlatform`/`AccountPlatform` 含 `'lingjing'`/`'generic'`（功能 35 已删 `'antigravity'` 及 `AccountType` 的 `oauth`/`setup-token`）；`RegisterRequest.username` 必填 |
| 🟡 中 | `backend/migrations/145_model_pricings_ssot_columns.sql` | 功能 26：14 列 + `pricing_status` 回填；合并后须执行 |
| 🟡 中 | `backend/internal/service/account_test_service.go` | 功能 28：`testLingjingAccountConnection`（SSE 流），视频模型强制 prompt + 8min 超时 |
| 🟡 中 | `backend/internal/service/lingjing_client.go` | 功能 28：apiId 常量 51 个，命名 `LingjingAPIID<产品><版本><任务类型>` |
| 🟡 中 | `backend/internal/pkg/sms/{volcengine,tencent,aliyun}.go` | 功能 29/30：上游无此目录；三家短信签名/请求实现 |
| 🟡 中 | `backend/internal/repository/sms_cache.go` | 功能 29：上游无此文件；验证码 Redis 缓存（节流+校验） |
| 🟡 中 | `backend/internal/server/routes/auth.go` | 功能 29：`/auth/{send-sms-code,phone-register,phone-login}` 三个限流路由 |
| 🟡 中 | `backend/internal/service/setting_service.go` & `settings_view.go` | 功能 29/30/31：`IsPhoneRegisterEnabled`、`SmsProvider`、`PasswordLoginEnabled`（默认 true） |
| 🔴 高 | `backend/internal/service/setting_update.go`（`buildSystemSettingsUpdates`）、`setting_public.go`（`GetPublicSettings`） | 上游 0.1.147 合并（`7c9e09d29`）曾**静默删除** `buildSystemSettingsUpdates` 尾部一整段 fork 字段写入（`currency_mode`/`ui_theme`/`phone_register` 等只进内存缓存、重启即丢，`git log -S` 也看不出diff），已在 `07af185e3` 修复并加回归测试 `setting_fork_fields_persist_test.go`；这是"合并静默吞掉 fork 代码块"模式第 3 次出现（另两次：GitHub Workflows 触发器、批量改账号丢模型映射），上游改这两个函数时必须逐行核对 fork 字段块是否完整保留 |
| 🔴 高 | `backend/internal/service/admin_compliance.go` | `GetAdminComplianceStatus` 硬编码 `Required: false`（fork 永久禁用管理员部署合规承诺门控，弹窗/中间件恒放行，仅保留历史确认记录展示）；上游若重构该文件的字段名/校验路径，须重新核对 `Required` 恒为 false 未被覆盖 |
| 🟡 中 | `backend/internal/service/domain_constants.go` | 功能 29/30/31：`SettingKeyPhoneRegisterEnabled`、`SettingKeySmsFrontend`、`SettingKey{Volcengine,Tencent,Aliyun}*`、`SettingKeyPasswordLoginEnabled` |
| 🟡 中 | `frontend/src/views/auth/{LoginView,RegisterView}.vue` | 功能 29/31：手机号模式 + 验证码倒计时 + 密码/手机号切换链接 |
| 🟡 中 | `frontend/src/views/admin/SettingsView.vue` | 功能 29/30/31：手机号注册 + 短信服务商 + 密码登录开关配置区 |
| 🟡 中 | `backend/internal/service/model_pricing_repository.go` | 功能 32/34：`ModelPricingListFilter.VisibleOnly` + `ModelPricingService`；`DBModelPricing.TierPricing` + `VideoPriceTier` + image/video unit 常量 |
| 🟡 中 | `backend/internal/repository/model_pricing_repository.go` | 功能 34：`List` visible_only 改用 `RoutableModelIDs`；`BulkUpsertMaas` + `tier_pricing` 读写；旧 CTE JOIN 已移除 |
| 🟡 中 | `frontend/src/views/admin/ModelPricingsView.vue` | 功能 34：已删「只显示可见模型」复选框；「图片（按张）」计价 + `output_cost_per_image` 表单；「同步 MaaS 定价」通用弹窗 |
| 🟡 中 | `backend/internal/service/{wanjie_sync,lingjing_video_pricing}.go` | 功能 34：通用 `ParseMaasFromBytes(source)`；视频真 token 计费（`tier_pricing`+火山公式）；`doubao_sync.go`/`lingjing_sync.go` 及 embed json 已删，勿引回 |
| 🟡 中 | `backend/internal/service/account_adapter.go`、`backend/internal/pkg/claude/{conversecompat,kirocompat}/` | 功能 37：账号级协议适配（Bedrock Converse / Kiro 归一 / 能力兜底）；上游无此三处，纯新增 |
| 🟡 中 | `backend/internal/handler/gateway_handler.go` | 功能 37：选号循环捕获 `RequestRerouteError` → `currentGroupID` 换组（计费仍按原 `currentAPIKey`）；上游改循环时确认未覆盖 |
| 🟢 低 | `backend/internal/service/bedrock_stream.go` | 功能 37：AWS Bedrock 流式路径 adapter 接管（Content-Type/EmitStreamEvent/FinishStream） |
| 🟢 低 | `backend/internal/service/generic_gateway.go` | 功能 25：上游无此文件；generic 端点选择/解析 |
| 🟢 低 | `backend/data/{model_discounts,model_pricing}.json` | 上游无此文件，不被覆盖；`{doubao,lingjing,wanjie}.json` 为运营上传模板（非 embed） |
| 🟢 低 | `backend/ent/schema/{lingjing_task,provider_pricing,endpoint}.go` | 上游无，通常不冲突；合并后须重跑 `go generate ./ent` |
| 🟢 低 | `backend/internal/service/lingjing_*.go` | 5 个新增文件，上游无，通常不冲突 |
| 🟢 低 | `backend/internal/service/{upstream_cost,provider_normalize,protocol_bridge_registry,scheduler_dual_bucket}.go` | 上游无；分别为成本快照、provider 归一化、协议桥注册表、双桶影子比较 |
| 🟢 低 | `backend/internal/repository/{lingjing_task_repo,provider_pricing_repository,endpoint_repository}.go`、`backend/internal/handler/lingjing_handler.go` | 上游无此文件 |
| 🟢 低 | `backend/internal/pkg/apicompat/*.go` | P3/P4 新增：协议桥 + SSE replay 框架；上游无 |
| 🟢 低 | `backend/cmd/migrate_provider/` | 上游无此目录；P1-1 历史回填 job |
| 🟢 低 | `statics/`、`script/`、`tools/`、`test/` | 上游无这些目录，合并通常不冲突 |
| 🟢 低 | `README.md` / `README_CN.md` / `README_JA.md` | 上游含大量赞助商/演示内容，合并后需重新精简（功能 22） |
| 🟢 低 | `.goreleaser.yaml` / `.goreleaser.simple.yaml` | `release.footer` 段含安装文档，上游修改时确认未被覆盖 |
| 🟢 低 | `backend/internal/repository/{ops_repo,ops_repo_request_details}.go` | 功能 20：`LEFT JOIN api_keys`、`client_ip`+`api_key_name` 列追加 |
| 🟢 低 | `backend/migrations/{149,150}_*.sql` | 功能 29/30：`phone` 列 + `signup_source` CHECK；合并后须执行 |
| 🟢 低 | `backend/migrations/{153,154,155,158}_*.sql` | 功能 33/34：`pricing_unit` 四值 CHECK + 数据清洗 + `tier_pricing` 列；合并后须执行 |
| 🟢 低 | `frontend/src/views/user/PlaygroundView.vue`、`frontend/src/components/playground/MessageBubble.vue`、`frontend/src/api/playground.ts` | 功能 38：API Playground 试玩页 + `/playground` 路由；上游无，通常不冲突 |
| 🟢 低 | `backend/internal/service/dual_bucket_stats.go`、`backend/internal/handler/admin/dual_bucket_stats_handler.go` | 功能 39：双桶统计看板；路由 `GET /admin/scheduler/dual-bucket-stats`，须在 `wire_gen.go` 注入 `dualBucketStatsHandler` 并进 `ProvideAdminHandlers` |
| 🟡 中 | `backend/internal/server/routes/user.go` | 功能 44（v2）：新增 `/user/enterprise/*`、`/user/team/*` 路由组。注意：v1 曾在 `api_key/usage/payment/user_handler/idempotency_helper` 及 `routes/payment.go` 引入 `GetResourceOwnerID`/`teamContext`/`teamAudit`，v2 已全部回退——这些文件应与上游一致，合并时若发现残留调用即为清理遗漏；功能 45：usage 组内 `statement{,/months,/export}` 三条静态路由（须先于 `/:id` 语义存在） |
| 🟡 中 | `frontend/src/components/admin/user/UserStatsModal.vue`、`frontend/src/components/user/PlatformUsageBreakdown.vue` | 功能 44（v2）复用功能 3 的管理员统计弹窗与平台费用悬浮组件，供企业管理页「成员使用统计」使用：`UserStatsModal.vue` 的 `user` prop 放宽为 `{id,email,status?}` 并新增可选 `fetchStats` prop（默认仍是 `adminAPI.users.getUserUsageStats`，向后兼容）；`PlatformUsageBreakdown.vue` 有可选 `align: 'left'\|'right'`（默认 `'right'`，向后兼容）控制悬浮层弹出方向，并将悬浮层 `Teleport` 到 `body` + `position:fixed` 定位（同 `UsageTable.vue`），逃出 `DataTable` 的 `.table-wrapper`（`overflow:auto`）裁剪。上游若重写这两个文件，须确认 `fetchStats`/`align` prop 与 Teleport 悬浮层未被覆盖丢失 |
| 🔴 高 | `backend/internal/service/gateway_claude_body.go` | 功能 40：上游 `gateway_claude_oauth_body.go` 剥离 OAuth 拟态后保留的共享工具（`replaceModelInBody`、cache-control 归一/上限、sticky session 种子、dateline 归一）。上游同步会重新引入原混装文件，须再次按符号拆分 |
| 🔴 高 | `backend/internal/service/gateway_fork_support.go` | 功能 40：上游拆分 `gateway_service.go` 后无对应位置的 fork 函数（`listSchedulableAccountsBase`/`invalidRequestFallbackGroupID`/`calculateVideoCost`/`resolveProviderKey`/`resolveUpstreamModelForCost`/`claudeUsageToTokens`） |
| 🟡 中 | `frontend/src/i18n/locales/{zh,en}/fork.ts`、`frontend/src/i18n/locales/forkMerge.ts` | 功能 41：fork 自定义 i18n 键（276/278 条）+ 深合并工具；上游把语言包拆成模块目录后，fork 键集中于此，不再与上游模块冲突 |
| 🟢 低 | `backend/internal/service/{endpoint_selector,endpoint_types,bridge_capabilities,gemini_compat_helpers,gemini_tier,protocol_consistency_check,account_schedulable,gateway_temp_unschedule,credentials_merge}.go` | 功能 25/37 拆分 helper（endpoint 选择/类型、协议桥能力、gemini 兼容/分级、协议一致性校验、可调度性、临时不可调度、凭据合并）；上游无，通常不冲突 |
| 🟡 中 | `backend/internal/service/{statement_port,balance_snapshot_service}.go`、`backend/internal/repository/{statement_repo,balance_snapshot_repo}.go` | 功能 45：月度对账聚合口径（Deposit=completed 订单+划入；Credit 排除充值链路兑换码防双算；反推法期初期末）与月结后台任务；上游无这些文件，但口径依赖 `payment_orders`/`redeem_codes`/`team_fund_transfers`/`usage_logs` 表结构，上游改这些表的字段/状态机时须重审 SQL |
| 🟢 低 | `backend/ent/schema/balance_snapshot.go`、`backend/migrations/183_add_balance_snapshots.sql` | 功能 45：`balance_snapshots` 月结快照表（UNIQUE(user_id,period)，无 users 外键台账语义）；合并后须重跑 `go generate ./ent`、迁移须执行 |
| 🟡 中 | `frontend/src/views/admin/UsersView.vue` | 功能 45：「更多」菜单「对账导出」项 + BaseDialog 选月弹窗 + `handleExportStatement`/`confirmExportStatement`，`:actions-count="8"`；上游重写该文件时须保留这一段并核对 actions-count |

---

## 功能详细说明

> 每节固定三段：功能一句话 → 关键文件 → 合并注意。文件级检查要点以上方风险表为准，此处不重复展开。

### 1. 自定义版本号

上游版本主号 +1（上游 `0.1.x` → fork `1.1.x`），载体 `backend/cmd/server/VERSION`（主号固定 `1`，实际值以文件为准，勿在文档硬编码）。

**合并注意**：`./script/sync_upstream_to_zhiguofan.sh` 同步后自动改主号；手动 merge 必须检查此文件。

### 2. 用户端模型广场 (Models)

用户查看账号白名单内可用模型、定价、折扣、图片模型；折后价 + 人民币价由后端 `cny_rate` 驱动；只显示 `credentials.model_mapping` 白名单内模型（含 `gpt-image-*`/`dall-e*`/`imagen-*`/`gemini-*-image` 图片模型），带复制模型名。

**文件**：前端 `frontend/src/views/user/ModelsView.vue`（+ `ModelsView.spec.ts`）、`frontend/src/api/models.ts`（`ModelInfo.discount_rate`/`ModelsResponse.cny_rate`）、`/models` 路由、i18n `models.*`/`nav.models`；后端 `routes/user.go`（`GET /api/v1/models`）、`usage_handler.go::ListModels`。

**合并注意**：路由 `/models`、`ModelsView.vue`、`ListModels`、i18n 键必须保留；不要退回「展示全量定价表」行为。

### 3. 管理员用户使用统计

管理员查看单用户使用数据（汇总/每日历史/模型/端点分布）：`GET /api/v1/admin/users/:id/usage`。

**文件**：前端 `UsersView.vue`（入口）、`UserStatsModal.vue`、`frontend/src/api/admin/users.ts`；后端 `admin/user_handler.go::GetUserUsage`、`admin_service.go::GetUserUsageStats`（签名 `(startTime, endTime time.Time)`）、`usage_log_repo.go::GetUserUsageStats`（复用 `getEntityUsageStats` 私有方法）。

**合并注意**：`NewAdminService` 参数需含 `usageLogRepo`；不能退回返回 mock 数据的旧实现。

### 4. 词云分析 (Prompt Analytics)（已移除）

已于 2026-06-08 整体移除（promptanalytics 插件、`keyword_stat` schema、路由/前端/i18n/wire 注入全删）；迁移 `159_drop_keyword_stats.sql` DROP 数据表，历史迁移 `082_create_keyword_stats.sql` 保留不动。功能号占位，不再复用。

### 5. 模型折扣与人民币定价

模型折扣率计费自动应用，用户端展示折后价与人民币换算价。**独立折扣页/路由已被功能 24/34 取代**：折扣统一为 `model_pricings.discount_rate` 在「模型定价」页管理；`ModelDiscountsView.vue` 已删、`/admin/model-discounts` 改重定向、后端 `settings/model-discounts` 路由已移除；计费侧 `pricingService.GetDiscount()` + DB `discount_rate` 仍是有效兜底路径。

**文件**：`backend/data/{model_discounts,model_pricing}.json`（冷启动兜底/运营模板，含 deepseek 等参考价，**非 seed 内置**，不违背功能 34）；`pricing_service.go`（`catalog`/`aliasIdx`、`SaveDiscounts`/`GetDiscount`/`GetCNYRate`/`GetCurrencyMode`/`ListAllModels`、`ModelInfo`）；`billing_service.go`（inline `discountRate`）；`config.go`（`PricingConfig.CNYRate` 默认 7、`DiscountFile`）；`setting_handler.go`/`dto/settings.go`/`setting_service.go`/`settings_view.go`（`CurrencyMode`/`CNYRate`）；`domain_constants.go`（`SettingKeyCNYRate`/`SettingKeyCurrencyMode`）；前端 `stores/app.ts`（`currencyMode`/`cnyRate`）、`CurrencySetupModal.vue`（首次货币设置向导，`AppLayout.vue` 触发，上游无此文件）、`ModelsView.vue`、`UsageView.vue` 及费用展示共享组件 `UsageStatsCards.vue`/`UsageTable.vue`/`GroupDistributionChart.vue`/`ModelDistributionChart.vue`/`UserBreakdownSubTable.vue`（`formatUSD`/`formatCost` 人民币换算）。

**合并注意**：折扣以 inline `discountRate` 应用，不存在 `applyDiscount()` 方法；上游不得重新引入 `fallbackPrices`（功能 26 已删，计费走 catalog 路径）；`NewSettingHandler` 新增两参数须在 `wire_gen.go` 同步；`cny_rate` 响应字段不能丢；`model_catalog_service.go`/`model_catalog_seed.go` 纯新增文件不能丢；上游若引回 `settings/model-discounts` 端点与 `ModelDiscountsView.vue` 需再删。`UsageView.vue` 曾被上游 `#2007` 整体重写（费用展示委托共享组件，冲突集中在 CSV 导出表头/数值格式化）；合并这些文件时务必保留 `formatUSD`/`formatCost` 人民币换算，且上游新增的 `showAccountCost`/`strikeStandardCost` 条件渲染需同时保留，禁止 `git checkout --ours/--theirs` 整文件二选一。

### 6. UI 主题扩展

新增 `violet`/`orange` 两套主题（`<html>` class 挂载）。

**文件**：前端 `main.ts`（`applyUITheme()`）、`stores/app.ts`（`uiTheme`）、`style.css`（`.theme-violet`/`.theme-orange` 变量）；后端 `setting_handler.go`（`UITheme` 枚举验证 `teal`/`violet`/`orange`）、`settings_view.go` & `setting_service.go`（持久化）。

**合并注意**：上游无此字段，合并 `setting_handler.go` 时确认 `UITheme` 未被丢弃。

### 7. 语言切换（Cookie 持久化）

语言偏好经 Cookie 持久化，刷新保持。前端 `main.ts` bootstrap 读 Cookie；fork i18n 键现集中在 `locales/{zh,en}/fork.ts`（见功能 41）。

**合并注意**：合并 i18n 时须保留所有 fork 新增键。

### 8. 网关响应遮蔽 (Response Masking)

账号级开关：用户问「你是谁/什么模型」时网关拦截返回固定身份回答，不透传上游。

**文件**：`gateway_response_masking.go`（新增：身份问题正则、模型名映射、SSE/非流式假响应）、`gateway_service.go`（`IsResponseMaskingEnabled()` + `isIdentityQuestion()` 分支）、`account.go`（`IsResponseMaskingEnabled()`）。

**合并注意**：`gateway_response_masking.go` 纯新增；`gateway_service.go` 合并时确认 masking 调用分支未被覆盖。

### 9. GitHub Actions 全部禁用自动触发

避免 Actions 额度耗尽：`.github/workflows/` 全部 6 个文件（backend-ci / security-scan / docker-push / release / sync-upstream / cla）的 `on:` 仅保留 `workflow_dispatch:`。

**合并注意**：上游携带自动触发配置，合并后必须逐文件检查 `on:` 段并还原。

### 10. 脚本与工具

上游无以下文件，通常不冲突，合并后确认未被误删：`script/dev_local.sh`（本地起停，后端 :8082 前端 :3002）、`script/sync_upstream_to_zhiguofan.sh`（上游同步）、`tools/openai_quick_test.py`、`test/api_reconciliation_test.go`、`statics/we2ai.com/index.html`、`statics/api.cxm.icu/index.html`。

### 11. 移除 OAuth 授权流程（前端 UI）

从账号创建/编辑弹窗移除全部 OAuth UI 入口与 handler 函数（`CreateAccountModal.vue`/`EditAccountModal.vue`/i18n）。**后续已被功能 35 扩展为整链删除**（前端 UI + 后端 OAuth 账号类型全删），合并口径以功能 35 为准。

### 12. 京东云灵境（Doubao Seedream / Seedance）接入

灵境平台三类能力：Seedream 生图（同步轮询）、Seedance 文/图生视频（异步 taskId），Base URL `https://model.jdcloud.com`。

**新增文件**：`ent/schema/lingjing_task.go`、`service/{lingjing_task_port,lingjing_client,lingjing_gateway_service,lingjing_images,lingjing_poll_runner}.go`、`repository/lingjing_task_repo.go`、`handler/lingjing_handler.go`。
**修改要点**：`domain/constants.go` 加 `PlatformLingjing`；`scheduler_snapshot_service.go` 平台列表；`account.go` 加 `IsLingjing()`/`GetLingjingAPIKey()`；`openai_gateway_service.go` 加 `SetLingjingService()`；`openai_images.go` `ForwardImages` 前置 `IsLingjing()` 分支；`routes/gateway.go` 图片路由放行 lingjing + `/lingjing/v1/video/*` 路由组；wire 全链注入（见速查表）；前端 `types/index.ts`/`platformColors.ts`/`CreateAccountModal.vue`/`EditAccountModal.vue` 加 lingjing 平台支持。
**路由**：`POST /v1/images/generations`（自动分流）、`POST /lingjing/v1/video/submit`（202 + taskId）、`GET /lingjing/v1/video/:taskId`。apiId 常量已由功能 28 扩展为 51 个，对照见 [`claudedocs/lingjing/model_catalog.md`](claudedocs/lingjing/model_catalog.md)。

**合并注意**：`gateway.go` 的 lingjing 平台判断和 `/lingjing/v1/video/*` 路由组不能丢；`wire_gen.go` lingjing 初始化链 + `provideCleanup`（含 `lingjingPollRunner`）不能丢；上游改 `openai_images.go::ForwardImages` 时确认 `IsLingjing()` 前置分支仍在。

### 13. 用户注册/创建必填用户名

注册接口与管理员创建用户均要求用户名必填。

**文件**：后端 `auth_handler.go`（`RegisterRequest.Username` `binding:"required"`）、`auth_service.go`（`RegisterWithVerification` 加 `username` 参数）、`admin/user_handler.go`（`CreateUserRequest.Username` 必填）；前端 `types/index.ts`（`RegisterRequest.username` 非 optional）、`RegisterView.vue`/`EmailVerifyView.vue`/`UserCreateModal.vue`、i18n `usernameLabel`/`usernamePlaceholder`/`usernameRequired`。

**合并注意**：上游 `RegisterRequest` 无 `username`，合并后确认前后端类型均含必填 `username`。

### 14. 上游成本快照（MAAS Phase 0）

每条 UsageLog 附加上游原始成本（9 列），支持毛利率查询与对账。

**新增文件**：`ent/schema/provider_pricing.go`、`repository/provider_pricing_repository.go`、`service/upstream_cost.go`（`UpstreamCostResolver` + `applyUpstreamCostSnapshot()`）、`handler/admin/provider_pricing_handler.go`（独立 handler，不进 SettingHandler）、`script/reconcile_upstream_cost.sh`。
**装配点（3 处）**：`gateway_usage_billing.go`（Claude 主路径，上游 0.1.147 从 `gateway_service.go` 拆出）、`openai_gateway_usage.go`（OpenAI 路径）、`lingjing_poll_runner.go`（异步路径，强制写入）。
**API**：`GET/POST/PUT/DELETE /api/v1/admin/provider-pricings`（复数，无前端页面）。
**门控**：由 `ApplyUpstreamCostSnapshot()` 命中 provider_pricing 与否驱动（命中填 9 列，否则 NULL）；`USAGE_UPSTREAM_COST_ENABLED` **未实现为 env/config**，不要据此找开关。

**合并注意**：`usage_log.go` schema 变更后必须 `go generate ./ent`；上游改 UsageLog 构造时确认三个装配点的 `ApplyUpstreamCostSnapshot()` 调用未丢。

### 15. 供应商规范化与分布视图（MAAS Phase 1）

账号 `extra.provider` 写入规范化 provider_key；新增 Provider/Account 跨 group 分布图表。

**文件**：`service/provider_normalize.go`（`NormalizeProvider()` 别名表）、`cmd/migrate_provider/`（历史回填 job）、`usage_log_repo.go`（`GetStatsByProvider`/`GetAccountStatsCrossGroup`，复用 `getEntityUsageStats`）；前端 `ProviderDistributionChart.vue`/`AccountDistributionChart.vue`、`CreateAccountModal.vue`（Provider 预设下拉 + 自定义 base_url）、i18n `providers.*` 键。

**合并注意**：repo 新增方法必须复用 `getEntityUsageStats`；回填 job 仅按 `extra.provider` 细粒度写入，**不按 `account.platform` 粗粒度回填**（防 provider_key 桶污染）。

### 16. InboundProtocol / OutboundProtocol 双写（MAAS Phase 2）

Group 加 `inbound_protocol`、Account 加 `outbound_protocol`，路由分流优先读新字段，`platform` 保留为回退 alias。

**文件**：`domain/protocol.go`（新增：协议常量 + `ResolveInboundProtocol()`）；`ent/schema/{group,account}.go`（新字段）；`domain_constants.go`（`PlatformOpenAI` 等改 alias）；`routes/gateway.go`（`getGroupInboundProtocol()` + 约 10 处协议 switch 改造）。

**合并注意**：`gateway.go` 高改动，同步后逐行检查 `getGroupInboundProtocol()` 与协议 switch 完整；schema 变更重跑 `go generate ./ent`；`domain_constants.go` 可能与上游 alias 冲突。守护：`grep -q '/lingjing/v1/video' backend/internal/server/routes/gateway.go`。

### 17. 协议桥接注册表 Bridge Registry（MAAS Phase 3）

协议转发逻辑抽到注册表，补齐 N×M 协议矩阵 + SSE 回归测试集。

**文件**：`service/protocol_bridge_registry.go`（`ProtocolBridgeRegistry` + `ProtocolBridge` interface）；`pkg/apicompat/` 下 `anthropic_to_chatcompletions.go`/`chatcompletions_to_anthropic.go`/`responses_to_chatcompletions.go`/`chatcompletions_to_responses.go`/`sse_replay.go`；`backend/testdata/sse/` fixtures。

**合并注意**：`apicompat/` 全新增；`protocol_bridge_registry.go` 须在 wire 正确初始化；所有桥实现保持接口满足。

### 18. Gemini 入站协议桥（MAAS Phase 4）

`/v1beta/*` Gemini 路由可路由到 Anthropic/OpenAI 账号，UsageLog 记 `inbound_protocol='gemini_v1beta'`。

**文件**：`pkg/apicompat/{gemini_to_openai,gemini_to_anthropic,gemini_types}.go`（新增）；`handler/gemini_v1beta_handler.go`（接入 Bridge Registry，native 路径保留回退）。

**合并注意**：上游改 `gemini_v1beta_handler.go` 时确认 Registry 接入点与 `inbound_protocol` 写入未丢；native 直连路径必须保留。

### 19. Generic 多 Endpoint + Scheduler 协议桶（MAAS Phase 5）

单账号挂多 endpoint，scheduler 按 endpoint 分桶，`ProtocolBucketEnabled` 回滚开关（env `PROTOCOL_BUCKET_ENABLED`，默认 false 走旧路径）。

**文件**：`ent/schema/endpoint.go`、`repository/endpoint_repository.go`、`service/scheduler_dual_bucket.go`（新增）；`ent/schema/account.go`（1→N Endpoint，迁移 `WHERE platform != 'lingjing'`）、`scheduler_snapshot_service.go`（L650 分支）、`domain/constants.go`（`PlatformGeneric`）、`config.go`；前端 `CreateAccountModal.vue`/`EditAccountModal.vue`（generic 端点表单，详见功能 24）、`types/index.ts`。

**合并注意（高危）**：`scheduler_snapshot_service.go` 高改动，同步后确认 `ProtocolBucketEnabled` 分支与 `scheduler_dual_bucket.go` 引用完整；lingjing 账号不参与 endpoint 迁移（排除条款须生效）；`endpoint.go` 合并后 `go generate ./ent`；`PlatformGeneric` 在 `constants.go`（非 `protocol.go`），两文件语义正交勿合并。

### 20. 操作日志与请求详情新增客户端 IP 和 API 密钥字段

运维错误日志与请求详情展示来源 IP 和 API 密钥名。

**文件**：后端 `ops_repo.go`（`LEFT JOIN api_keys`）、`ops_repo_request_details.go`（`client_ip`/`api_key_name` 列）、`ops_models.go`/`ops_request_details.go`（结构体字段）；前端 `api/admin/ops.ts`、`OpsErrorDetailModal.vue`/`OpsErrorLogTable.vue`/`OpsRequestDetailsModal.vue`、`BaseDialog.vue`（扩宽 full 尺寸）、i18n 4 键。

**合并注意**：上游改这两个 repo 的 SQL 时确认 JOIN 与列追加未丢。

### 21. 版本发布基础设施对接 fork 仓库

检查更新与安装脚本指向 `imleoo/tokenpanel`；修复前端把 `1.x` 误判为自定义构建。

**文件**：`update_service.go`（`githubRepo` 常量）、`VersionBadge.vue`（`isCustomBuild` 改判 `build_type === 'source'`）、`deploy/install.sh`（`GITHUB_REPO` 等）、`Dockerfile.goreleaser`（labels）。

**合并注意**：上游会把 `GITHUB_REPO` 覆盖回上游地址、把 `isCustomBuild` 覆盖回按版本号判断，须还原。

### 22. 文档精简与 Release 安装说明增强

三语 README 删除赞助商/演示/生态内容；`.goreleaser{,.simple}.yaml` 的 `release.footer` 扩展为完整安装/升级指南；文档统一 PostgreSQL 18+ / Redis 8+。

**合并注意**：每次 merge 后需重新清理 README；`release.footer` 段不被覆盖。

### 23. 全量文档同步更新（代码与文档对齐）

一次性文档维护（2026-05-21，v1.1.129）：6 个文档与代码对齐。纯文档改动，无合并风险；将来文档漂移时按「先确认代码，再更新文档」操作。

### 24. 从上游导入模型到定价表（独立工具）+ 通用渠道账号级密钥

「模型定价」页「从上游导入」：填 Base URL + API Key + Provider（+可选鉴权头/方案）请求上游 `/v1/models` 写入 `model_pricings`（兼容 newapi/OpenAI 变体）；generic 账号为**账号级单 API Key（`credentials.api_key`）+ N 个协议 endpoint**。

**关键文件**：后端 `upstream_models.go`（`FetchModelsByConfig`，authHeader 空回退 `Authorization`、自定义 header 用裸 key）、`model_pricing_handler.go`（`SyncFromUpstream`：`SeedIfNotExists` 仅插新不覆盖）、路由 `POST /admin/model-pricings/sync-from-upstream`、`model_routing_service.go`（generic 折入账号级 `model_mapping`）、`generic_gateway.go`（`genericEndpointSupportsModel`、`genericEndpointModelIDs` **唯一口径**——`routableFromAccounts`/`GetAvailableModels` simple 路径/`admin_service.GetGroupModelsListCandidates` 共用）、`gateway_service.go::GetAvailableModels`（标准模式委托 `ModelRoutingService`、simple 模式 raw；含 `SetModelRoutingService` setter）、`admin_service.go::GetGroupModelsListCandidates`（`NewAdminService` 新增 `endpointRepo` 参数）、`gateway_handler.go::Models()`（generic 无可路由模型返回空列表，不回退 claude 默认）；前端 `api/admin/modelPricings.ts`、`ModelPricingsView.vue`（导入弹窗）、`GenericEndpointModelsField.vue`（**fork 独有**共享组件 + `GenericEndpointModelsField.spec.ts`）、`CreateAccountModal.vue`/`EditAccountModal.vue`（账号级 Key + 端点列表 + 别名映射编辑器）、`types/index.ts`（`AccountEndpointInput` 不含 api_key）。

**关键设计**：写入 `is_custom=true` + `SeedIfNotExists`（已设价不覆盖、不被 LiteLLM 同步删）；Provider 必填用渠道 slug；万界方舟所有端点都用 `Authorization: Bearer`（newapi/maas 中转通用做法），端点表单默认值即此。

**合并注意**：`NewModelPricingHandler` 的 `accountTestService` 参数须在 `wire_gen.go` 同步；`upstream_models.go`/`model_pricing_handler.go` 高改动，同步后确认未被覆盖；`sync-from-upstream` 路由须保留。运行时转发见功能 25。

### 25. generic 渠道运行时转发（MAAS Phase 5 收尾，flag 守卫）

generic 账号真正转发：按「入站协议 → 匹配同 `outbound_protocol` 健康 endpoint」直通，账号级 key + 端点鉴权头。flag `Gateway.Scheduling.GenericRuntimeEnabled`（env `GATEWAY_SCHEDULING_GENERIC_RUNTIME_ENABLED`，**默认 true**），仅影响 generic 账号，设 false 即回滚。

**已实现**：openai（chat/completions 原生直通 + responses）+ anthropic（messages 直通）+ gemini（v1beta ForwardNative APIKey 路径）三协议。
**关键文件**：`account.go`（`IsGeneric()`/`GetGenericAPIKey()`）、`generic_gateway.go`（端点选择/协议映射 + `generic_gateway_test.go`）、`openai_gateway_service.go`/`gateway_service.go`/`gemini_messages_compat_service.go`（各自 `endpointRepo` + `SetEndpointRepository` + generic 调度/转发分支）、`openai_gateway_chat_completions_raw.go`（`forwardAsRawChatCompletions`）、`config.go`。UsageLog `endpoint_id` 归因、GET /v1beta/models、端点 `supported_models`（迁移 `144_endpoint_supported_models.sql`）均已落地；在线验证脚本 `script/test_generic_runtime.sh`。

**合并注意**：改动在调度+网关热路径但全部 flag 守卫；同步后确认三个网关服务的 generic 分支未丢。**`wire_gen.go` 的 `SetEndpointRepository(endpointRepository)` 手改 setter 共 4 处**（`gatewayService`/`openAIGatewayService`/`geminiMessagesCompatService`/`accountTestService`），`go generate ./cmd/server` 重生成会**整体丢失**——每次 generate 后必须 `grep -c '\.SetEndpointRepository(' backend/cmd/server/wire_gen.go` 确认为 **4**，否则报 `Endpoint repository is not configured`、generic 转发失效（2026-07-08 曾回归）。**另有 `gatewayService.SetModelRoutingService(modelRoutingService)` 1 处**同样会被 generate 丢失——`grep -c '\.SetModelRoutingService(' backend/cmd/server/wire_gen.go` 应为 **1**，丢失则标准模式 `/v1/models` 退回 raw 口径。

### 26. 模型定价 SSOT 重构（DB 为唯一真源）

计费核心从「64 条硬编码 fallbackPrices + 三套内存 map」迁移到 `model_pricings` DB 表 SSOT，内存层为定时刷新只读 catalog + alias 索引（8 PR，2026-05-28 完成）。计费路径：catalog 直接命中 → aliasIdx 归一化命中 → `matchByModelFamily` 家族匹配 → `pricing_status=unpriced` fail-closed 5xx。

**新增文件**：`migrations/145_model_pricings_ssot_columns.sql`（14 列 + `pricing_status` 回填）、`model_catalog_seed.go`（`BootstrapPricingSeeds()`，现 16 条基础 seed）+ `model_catalog_seed_test.go`、`model_catalog_service.go`（`UpsertModels` 统一写路径）、`billing_service_golden_test.go`（黄金值 1e-12）、`pricing_service_match_test.go`。注：`backend/scripts/verify_pricing_coverage/main.go` 被 .gitignore 忽略、未入库。
**核心修改**：`ent/schema/model_pricing.go`（14 字段）、`pricing_service.go`（`catalog`/`aliasIdx`，删三套 map）、`billing_service.go`（`GetModelPricing` 重写，删 `fallbackPrices`）、`openai_codex_transform.go`（`CodexAliasPairs()`）、`repository/model_pricing_repository.go`（按 `source` 覆盖规则）、`model_pricing_handler.go`/`account_handler.go`/`usage_handler.go`（写路径统一）。灰度开关已终态（`UseCatalogPath=true`、`LegacyFallbackEnabled=false`、`ShadowCompare` 已删）。

**合并注意**：上游不得重新引入 `fallbackPrices`/`pricingData`/`discounts`/`customPrices`；`model_catalog_service.go`/`model_catalog_seed.go` 不能被删；迁移 145 必跑；合并后黄金值测试必须全绿。完整报告：`docs/model-pricing-ssot-refactor-completed.md`。

### 27. Bill-Request-ID 下游对账标识

专用响应头 `Bill-Request-ID`：下游上传（≤64 字符原样回写+落库 `usage_logs.bill_request_id`），未带/超长回退 X-Client-Request-ID（UUID）；与 `X-Request-Id`（被上游覆盖）隔离。

**文件**：`migrations/148_usage_log_bill_request_id.sql`；`ctxkey.go`（`BillRequestID`）、`middleware/client_request_id.go`（两条代码路径均处理 + `client_request_id_test.go`）、`ent/schema/usage_log.go`（字段）、`usage_log.go`/`gateway_service.go`/`openai_gateway_service.go`（写入）、`usage_log_repo.go`（select/insert×4/scan/filters 全链路）、`usage_log_types.go`/`dto/types.go`/`dto/mappers.go`/`usage_handler.go`（过滤查询）。

**合并注意**：`usage_log_repo.go` 🔴 高危——INSERT SQL arg 顺序（`bill_request_id` 在 `video_seconds` 之后、`created_at` 之前）错位即数据错位；`client_request_id.go` 两条路径都要有处理逻辑。上游 `#2007` 实测：`usage_handler.go::List()` 被重构为 `parseUserUsageFilters()` 后，`bill_request_id` 解析需迁移为对 `parsed.Filters.BillRequestID` 赋值，否则冲突后整段易丢。API 说明见 `docs/RECONCILIATION_API_CN.md` 附录。

### 28. 灵境模型目录全量化与官方刊例价落库

灵境官方全部模型（26 canonical + 3 新增 + 6 别名）注册到 `model_pricings`，JoySpace 刊例价换算 USD 写入 `output_cost_per_image`（video=USD/秒、image=USD/张，汇率 7.28）。

**文件**：`migrations/151_seed_lingjing_models.sql`（INSERT，ON CONFLICT DO NOTHING）、`152_update_lingjing_prices.sql`（价格 UPDATE + 新模型 + 别名行）；`lingjing_client.go`（apiId 常量 6→**51** 个）、`lingjing_gateway_service.go`（`lingjingModelCatalog` 27 条 + `lingjingModelAliasToCanonical` 6 条 + `lookupLingjingModel` 三段式解析）、`account_test_service.go`（`testLingjingAccountConnection`，视频模型强制覆盖 prompt 绕开上游排队卡死，超时 8min）。对照总表（唯一真源）：[`claudedocs/lingjing/model_catalog.md`](claudedocs/lingjing/model_catalog.md)。

**合并注意**：新增灵境模型须**同时**改 `lingjingModelCatalog` 和 151 迁移且 model_id 严格一致；6 条别名走 `lingjingModelAliasToCanonical`，不应在 catalog 重复登记；`output_cost_per_image` 对 video 的 per-second 语义是历史约定（`model_catalog_seed.go` 注释），误改 per-task 计费偏差 5-10×；`viduq2-turbo`/`v5.6` 待确认 apiId 后需补入 catalog（现仅有价格行）。

### 29. 手机号注册 + 短信验证码登录（火山引擎）

手机号+验证码注册/登录，火山引擎 SMS 下发，Redis 缓存节流校验，受 `phone_register_enabled` 开关守卫。E.164 归一化（大陆 11 位自动补 `+86`）；验证码 5min TTL + 60s 重发节流 + 路由级限流（fail-close）。

**新增文件**：`pkg/sms/volcengine.go`、`repository/sms_cache.go`、`service/sms_service.go`、`service/auth_phone.go`、`migrations/149_users_add_phone.sql`。
**修改**：`ent/schema/user.go`（phone）、`user_repo.go`（`FindByPhone`）、`auth_handler.go`（三 handler）、`routes/auth.go`（三限流路由）、`setting_handler.go`/`dto/settings.go`/`setting_service.go`/`domain_constants.go`（开关 + Volcengine 5 项凭证）、`auth_service.go`（注入 smsService）、wire 注入链；前端 `LoginView.vue`/`RegisterView.vue`（Tab + 倒计时）、`SettingsView.vue`、`api/auth.ts`、`stores/auth.ts`、`utils/featureFlags.ts`、i18n。方案文档：`claudedocs/手机号注册短信登录方案.md`。

**合并注意**：`user.go` schema 变更后 `go generate ./ent`、迁移 149 必跑；wire 的 `smsCache → smsService → authService` 注入链不能丢；上游改 `setting_handler.go`/`routes/auth.go` 时确认字段/路由未被覆盖。

### 30. 短信服务商抽象（腾讯云 + 阿里云）

功能 29 扩展为多服务商：`smsProvider` 接口 + `loadSmsClient` 按 `sms_provider` 设置（`volcengine`/`tencent`/`aliyun`）动态构造，缺省 volcengine 向后兼容。

**文件**：`pkg/sms/tencent.go`（TC3-HMAC-SHA256）、`pkg/sms/aliyun.go`（RPC POP 签名）、`migrations/150_users_signup_source_add_phone.sql`（CHECK 加 `phone`/`sms`）；`sms_service.go`/`setting_service.go`/`settings_view.go`/`domain_constants.go`/`setting_handler.go`/`dto/settings.go`（三家配置）、`auth_phone.go`（错误分支细化）；前端 `SettingsView.vue`（服务商下拉 + 凭证分组）、`api/admin/settings.ts`、i18n。

**合并注意**：`smsProvider` 接口与三家分支不能退回单一实现；三家 SettingKey 常量须保留；迁移 150 必跑。

### 31. 密码登录开关 + 手机号 + 密码登录方式

管理员设置 `password_login_enabled`（**默认 true**）控制密码登录；「手机号+密码」加入登录方式。

**文件**：后端 `admin/setting_handler.go` + 公开 `setting_handler.go`（暴露字段）、`dto/{settings,types,mappers}.go`、`setting_service.go`（默认 `!= "false"`）、`settings_view.go`、`domain_constants.go`；前端 `LoginView.vue`（切换链接 + phone 字段）、`SettingsView.vue`（开关）、`ProfileInfoCard.vue`/`ProfileIdentityBindingsSection.vue`/`ProfileAccountBindingsCard.vue`/`ProfileView.vue`（手机号绑定展示）、`api/admin/settings.ts`、`types/index.ts`、i18n。

**合并注意**：默认值必须保持 true，否则升级后无人能登录；上游改公开设置端点时确认字段未丢。

### 32. 模型定价表「只显示可见模型」过滤（已被 v6 推翻）

复选框与 `visible_only` query 已删（2026-06-08 v6），后台**默认即广场口径**，可见集计算收敛到 `ModelRoutingService` + `FilterVisibleModels`（详见功能 34）。`ModelPricingListFilter.VisibleOnly` 字段保留但由 handler 固定 true；`ModelPricingService` 包装层与 wire 注入仍在。
**例外（2026-07-08）**：`List` 带显式 `provider` 时跳过广场过滤（解决新 provider 未被路由永不可见、无法补价启用的鸡生蛋问题）。

### 33. 模型定价计费单位 `pricing_unit`（token vs second）

`model_pricings.pricing_unit` enum 区分按 token/按秒计费（迁移 153）；**已被 v6 扩展为四值**（增 `image_generation`/`video_generation`，迁移 154）+ 价格分列（`output_cost_per_image` 按张 / `output_cost_per_image_token` 按 token）+ 视频真 token 计费（迁移 158），详见功能 34。

**文件**：`ent/schema/model_pricing.go`、`migrations/153_model_pricing_pricing_unit.sql`、`model_pricing_handler.go`/两层 `model_pricing_repository.go`/`pricing_service.go`（`ModelInfo.PricingUnit`）；前端 `ModelPricingsView.vue`（计费单位下拉）、`ModelsView.vue`（按单位显示 USD/1K tokens vs USD/秒）、`api/admin/modelPricings.ts`/`api/models.ts`、i18n。

**合并注意**：schema 变更后 `go generate ./ent`、迁移 153 必跑；`ModelInfo.PricingUnit` 须保留；前端单位显示不能退回固定 "USD/1K tokens"。

### 34. 定价通用化（绝对不内置）+ 后台=广场口径 + 视频真 token 计费 + 图片按张表单（v6）

2026-06-08 定价系统重构：**价格完全由运营管理、系统不内置任何厂商价**，后台所见=用户所见。推翻「后台始终看全集」与「内置 doubao/lingjing seed」旧约定。设计文档：`claudedocs/可见模型口径统一方案.md` v6。

- **(A) 通用化删内置价**：`wanjie_sync.go` 通用化为 `ParseMaasFromBytes(source)`；**删除** `doubao_sync.go`/`lingjing_sync.go` 及 embed json；`model_catalog_seed.go` 删灵境/豆包 seed（仅留 16 条基础兜底）；三个平台同步端点合并为 `POST /admin/model-pricings/sync-maas`（`source` 必填）；前端合并为「同步 MaaS 定价」通用弹窗（`syncModelPricingsFromMaas`）。副作用：全新部署上传 JSON 前视频计费 fail-closed。
- **(B) 视频真 token 计费**：`tier_pricing` text 列（迁移 158）；`lingjing_video_pricing.go` 重写为 tier-based（`⌈W/32⌉×⌈H/32⌉×fps×duration` 火山公式）；`billing_service.go::CalculateSeedanceVideoCost` 从 catalog 读 `TierPricing`。
- **(C) 可见口径单一实现**：新增 `model_routing_service.go`（`RoutableModelInfos`/`OperatorRoutableModelInfos`）+ `model_visibility.go`（`FilterVisibleModels` + 版本下限）；`usage_handler.go::ListModels` 与 `model_pricing_handler.go::List` 都走此口径；前端删「只显示可见模型」复选框；wire 注入 `modelRoutingService`（usage + model_pricing 两 handler）。取舍：leoobai 拍板「后台=广场」，未被路由/低版本海外模型改走搜索框或 sync-maas 配价。
- **(D)(E)** 前端「图片（按张）」计价入口写 `output_cost_per_image`；`pricing_unit` 扩四值 + 迁移 154/155（数据清洗）。

**合并注意**：`doubao_sync.go`/`lingjing_sync.go` 及 embed 副本已删，上游引回须再删；`sync-from-doubao/lingjing/wanjie` 路由已合并为 `sync-maas`；`model_routing_service.go`/`model_visibility.go` 纯新增，wire 须注入；迁移 154/155/158 必跑 + `go generate ./ent`；后台 `List` 不要退回「全集 + visible_only 开关」；`CalculateSeedanceVideoCost` 必须走 `tier_pricing`；`backend/data/{doubao,lingjing,wanjie}.json` 保留为运营上传模板（非 embed）。

### 35. 逆向订阅清理（删除 antigravity / OAuth / Codex 整链）

**彻底删除上游的「订阅逆向」能力**（拿他人 Claude Code/Codex/Gemini CLI/Antigravity 订阅 OAuth 换 token、伪装官方客户端转卖额度），只走正规 API 接入（apikey/bedrock/vertex/service_account/generic/lingjing）。已删：antigravity 整平台（共享类型迁到 `pkg/gemini`）、四家 OAuth 逆向链与 token 刷新设施、`AccountTypeOAuth`/`AccountTypeSetupToken` 枚举、`require_oauth_only`+`mcp_xml_inject`（migration 160）、`IsOAuth()` 及全部死分支、逆向路由/前端 OAuth UI、thinking plumbing 等收尾死代码（follow-up 三类已于 2026-06-16 收尾，migration 161 drop `require_privacy_set`）。
**保留（误删即破坏）**：用户登录社交 OAuth（LinuxDo/GitHub/Google/WeChat/DingTalk/OIDC、`pkg/oauth` PKCE、`refresh_token_cache`）、面板自有订阅/配额、正规云接入。

**合并注意（最高优先）**：同步上游会**整套重新引入**逆向代码，合并后必须按 `claudedocs/逆向清理_执行报告_v6.md` 重新删除，用风险表「逆向订阅代码」行 grep 门禁校验；`script/pre_push_check.sh` 检查 5 已自动拦截推送范围新增行的逆向关键词。方案与证据：`claudedocs/逆向代码清理执行方案_v6.md`、`逆向清理_后遗症收尾_follow-up.md`。

### 36. 全功能自包含 E2E 测试套件

自起服务 + admin API 自动 seed 的端到端套件，对真实上游 apikey 验证三平台转发与计费/限额闭环（build tag `e2e`）。

**文件（上游无）**：`script/e2e-test.sh`（runner：起服务→health→加载 gitignored `script/e2e.env`→跑 e2e→拆环境；`E2E_BASE_URL` 可指向已运行实例）、`script/e2e.env.example`、`backend/internal/integration/e2e_full_provision_test.go`（provisioning 助手）、`e2e_full_test.go`（三平台转发 + 计费扣减 + quota/余额/限流拦截 + CRUD）、`e2e_full_protocol_test.go`（Claude 协议深度）、`e2e_full_batchedit_repro_test.go`（已知陷阱#1 repro）。

**合并敏感（上游改契约 e2e 会 loud 失败）**：provisioning 依赖 admin API 请求体形状、网关路由（`/v1/messages{,/count_tokens}`、`/v1/chat/completions`、`/v1beta/models/{m}:generateContent`）、余额预检语义（`api_key_auth.go` `Balance <= 0` → 403）、key status 合法值。历史黑盒套件（`e2e_gateway_test.go`/`e2e_user_flow_test.go`）并存不冲突。

### 37. 账号级协议适配（Bedrock Converse 兼容 + Kiro 归一 + 能力兜底）

账号级 `AccountProtocolAdapter` 机制（与功能 8 同源），两端始终走标准 `/v1/messages` 不新增路由：① **Bedrock Converse 兼容**（`extra.bedrock_compat`）：响应改写为 Converse 形态（非流式 JSON 重映射 + 流式 EventStream 二进制）；② **Kiro 响应归一**（复用 `extra.response_masking`）：补 `stop_reason`、移除 `usage.inference_geo`；③ **Kiro 能力跨组兜底**：image/document → reroute 到 `groups.fallback_group_id_on_invalid_request`（复用既有字段，无新 migration）。设计文档：`claudedocs/账号级协议适配-bedrock与kiro-设计方案.md`。

**文件**：`account.go`（`IsBedrockCompatEnabled()`）、`account_adapter.go`（接口 + `pickAdapter` + 两 adapter，新增）、`pkg/claude/conversecompat/`（`mapping.go`/`eventstream.go`/`stream.go`）、`pkg/claude/kirocompat/`（`normalize.go`）、`gateway_service.go`（gating + `RequestRerouteError` + 流式接管）、`bedrock_stream.go`、`gateway_handler.go`（选号循环捕获 reroute）；前端 `CreateAccountModal.vue`/`EditAccountModal.vue`（开关与遮蔽互斥）；e2e `e2e_full_bedrock_compat_test.go`/`e2e_full_kiro_compat_test.go`/`e2e_full_kiro_reroute_test.go`；`script/capture-kiro-fixtures.sh`。

**合并注意**：`account_adapter.go`/两个 pkg 纯新增；`gateway_service.go` 合并时确认 adapter gating（`response_masking` gate 之后、`anthropic_passthrough` 之前）与流式接管分支未被覆盖；`gateway_handler.go` 的 `RequestRerouteError` 捕获（换 `currentGroupID`，计费仍 `currentAPIKey`）须保留；前端互斥逻辑与 `GroupsView.vue` 兜底组编辑控件须保留。

### 38. API Playground（用户对话/API 试玩页）

用户端可视化对话测试页，用自己的网关 Key 直发网关验证账号/模型（流式/非流式）。纯前端，复用现有网关 API。

**文件**：`PlaygroundView.vue`（+ `PlaygroundView.spec.ts`）、`MessageBubble.vue`（+ `MessageBubble.spec.ts`）、`api/playground.ts`（+ `playground.spec.ts`）、`/playground` 路由、i18n `playground.*`。

**合并注意**：上游改路由守卫或 i18n 时确认路由与键未丢。

### 39. 双桶调度统计看板（Dual-Bucket Stats）

P5 双桶影子比较的观测面：聚合协议桶 vs 单桶调度指标差异，供切单桶决策。

**文件**：`service/dual_bucket_stats.go`、`handler/admin/dual_bucket_stats_handler.go`（依赖 `schedulerSnapshotService`，须 wire 注入并进 `ProvideAdminHandlers`）、路由 `GET /api/v1/admin/scheduler/dual-bucket-stats?platform=&days=`。

**合并注意**：上游改 admin 路由/wire 时确认注入链与路由未丢。

### 40. 上游巨型文件拆分后的 fork 落位（0.1.147 同步产物）

上游 0.1.147 拆分 5 个巨型文件后，两类 fork 代码无对应位置，落到两个新文件：

| 文件 | 说明 |
|-----|------|
| `backend/internal/service/gateway_claude_body.go` | 上游 `gateway_claude_oauth_body.go` **混装**了 OAuth 拟态（逆向）与共享工具。fork 只保留后者：`replaceModelInBody`、`sanitizeSystemText`、`deleteJSONPathBytes`、sticky session 种子（`buildStableSessionSeed` 等）、cache-control 归一/上限/TTL 注入、dateline 归一 |
| `backend/internal/service/gateway_fork_support.go` | `listSchedulableAccountsBase`（功能 25）、`invalidRequestFallbackGroupID`（功能 37）、`calculateVideoCost`（功能 34）、`resolveProviderKey`/`resolveUpstreamModelForCost`（功能 14）、`claudeUsageToTokens` |

**合并注意**：上游同步会重新引入 `gateway_claude_oauth_body.go`（整包含 OAuth 拟态），合并后须：① 删除该文件；② 确认两处符号未与上游重复声明；③ `grep -c "buildOAuthMetadataUserID\|normalizeClaudeOAuthRequestBody" backend/internal/service/` 为 0。另有两处**签名融合**勿被上游整体覆盖：`isOpenAICompatibleAccountEligibleForRequest`（上游加 `platform` 形参，fork 加 generic 早返回；窄化包装 `isOpenAIAccountEligibleForRequest`）；`OpenAIGatewayService.listSchedulableAccounts(ctx, groupID, platform)`（fork 在 `platform == PlatformOpenAI` 时并入 generic）。

### 41. i18n 语言包模块化 + fork 键深合并（0.1.147 同步产物）

上游把单体语言包拆成模块目录后，fork 的 276（zh）/278（en）个自定义键提取到 `locales/{zh,en}/fork.ts`，由 `locales/forkMerge.ts::deepMergeLocale`（递归深合并，数组整体替换）在 `locales/{zh,en}/index.ts` 末尾覆盖上游模块。

**合并注意**：新增 fork 键**直接改 `fork.ts`**，不要写进上游模块文件；上游新增同名键时 fork 键优先（后置覆盖）。

### 42. 管理员部署合规承诺门控（已禁用）

fork 在 `6170de3f7` 永久禁用上游的 admin 首次合规确认门控：`admin_compliance.go::GetAdminComplianceStatus` 硬编码 `Required: false`，中间件恒放行，仅保留历史确认记录展示。

**合并注意**：上游重构该文件时重新核对 `Required` 恒 false；`grep -n "Required.*false" backend/internal/service/admin_compliance.go` 应至少命中一处。

### 43. 渠道级图片输入定价（image_input_price）

渠道模型定价新增图片输入 token 价格列（与 `image_output_price` 对称），未设置回退 `input_price`。

**文件**：`migrations/162_add_channel_image_input_price.sql`（与上游 162 重号，runner 按文件名字典序为 fork 既知常态）、`channel_handler.go`（`ImageInputPrice *float64`）、`channel_repo_pricing.go`（CRUD SQL 全链路携带该列）。

**合并注意**：上游调整 `channel_model_pricing` 表结构或 `channel_repo_pricing.go` SQL 列清单时确认该列未被丢弃。

### 44. 企业组织与额度分配（Team 协作 v2）

用户自助升级企业客户（`enterprise_profiles` 行存在即企业），邀请员工（可指定一级部门/角色/额度模式/初始额度）；员工保留独立账号自管 Key；额度通过**真实余额划转**分配——`allocated` 手动划转 / `shared` 后台自动补给到水位，全部进 `team_fund_transfers` 台账。角色 `owner`/`admin`/`member`（仅 owner 可升降 admin）。计费模型：员工 Key owner 是员工本人，消费扣自己余额，**网关热路径/api_keys/usage_logs 零改动**；回收上限 = min(净投入, 当前余额)。邀请双通道：邮件链接 + 已注册用户站内「我收到的邀请」一键接受（`GET /user/team/invitations/received` 按登录邮箱返回 pending 邀请）。成员使用统计复用功能 3 弹窗，按 `VerifyMemberAccess` 团队粒度授权（防跨企业越权）；`ListMembers` 按角色分视图——owner/admin 全量，普通 member 仅见 owner 行 + 自己一行（防同事额度互见，2026-07-23 收紧）；前端为 member 提供只读「我的企业」卡（切换器标注只读，管理 Tab 不渲染）。v1「共享控制面」（X-Team-Id 上下文切换）未推送即废弃并全部回退。

**文件（上游均无，除标注）**：`ent/schema/{team_member,team_invitation,team_activity_log,enterprise_profile,team_department,team_fund_transfer}.go` + `migrations/175_add_team_collaboration_tables.sql`（6 张表，审计/台账表无 users 外键）；`service/{team_port,team_service,team_fund_service,team_auto_topup_service,enterprise_service,team_department_service}.go`（邀请 token/60s 限流/行锁接受/成员上限 50、双行 FOR UPDATE 固定锁序原子划转、`tryAcquireSingletonLeaderLock` 补给互斥）；`repository/{team_member_repo,team_invitation_repo,team_activity_log_repo,team_fund_repo,enterprise_profile_repo,team_department_repo}.go`；`handler/{team_handler,enterprise_handler}.go`（`resolveTeamOwnerID` 解析 `?owner_user_id=` 支持一人管多企业，仅限这组接口）；`routes/user.go`（`/user/enterprise/*`、`/user/team/*`）；wire 注入；前端 `stores/team.ts`、`api/team.ts`、`api/enterprise.ts`、`EnterpriseUpgradeCard.vue`、`TeamMembersView.vue`（5 Tab，对话框用 `BaseDialog.vue`，布局充满宽度）、`TeamInviteAcceptView.vue`；`LoginView.vue`/`RegisterView.vue`/`EmailVerifyView.vue`（上游已有：邀请 `redirect` 全链透传）；i18n `fork.ts`（`nav.team`、`team.*`、`enterprise.*`）。

**合并注意**：零改动网关热路径（`gateway_service.go`/`openai_gateway_service.go`/`ConcurrencyService`/`DeductBalance`），上游同步这些文件互不冲突。v1 曾把 ~26 处 `subject.UserID` 改为 `GetResourceOwnerID(c)`，**v2 已全部回退**——合并时相关 handler 应与上游一致，发现残留 `GetResourceOwnerID` 调用即为清理遗漏。划转/补给依赖 `users.balance` 行级并发语义，上游改 `DeductBalance` 原子自减需重评并发安全。`UserStatsModal.vue`/`PlatformUsageBreakdown.vue` 为共享组件（见风险表行），被上游重写时对照功能 3 与本功能一并核对。认证页须保留邀请 `redirect` 透传。

### 45. 月度对账（Vendor Report）

按 Eonreach 供应商对账单模板按月对账：用户端「月度对账」页选月查看六种行（期初/Deposit/Withdraw/Credit/按日 Utilisation/期末）并导出 Excel；管理端用户「更多」菜单导出任意用户对账单。设计定稿：`claudedocs/月度对账功能设计方案.md`。

**核心口径**（唯一真源 `statement_port.go` 顶部注释，改动前必读）：余额在 `users.balance` 原地更新、无流水总账，采用「反推法 + 月结快照」双轨（历史月读 `balance_snapshots`，缺失时按当前余额减其后净变动实时反推）；**防双算**：Credit 统计 `redeem_codes` 须 `NOT EXISTS(payment_orders.recharge_code = code)` 排除充值链路兑换码；Deposit=completed 余额订单（按 `completed_at` 归月）+ 企业划入，Withdraw=退款 + 企业划出；恒等式 `closing = opening + deposit − withdraw + credit − utilisation` 单行自校验，gap 非 0 暴露未留痕手工改余额。

**文件（上游均无，除标注）**：`ent/schema/balance_snapshot.go` + `migrations/183_add_balance_snapshots.sql`；`service/statement_port.go`（接口 + 口径注释）、`repository/statement_repo.go`（五表 raw SQL 聚合，零 stub 冲击）、`repository/balance_snapshot_repo.go`（Upsert）、`service/balance_snapshot_service.go`（月结后台任务，1h tick、每月 1 日补算、leader lock、`RecomputeForUserMonth` 幂等）、`pkg/statement/types.go`（共享 DTO）、`service/statement_service.go`（拼装）、`pkg/xlsxreport/vendor_report.go`（excelize 复刻模板，`WriteVendorReportAttachment` 唯一导出口，引入 `github.com/xuri/excelize/v2`）、`handler/statement_handler.go`（用户端 `GET /usage/statement{,/months,/export}`）、`handler/admin/user_statement_handler.go`（管理端导出，依赖 `SetStatementService` setter）；前端 `StatementView.vue`（`/statement` 路由，Simple 模式隐藏）、`api/statement.ts`、`UsersView.vue`（上游已有：菜单项 + 选月弹窗）、`api/admin/users.ts`（`exportUserStatement`）；e2e `e2e_full_statement_test.go`。

**合并注意**：全部为新增文件，仅 `repository/wire.go`、`service/wire.go`、`cmd/server/wire.go`（provideCleanup 形参/stop 条目）、`wire_gen_test.go`（加 nil 参）四处纯追加触碰共享文件。聚合 SQL 依赖 `payment_orders` 状态机、`redeem_codes.recharge_code` 关联、`team_fund_transfers.direction` 枚举、`usage_logs.total_cost/actual_cost` 语义——上游改这些表结构或充值入账链路（`payment_fulfillment.go doBalance`）时须重审 `statement_repo.go` 全部 SQL 与 `statement_repo_integration_test.go`。前端触碰的上游共享文件仅 `UsersView.vue`、`router/index.ts`、`AppSidebar.vue`、`admin/users.ts`、i18n，均为纯追加段落。

---

## 合并后最低验证清单

```bash
# 1. 后端编译和单元测试
cd backend && go build ./cmd/server/ && go test -tags=unit ./internal/service ./internal/repository ./internal/handler/...

# 2. 关键路由是否存在
grep -r "model-discounts\|/models\|provider-pricing" backend/internal/server/routes/

# 3. wire_gen.go 关键注入点
grep -A2 "NewSettingHandler\|NewAdminService\|providerPricingHandler\|endpointRepository" backend/cmd/server/wire_gen.go

# 4. billing_service 折扣调用
grep "applyDiscount" backend/internal/service/billing_service.go

# 5. 前端路由完整性
grep -E "'/models'|model-discounts|provider-pricing" frontend/src/router/index.ts

# 6. i18n 键完整性
grep -E "nav\.models|models\.|providers\." frontend/src/i18n/locales/zh.ts | head -10

# 7. Workflows 触发条件
grep -A3 "^on:" .github/workflows/*.yml

# 8. 前端测试
cd frontend && pnpm exec vitest run src/views/user/__tests__/ModelsView.spec.ts src/i18n/__tests__/usageServiceTierLocales.spec.ts

# 9. fork 功能守护（MAAS P2-6）
grep -q '/lingjing/v1/video' backend/internal/server/routes/gateway.go && echo "OK: lingjing route" || echo "MISSING: lingjing route"

# 10. MAAS Phase 2 协议分流函数
grep -q 'getGroupInboundProtocol' backend/internal/server/routes/gateway.go && echo "OK" || echo "MISSING"

# 11. MAAS Phase 5 回滚开关
grep -q 'ProtocolBucketEnabled' backend/internal/config/config.go && echo "OK" || echo "MISSING"

# 12. MAAS Phase 0 上游成本快照装配点（上游 0.1.147 拆分后在 gateway_usage_billing.go）
grep -q 'ApplyUpstreamCostSnapshot' backend/internal/service/gateway_usage_billing.go && echo "OK" || echo "MISSING"

# 13. Ent schema 是否需要重跑 generate（有 schema 文件变更时执行）
cd backend && go generate ./ent

# 14. 注册接口用户名必填校验
grep 'username.*binding.*required' backend/internal/handler/auth_handler.go && echo "OK" || echo "MISSING"
```

---

## wire_gen.go 关键注入点速查

```go
// 用户统计 Handler（需传入 usageLogRepo）
adminService := service.NewAdminService(..., usageLogRepo, ...)

// Setting Handler（需传入 pricingService、adminService）
settingHandler := admin.NewSettingHandler(..., pricingService, adminService)

// 路由注册
routes.RegisterAdminRoutes(v1, h, adminAuth)

// 灵境接入（lingjing）— openAIGatewayService 创建之后
lingjingClient := service.NewLingjingClient()
lingjingTaskRepository := repository.NewLingjingTaskRepository(client)
lingjingGatewayService := service.NewLingjingGatewayService(lingjingClient, lingjingTaskRepository, accountRepository, schedulerSnapshotService)
openAIGatewayService.SetLingjingService(lingjingGatewayService)

// lingjing Handler（availableChannelHandler 附近）
lingjingHandler := handler.NewLingjingHandler(lingjingGatewayService, billingCacheService, apiKeyService)

// lingjing 后台轮询 Runner（channelMonitorRunner 附近）
lingjingPollRunner := service.ProvideLingjingPollRunner(lingjingTaskRepository, lingjingGatewayService, accountRepository, billingService, billingCacheService)

// provideCleanup 末尾参数
provideCleanup(..., lingjingPollRunner)

// ===== MAAS 重构新增注入点（feature/maas-refactor） =====

// Phase 0：供应商定价（provider_pricing）
providerPricingRepository := repository.NewProviderPricingRepository(client)
upstreamCostResolver := service.NewUpstreamCostResolver(providerPricingRepository)
providerPricingHandler := admin.NewProviderPricingHandler(providerPricingRepository)
// 注：applyUpstreamCostSnapshot() 通过 upstreamCostResolver 在 gateway_service / openai_gateway_service 内部调用

// Phase 3：协议桥接注册表
protocolBridgeRegistry := service.NewProtocolBridgeRegistry()
// 各桥在 registry 初始化时通过 Register() 注入，无需独立 wire 变量

// Phase 5：Endpoint Repository
endpointRepository := repository.NewEndpointRepository(client)
// schedulerSnapshotService 接收 cfg.Gateway.Scheduling（含 ProtocolBucketEnabled）
schedulerSnapshotService := service.NewSchedulerSnapshotService(..., endpointRepository, cfg.Gateway.Scheduling)

// 功能 24/34：模型定价 Handler（6 参，含 accountTestService 用于 sync-from-upstream + modelRoutingService 用于后台=广场口径）
modelRoutingService := service.NewModelRoutingService(accountRepository, endpointRepository, pricingService)
modelPricingHandler := admin.NewModelPricingHandler(modelPricingService, modelPricingRepository, pricingService, settingService, accountTestService, modelRoutingService)
// 功能 34：usage Handler 也注入 modelRoutingService（广场口径）
usageHandler := handler.NewUsageHandler(..., modelRoutingService)
```

---

## 版本历史

> 仅作时间线索引；2026-07-06 起的同步/改动细节以根目录 `CHANGELOG.md` 对应版本段为唯一详录，此表不再复述。

| fork 版本 / 分支 | 上游版本 | 摘要 |
|----------|---------|-----------|
| 1.1.108 | 0.1.108 | 模型广场、用户统计、词云（功能 2/3/4；词云 2026-06-08 已移除） |
| 1.1.121 | 0.1.121 | 模型折扣、人民币定价、UI 主题、语言 Cookie（功能 5/6/7） |
| 1.1.123 | 0.1.123 | 响应遮蔽、Actions 禁用（功能 8/9）；移除 OAuth 前端 UI（功能 11） |
| 1.1.125–126 | 0.1.125–126 | 同步上游；灵境接入（功能 12）；注册必填用户名（功能 13） |
| feature/maas-refactor（已并入） | 0.1.126–129 | MAAS P0-P5（功能 14-19）；发布基础设施/文档（功能 21-23）；模型导入 + generic（功能 24/25）；SSOT 8 PR（功能 26）；Bill-Request-ID（功能 27，migration 148）；灵境目录全量化（功能 28，migration 151/152）；手机号/短信/密码登录（功能 29-31，migration 149/150）；定价 v6（功能 32-34，migration 153-158）；词云移除（migration 159） |
| 1.1.129 | 0.1.129 | 运维日志 IP/密钥字段（功能 20）；首个 GitHub Release |
| 1.1.136 | 0.1.136 | 同步上游；P1/P2 修复加固（依据 `claudedocs/代码修复报告-2026-06-09.md`）；`model_pricing.json` 补 deepseek 冷启动模板；功能 36 扩展协议深度 e2e |
| feature/reverse-cleanup-v6（已并入） | — | 逆向订阅清理（功能 35，migration 160/161）；全功能 E2E 套件（功能 36） |
| feature/bedrock-converse-compat（已并入，2026-06-28） | — | 功能 37 账号级协议适配；本文档移至根目录 |
| 1.1.141 | 0.1.141 | 同步上游 `#2007` 用户用量口径对齐；`usage_handler.go`/`UsageView.vue` 冲突手动解决 |
| 1.1.145–1.1.147 | 0.1.145–147 | 0.1.145 合并回炉清理；0.1.146 同步（apikey 请求头覆写等）；0.1.147 巨型文件拆分三方归并 → 功能 40/41，Grok 保留官方 API/删订阅逆向；详见 `CHANGELOG.md` |
| 1.1.151–1.1.153 | 0.1.151–153 | 装配点路径校准；采纳 Grok 官方 xAI API 全链 + Codex 按次计费（migration 174）；grok OAuth 逆向链继续删除；详见 `CHANGELOG.md` |
| feature/team-collaboration（已并入，2026-07-15） | 1.1.153 | 功能 44 企业组织与额度分配 v2（migration 175）；v1 未推送即废弃回退 |
| 1.1.160 | 0.1.160 | 采纳上游 prompt 审计（`securityaudit`）等；fork 适配 wire/handler 不引 antigravity；详见 `CHANGELOG.md` [1.1.160] |
| 1.1.162 | 0.1.162 | 同步上游（客户端 IP 可配置化/step-up 2FA/ops 入口拒绝/outbox/reasoning policy，migration 183-185）；逆向裁剪与 unused 清扫；恢复 wire setter 5 处与 grok 测试分流两处存量断线；详见 `CHANGELOG.md` [1.1.162] |
| zhiguofan（2026-07-21） | — | 逆向订阅残留清理三轮（i18n 死文案 / OAuth 孤儿函数簇 / 依赖裁剪）+ 测试覆盖补齐 + 门禁检查 5；详见 `CHANGELOG.md` 2026-07-21 段 |

---

*最后更新：2026-07-22（1.1.162 同步上游后更新版本历史；文档精简：45 个功能节统一为「功能一句话 + 关键文件 + 合并注意」紧凑三段式，版本历史压缩为索引、尾部历史更新段移除；所有合并注意要点与风险表条目完整保留，改动详录职责移交 `CHANGELOG.md` 与 git 历史）*
