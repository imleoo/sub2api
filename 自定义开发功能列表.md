# zhiguofan 分支自定义功能列表

本文档记录 zhiguofan 分支相对于上游 `Wei-Shaw/sub2api` 的全部差异，用于同步上游代码时快速评估合并风险。

> **权威性说明**：本文档与 `CLAUDE.md` 保持一致，两者有冲突时以 **当前代码** 为准。

---

## 快速风险评估表

同步上游后，按此表逐项 grep/检查，若文件被还原则需手动恢复。

> 同一文件只列一行；多个功能涉及同一文件时检查要点合并标注功能号。

| 风险等级 | 文件 / 位置 | 检查要点 |
|---------|-----------|---------|
| 🔴 高 | `backend/cmd/server/wire_gen.go` | adminUserHandler、NewSettingHandler 参数数量；lingjingGatewayService/lingjingPollRunner/lingjingHandler 注入；providerPricingHandler、endpointRepository、schedulerSnapshotService（含 dual-bucket cfg）注入；功能 24/34：`NewModelPricingHandler`（6 参，含 `accountTestService`+`modelRoutingService`）、`NewUsageHandler`（8 参，含 `modelRoutingService`） |
| 🔴 高 | `backend/internal/server/routes/admin.go` | `provider-pricings` 路由组（**复数**，backend admin-API-only，无前端页面）；`model-pricings/sync-from-upstream`（功能 24）+ `sync-maas`（功能 34）；注：旧 `settings/model-discounts` 路由已随功能 24/34 移除（折扣并入 `model_pricings.discount_rate`） |
| 🔴 高 | `backend/internal/server/routes/gateway.go` | masking 逻辑；`getGroupInboundProtocol` 函数及协议分流 switch；`/v1/images/generations` lingjing 平台判断；`/lingjing/v1/video/*` 路由组 |
| 🔴 高 | `backend/internal/domain/protocol.go` | 上游无此文件，确认未被删除/被 constants.go 覆盖；`InboundProtocol`/`OutboundProtocol` 常量集中、`ResolveInboundProtocol()` |
| 🔴 高 | `backend/ent/schema/usage_log.go` | P0-2 新增 9 列：`upstream_unit_price_*`、`upstream_total_cost`、`provider`、`pricing_source`、`async_task_id`、`cost_finalized_at`；合并后须重跑 `go generate ./ent` |
| 🔴 高 | `backend/internal/config/config.go` | `ProtocolBucketEnabled`（P5-6 回滚开关）、`Gateway.Scheduling.GenericRuntimeEnabled`（功能 25，env `GATEWAY_SCHEDULING_GENERIC_RUNTIME_ENABLED`）；上游改 config 结构时确认两字段未丢失。注：`USAGE_UPSTREAM_COST_ENABLED` **未实现为 config 字段/env**（仅注释提及），上游成本快照由 `ApplyUpstreamCostSnapshot` 命中 provider_pricing 与否驱动（功能 14） |
| 🔴 高 | `frontend/src/router/index.ts` | `/models`（`SupportedModels`）；`/admin/model-discounts` 现为**重定向**到 `/admin/model-pricings`（独立折扣页已删）。注：provider-pricing 无前端路由/页面（仅后端 admin API） |
| 🔴 高 | `backend/internal/service/billing_service.go` | 折扣以 inline `discountRate` 应用（读 `dbEntry.DiscountRate`，回退 `pricingService.GetDiscount(model)`；无独立 `applyDiscount()` 方法）；`GetModelPricing` 必须走 catalog 路径（不得重新引入 `fallbackPrices`）；功能 34：`CalculateSeedanceVideoCost` 必须走 `tier_pricing`（勿退回写死 ¥/秒） |
| 🔴 高 | `backend/internal/service/pricing_service.go` | `catalog`/`aliasIdx` 字段必须保留；不得重新引入 `pricingData`/`discounts`/`customPrices`；`GetCNYRate`、`ListAllModels`；功能 33/34：`ModelInfo.PricingUnit`/`OutputCostPerImage[Token]` 暴露，`ListEnabledCatalogModels` 按 mode 透传 image/video unit，灵境 seed/`matchLingjingModel` 等已删 |
| 🔴 高 | `backend/internal/service/model_catalog_service.go` | 上游无此文件（功能 26）；`UpsertModels` 是所有写路径的统一入口 |
| 🔴 高 | `backend/internal/service/model_catalog_seed.go` | 上游无此文件（功能 26）；`BootstrapPricingSeeds()` 16 条基础 seed 是计费安全的硬前置（灵境/豆包 seed 已被功能 34 删除） |
| 🔴 高 | `.github/workflows/*.yml` | 所有文件的 `on:` 必须为 `workflow_dispatch:` |
| 🔴 高 | 逆向订阅代码（antigravity / OAuth / Codex 整链） | 功能 35：本分支**已删除**整套订阅逆向；同步上游会**重新引入**，合并后须按 `逆向清理_执行报告_v6.md` + `逆向清理_后遗症收尾_follow-up.md` 重新删除。门禁：`rg -n "PlatformAntigravity\|AccountTypeOAuth\|AccountTypeSetupToken\|IsOAuth\|require_oauth_only\|mcp_xml_inject\|antigravity" backend/internal frontend/src` 仅剩用户登录 OAuth 白名单 |
| 🔴 高 | `backend/internal/service/lingjing_gateway_service.go` | 功能 28：`lingjingModelCatalog` + `lingjingModelAliasToCanonical` + `lookupLingjingModel`；27 条模型路由是计费 + 网关分流唯一真源，与 migration 151/152 严格 1:1 |
| 🔴 高 | `backend/migrations/151_seed_lingjing_models.sql` / `152_update_lingjing_prices.sql` | 功能 28：模型 INSERT + 价格 UPDATE + 别名；model_id 写错计费 miss；价格单位错（per-image vs per-second）偏差 5-10× |
| 🔴 高 | `backend/ent/schema/user.go` | 功能 29：`phone` 字段；`go generate ./ent`，迁移 149 必跑 |
| 🔴 高 | `backend/internal/service/auth_phone.go` | 功能 29：上游无此文件；`NormalizePhone` E.164、`RegisterWithPhone`/`LoginWithPhone` |
| 🔴 高 | `backend/internal/service/sms_service.go` | 功能 29/30：`smsProvider` 接口 + `loadSmsClient` 三家分支（火山/腾讯/阿里） |
| 🔴 高 | `backend/internal/service/model_routing_service.go` `model_visibility.go` | 功能 34：上游无此两文件；可见模型口径唯一来源（`RoutableModelInfos`/`OperatorRoutableModelInfos`/`FilterVisibleModels`） |
| 🔴 高 | `backend/ent/schema/model_pricing.go` | 功能 33/34：`pricing_unit` enum 四值 + `tier_pricing` text 列；`go generate ./ent`，迁移 153/154/158 必跑 |
| 🟡 中 | `backend/internal/service/scheduler_snapshot_service.go` | L484/L786 平台列表含 `PlatformLingjing`；L650 `ProtocolBucketEnabled` 分支；`scheduler_dual_bucket.go` 双桶影子比较 |
| 🟡 中 | `backend/internal/service/gateway_service.go` | masking 调用分支；上游成本快照 `ApplyUpstreamCostSnapshot()` 调用**已移至 `gateway_usage_billing.go`**（上游 0.1.147 拆分，见功能 14）；功能 25：`listSchedulableAccounts` 包一层并入 generic（anthropic+gemini）、Forward generic→anthropic 直通、`buildUpstreamRequestAnthropicAPIKeyPassthrough` base_url 覆盖、`SetEndpointRepository`（flag 守卫）；功能 37：Forward 的 adapter gating（在 `response_masking` gate 之后、`anthropic_passthrough` 分支之前的 `InspectRequest` switch）+ `RequestRerouteError`/`invalidRequestFallbackGroupID` + 通用/直通两条流式路径的 `streamAdapter` 接管分支 |
| 🟡 中 | `backend/internal/service/openai_gateway_service.go` | `applyUpstreamCostSnapshot()` 调用；`SetLingjingService()`；功能 25：`listSchedulableAccounts` 并入 generic、eligibility generic 分支、token/base_url 覆盖、`SetEndpointRepository`（flag 守卫） |
| 🟡 中 | `backend/internal/handler/admin/provider_pricing_handler.go` | 上游无此文件；`NewProviderPricingHandler` 须在 wire_gen 注入 |
| 🟡 中 | `backend/internal/service/upstream_models.go` | 功能 24：`FetchModelsByConfig`（无账号按 base_url+key 拉取）；`upstreamModelEntry.owned_by`（newapi 兼容） |
| 🟡 中 | `backend/internal/handler/admin/model_pricing_handler.go` | 功能 24：`SyncFromUpstream` + `accountTestService`；功能 34：`List` 默认广场口径（`VisibleOnly=true`+`FilterVisibleModels`，不解析 `visible_only`）——**例外：`provider` 显式筛选时跳过广场过滤，展示该来源全部同步模型（含 unpriced），供运营给新同步 provider 补价+启用**；`SyncMaas`（替换三旧端点）、Create/Update 收 `pricing_unit` 四值 + `output_cost_per_image` |
| 🟡 中 | `backend/internal/service/openai_gateway_chat_completions_raw.go` | 功能 25：`forwardAsRawChatCompletions` 对 generic 用账号 key + 端点 base_url |
| 🟡 中 | `backend/internal/service/gemini_messages_compat_service.go` | 功能 25：`endpointRepo` + `SetEndpointRepository`；`ForwardNative` APIKey 路径对 generic 用端点 base_url |
| 🟡 中 | `frontend/src/views/user/ModelsView.vue` | 白名单过滤、图片模型、复制按钮 |
| 🟡 中 | `backend/internal/handler/usage_handler.go` | `ListModels` 完整性（功能 34 改走 `RoutableModelInfos`+`FilterVisibleModels`） |
| 🟡 中 | `backend/internal/handler/admin/user_handler.go` | `GetUserUsage` 调用真实 usageLogRepo；`CreateUserRequest.Username` 必填 |
| 🟡 中 | `backend/internal/handler/admin/setting_handler.go` | `GetModelDiscounts`/`UpdateModelDiscounts`、`CurrencyMode`/`CNYRate`/`UITheme`；功能 29/30/31：`PhoneRegisterEnabled`+`SmsProvider`+三家凭证+`PasswordLoginEnabled` |
| 🟡 中 | `frontend/src/i18n/locales/zh.ts` & `en.ts` | `models.*`、`nav.models`、`admin.accounts.providers.*`、`admin.dashboard.providers.*`、`auth.usernameLabel/usernameRequired` |
| 🟡 中 | `frontend/src/utils/platformColors.ts` | `lingjing` 颜色条目及 `isPlatform()` |
| 🟡 中 | `frontend/src/types/index.ts` | `GroupPlatform`/`AccountPlatform` 含 `'lingjing'`/`'generic'`（功能 35 已删 `'antigravity'` 及 `AccountType` 的 `oauth`/`setup-token`）；`RegisterRequest.username` 必填 |
| 🟡 中 | `backend/migrations/145_model_pricings_ssot_columns.sql` | 功能 26：14 列 + `pricing_status` 回填；合并后须执行 |
| 🟡 中 | `backend/internal/service/account_test_service.go` | 功能 28：`testLingjingAccountConnection`（SSE 流），视频模型强制 prompt + 8min 超时 |
| 🟡 中 | `backend/internal/service/lingjing_client.go` | 功能 28：apiId 常量 51 个，命名 `LingjingAPIID<产品><版本><任务类型>` |
| 🟡 中 | `backend/internal/pkg/sms/{volcengine,tencent,aliyun}.go` | 功能 29/30：上游无此目录；三家短信签名/请求实现 |
| 🟡 中 | `backend/internal/repository/sms_cache.go` | 功能 29：上游无此文件；验证码 Redis 缓存（节流+校验） |
| 🟡 中 | `backend/internal/server/routes/auth.go` | 功能 29：`/auth/{send-sms-code,phone-register,phone-login}` 三个限流路由 |
| 🟡 中 | `backend/internal/service/setting_service.go` & `settings_view.go` | 功能 29/30/31：`IsPhoneRegisterEnabled`、`SmsProvider`、`PasswordLoginEnabled`（默认 true） |
| 🟡 中 | `backend/internal/service/domain_constants.go` | 功能 29/30/31：`SettingKeyPhoneRegisterEnabled`、`SettingKeySmsFrontend`、`SettingKey{Volcengine,Tencent,Aliyun}*`、`SettingKeyPasswordLoginEnabled` |
| 🟡 中 | `frontend/src/views/auth/{LoginView,RegisterView}.vue` | 功能 29/31：手机号模式 + 验证码倒计时 + 密码/手机号切换链接 |
| 🟡 中 | `frontend/src/views/admin/SettingsView.vue` | 功能 29/30/31：手机号注册 + 短信服务商 + 密码登录开关配置区 |
| 🟡 中 | `backend/internal/service/model_pricing_repository.go` | 功能 32/34：`ModelPricingListFilter.VisibleOnly` + `ModelPricingService`；`DBModelPricing.TierPricing` + `VideoPriceTier` + image/video unit 常量 |
| 🟡 中 | `backend/internal/repository/model_pricing_repository.go` | 功能 34：`List` visible_only 改用 `RoutableModelIDs`；`BulkUpsertMaas` + `tier_pricing` 读写；旧 CTE JOIN 已移除 |
| 🟡 中 | `frontend/src/views/admin/ModelPricingsView.vue` | 功能 34：已删「只显示可见模型」复选框；「图片（按张）」计价 + `output_cost_per_image` 表单；「同步 MaaS 定价」通用弹窗 |
| 🟡 中 | `backend/internal/service/{wanjie_sync,lingjing_video_pricing}.go` | 功能 34：通用 `ParseMaasFromBytes(source)`；视频真 token 计费（`tier_pricing`+火山公式）；`doubao_sync.go`/`lingjing_sync.go` 及 embed json 已删，勿引回 |
| 🟡 中 | `backend/internal/service/account_adapter.go`、`backend/internal/pkg/claude/{conversecompat,kirocompat}/` | 功能 37：账号级协议适配（Bedrock Converse / Kiro 归一 / 能力兜底）；上游无此三处，纯新增 |
| 🟡 中 | `backend/internal/handler/gateway_handler.go` | 功能 37：选号循环捕获 `RequestRerouteError` → `currentGroupID` 换组（计费仍按原 `currentAPIKey`）；上游改循环时确认未覆盖 |
| 🟢 低 | `backend/internal/service/bedrock_stream.go` | 功能 37：AWS Bedrock 流式路径 adapter 接管（Content-Type/EmitStreamEvent/FinishStream） |
| 🟢 低 | `backend/internal/service/generic_gateway.go` | 功能 25：上游无此文件；generic 端点选择/解析 |
| 🟢 低 | `backend/data/{model_discounts,model_pricing}.json` | 上游无此文件，不被覆盖；`{doubao,lingjing,wanjie}.json` 为运营上传模板（非 embed） |
| 🟢 低 | `backend/ent/schema/{lingjing_task,provider_pricing,endpoint}.go` | 上游无，通常不冲突；合并后须重跑 `go generate ./ent` |
| 🟢 低 | `backend/internal/service/lingjing_*.go` | 5 个新增文件，上游无，通常不冲突 |
| 🟢 低 | `backend/internal/service/{upstream_cost,provider_normalize,protocol_bridge_registry,scheduler_dual_bucket}.go` | 上游无；分别为成本快照、provider 归一化、协议桥注册表、双桶影子比较 |
| 🟢 低 | `backend/internal/repository/{lingjing_task_repo,provider_pricing_repository,endpoint_repository}.go`、`backend/internal/handler/lingjing_handler.go` | 上游无此文件 |
| 🟢 低 | `backend/internal/pkg/apicompat/*.go` | P3/P4 新增：协议桥 + SSE replay 框架；上游无 |
| 🟢 低 | `backend/cmd/migrate_provider/` | 上游无此目录；P1-1 历史回填 job |
| 🟢 低 | `statics/`、`script/`、`tools/`、`test/` | 上游无这些目录，合并通常不冲突 |
| 🟢 低 | `README.md` / `README_CN.md` / `README_JA.md` | 上游含大量赞助商/演示内容，合并后需重新精简（功能 22） |
| 🟢 低 | `.goreleaser.yaml` / `.goreleaser.simple.yaml` | `release.footer` 段含安装文档，上游修改时确认未被覆盖 |
| 🟢 低 | `backend/internal/repository/{ops_repo,ops_repo_request_details}.go` | 功能 20：`LEFT JOIN api_keys`、`client_ip`+`api_key_name` 列追加 |
| 🟢 低 | `backend/migrations/{149,150}_*.sql` | 功能 29/30：`phone` 列 + `signup_source` CHECK；合并后须执行 |
| 🟢 低 | `backend/migrations/{153,154,155,158}_*.sql` | 功能 33/34：`pricing_unit` 四值 CHECK + 数据清洗 + `tier_pricing` 列；合并后须执行 |
| 🟢 低 | `frontend/src/views/user/PlaygroundView.vue`、`frontend/src/components/playground/MessageBubble.vue`、`frontend/src/api/playground.ts` | 功能 38：API Playground 试玩页 + `/playground` 路由；上游无，通常不冲突 |
| 🟢 低 | `backend/internal/service/dual_bucket_stats.go`、`backend/internal/handler/admin/dual_bucket_stats_handler.go` | 功能 39：双桶统计看板；路由 `GET /admin/scheduler/dual-bucket-stats`，须在 `wire_gen.go` 注入 `dualBucketStatsHandler` 并进 `ProvideAdminHandlers` |
| 🔴 高 | `backend/internal/service/gateway_claude_body.go` | 功能 40：上游 `gateway_claude_oauth_body.go` 剥离 OAuth 拟态后保留的共享工具（`replaceModelInBody`、cache-control 归一/上限、sticky session 种子、dateline 归一）。上游同步会重新引入原混装文件，须再次按符号拆分 |
| 🔴 高 | `backend/internal/service/gateway_fork_support.go` | 功能 40：上游拆分 `gateway_service.go` 后无对应位置的 fork 函数（`listSchedulableAccountsBase`/`invalidRequestFallbackGroupID`/`calculateVideoCost`/`resolveProviderKey`/`resolveUpstreamModelForCost`/`claudeUsageToTokens`） |
| 🟡 中 | `frontend/src/i18n/locales/{zh,en}/fork.ts`、`frontend/src/i18n/locales/forkMerge.ts` | 功能 41：fork 自定义 i18n 键（276/278 条）+ 深合并工具；上游把语言包拆成模块目录后，fork 键集中于此，不再与上游模块冲突 |
| 🟢 低 | `backend/internal/service/{endpoint_selector,endpoint_types,bridge_capabilities,gemini_compat_helpers,gemini_tier,protocol_consistency_check,account_schedulable,gateway_temp_unschedule,credentials_merge}.go` | 功能 25/37 拆分 helper（endpoint 选择/类型、协议桥能力、gemini 兼容/分级、协议一致性校验、可调度性、临时不可调度、凭据合并）；上游无，通常不冲突 |

---

## 功能详细说明

### 1. 自定义版本号

**版本策略**：上游版本主号 +1（上游 `0.1.x` → fork `1.1.x`）

| 文件 | 说明 |
|-----|------|
| `backend/cmd/server/VERSION` | 主号固定 `1`，次/修订号跟随上游（实际值以文件为准，勿在文档硬编码） |

**合并注意**：同步上游后 `./script/sync_upstream_to_zhiguofan.sh` 会自动修改版本主号；手动 merge 需检查此文件。

---

### 2. 用户端模型广场 (Models)

用户可查看账号白名单内的可用模型、定价、折扣、图片模型等信息。

**前端**

| 文件 | 说明 |
|-----|------|
| `frontend/src/views/user/ModelsView.vue` | 模型广场页面（白名单过滤、图片模型、版本过滤、复制模型名） |
| `frontend/src/views/user/__tests__/ModelsView.spec.ts` | 对应测试 |
| `frontend/src/api/models.ts` | API 客户端，含 `ModelInfo.discount_rate`、`ModelsResponse.cny_rate` |
| `frontend/src/router/index.ts` | `/models` 路由（`SupportedModels`） |
| `frontend/src/i18n/locales/zh.ts` & `en.ts` | `models.*`、`nav.models` 键值 |

**后端**

| 文件 | 说明 |
|-----|------|
| `backend/internal/server/routes/user.go` | `GET /api/v1/models` |
| `backend/internal/handler/usage_handler.go` | `ListModels` 方法，白名单过滤 + 图片模型归一化 |

**关键功能特性**
- 账号白名单过滤：只显示 `credentials.model_mapping` 配置的模型，不展示全量定价表
- 图片模型：`gpt-image-*`、`dall-e*`、`imagen-*`、`gemini-*-image` 等须在白名单内可见
- 复制模型名：带剪贴板 fallback 和短暂"已复制"状态
- 价格显示：折后价 + 折扣标签 + 人民币价格（由后端 `cny_rate` 驱动）

**合并注意**：路由 `/models`、`ModelsView.vue`、`ListModels`、i18n 键必须保留；不要退回"展示全量定价表"行为。

---

### 3. 管理员用户使用统计

管理员可查看单个用户的详细使用数据（汇总、每日历史、模型分布、端点分布）。

**前端**

| 文件 | 说明 |
|-----|------|
| `frontend/src/views/admin/UsersView.vue` | 用户列表，含"查看统计"入口 |
| `frontend/src/components/admin/user/UserStatsModal.vue` | 统计弹窗 |
| `frontend/src/api/admin/users.ts` | `GET /api/v1/admin/users/:id/usage` |

**后端**

| 文件 | 说明 |
|-----|------|
| `backend/internal/handler/admin/user_handler.go` | `GetUserUsage`，依赖真实 usageLogRepo（非 mock） |
| `backend/internal/service/admin_service.go` | `GetUserUsageStats` 签名变更为 `(startTime, endTime time.Time)`，调用 `usageLogRepo` |
| `backend/internal/repository/usage_log_repo.go` | 新增 `GetUserUsageStats`，复用 `getEntityUsageStats` 私有方法 |

**API**：`GET /api/v1/admin/users/:id/usage`，返回 `summary`、`history`、`models`、`endpoints`。

**合并注意**：`AdminService.GetUserUsageStats` 签名已变更，`wire_gen.go` 中 `NewAdminService` 参数需包含 `usageLogRepo`；不能退回返回 mock 数据的旧实现。

---

### 4. 词云分析 (Prompt Analytics)（已移除）

> **已于 2026-06-08 整体移除**：promptanalytics 插件、`keyword_stat` schema、`/admin/prompt-analytics` 路由与前端页面、`admin.promptAnalytics.*` i18n 键全部删除；网关中间件、wire 注入、`registerPromptAnalyticsRoutes`、`GET /api/v1/admin/prompt-analytics/top-keywords` 接口均已去除。历史迁移 `082_create_keyword_stats.sql` 保留不动，新增迁移 `159_drop_keyword_stats.sql` 执行 `DROP TABLE IF EXISTS keyword_stats` 清理数据表。本功能号予以保留作占位，不再复用。

---

### 5. 模型折扣与人民币定价

管理员可对模型设置折扣率，计费时自动应用；用户端同步展示折后价和人民币换算价。

> **⚠️ 已被功能 24/34 取代（独立折扣页/路由已删）**：折扣不再有独立管理页与 `settings/model-discounts` 路由，已统一为 `model_pricings.discount_rate`，在「模型定价」页（功能 24/34）管理。下表中标注为 ~~删除线~~ 或「已移除」的项为历史记录，保留以便理解演进；计费侧 `pricingService.GetDiscount()` + DB `discount_rate` 仍是有效兜底路径。

**配置 / 数据**

| 文件 | 说明 |
|-----|------|
| `backend/data/model_discounts.json` | 折扣配置模板（47B 占位，DB `discount_rate` 接管后基本停用） |
| `backend/data/model_pricing.json` | 冷启动兜底定价模板（SHA256 校验）；含参考定价（如 commit eb6cf4ae 的 deepseek），属**运营模板/冷启动兜底，非 seed 内置**（不违背功能 34「seed 绝对不内置」） |

**后端**

| 文件 | 说明 |
|-----|------|
| `backend/internal/config/config.go` | `PricingConfig.CNYRate`（默认 7）、`PricingConfig.DiscountFile` |
| `backend/internal/service/pricing_service.go` | `catalog`/`aliasIdx` 内存索引（SSOT 重构后）、`SaveDiscounts`、`GetDiscount`、`GetCNYRate`、`GetCurrencyMode`、`ListAllModels`、`ModelInfo` 结构体 |
| `backend/internal/service/billing_service.go` | 折扣以 inline `discountRate` 应用（读 `dbEntry.DiscountRate`，回退 `pricingService.GetDiscount(model)`；**无独立 `applyDiscount()` 方法**）；`GetModelPricing` 走 catalog → aliasIdx → matchByModelFamily（SSOT 重构后，fallbackPrices 已删除） |
| `backend/internal/service/model_catalog_service.go` | `UpsertModels` 统一写路径（SSOT 重构功能 26 新增） |
| `backend/internal/service/model_catalog_seed.go` | `BootstrapPricingSeeds()` 16 条启动 seed（SSOT 重构功能 26 新增；灵境/豆包 seed 已被功能 34 删除） |
| `backend/internal/service/domain_constants.go` | `SettingKeyCNYRate`、`SettingKeyCurrencyMode` 常量 |
| `backend/internal/handler/admin/setting_handler.go` | ~~`GetModelDiscounts`/`UpdateModelDiscounts` API~~（已移除）；`SettingHandler` 新增 `pricingService`、`adminService` 字段（仍在，供货币/主题等用） |
| `backend/internal/handler/dto/settings.go` | `CurrencyMode`、`CNYRate` 字段 |
| `backend/internal/service/setting_service.go` | `CurrencyMode`、`CNYRate` 字段持久化 |
| `backend/internal/service/settings_view.go` | Settings 视图新增货币字段 |
| `backend/internal/server/routes/admin.go` | ~~`GET/PUT /api/v1/admin/settings/model-discounts`~~（路由已移除，折扣并入 `model_pricings`） |

**前端**

| 文件 | 说明 |
|-----|------|
| ~~`frontend/src/views/admin/ModelDiscountsView.vue`~~ | **已删除**（折扣并入 `ModelPricingsView.vue`） |
| `frontend/src/router/index.ts` | `/admin/model-discounts` 现**重定向**到 `/admin/model-pricings`（非独立页面） |
| ~~`frontend/src/api/admin/settings.ts` 折扣 CRUD~~ | 折扣改走 `api/admin/modelPricings.ts`（`discount_rate`） |
| `frontend/src/api/models.ts` | `ModelInfo.discount_rate`、`ModelsResponse.cny_rate` |
| `frontend/src/views/user/ModelsView.vue` | 价格列显示折后价、折扣标签、人民币价格 |
| `frontend/src/views/user/UsageView.vue` | 用量页费用展示（总费用卡片、明细表 tooltip、CSV 导出）均走 `formatUSD()`/`currencyLabel()`；2026-07 upstream `#2007 feature/user-usage-admin-parity` 曾整体重写此文件（费用展示改为委托给 `UsageStatsCards`/`UsageTable`/`*DistributionChart` 等共享组件），合并冲突集中在 CSV 导出的表头与数值格式化两处 |
| `frontend/src/components/admin/usage/{UsageStatsCards,UsageTable}.vue`、`frontend/src/components/charts/{GroupDistributionChart,ModelDistributionChart,UserBreakdownSubTable}.vue` | 费用展示共享组件，`formatCost`/`formatUSD` 承载人民币换算；上游新增的 `showAccountCost`/`strikeStandardCost` 条件渲染与折扣的 `formatUSD` 需同时保留，不可二选一整体覆盖 |
| `frontend/src/stores/app.ts` | `currencyMode`、`cnyRate` 状态 |
| `frontend/src/components/admin/CurrencySetupModal.vue` | 首次货币设置向导弹窗（由 `AppLayout.vue` 触发，引导管理员设置货币模式/汇率；上游无此文件） |

**合并注意**：折扣以 inline `discountRate`（`dbEntry.DiscountRate` / `pricingService.GetDiscount()`）应用，不存在 `applyDiscount()` 方法；上游不得重新引入 `fallbackPrices`——功能 26 SSOT 重构已删除此字段，计费走 catalog 路径；`NewSettingHandler` 签名已新增两个参数，`wire_gen.go` 必须同步更新；`cny_rate` 响应字段不能丢失；`model_catalog_service.go` 和 `model_catalog_seed.go` 为纯新增文件，不能丢失。独立折扣页/路由已被功能 24/34 取代（`model_pricings.discount_rate`），合并上游若引回 `settings/model-discounts` 端点与 `ModelDiscountsView.vue` 需再删。`UsageView.vue` 及其费用展示共享组件（`UsageStatsCards`/`UsageTable`/`*DistributionChart`/`UserBreakdownSubTable`）合并冲突时，务必保留 `formatUSD`/`formatCost` 的人民币换算逻辑，禁止用 `git checkout --ours/--theirs` 整文件二选一。

---

### 6. UI 主题扩展

新增 `violet`、`orange` 两套主题，挂载在 `<html>` class 上。

**文件**

| 文件 | 说明 |
|-----|------|
| `frontend/src/main.ts` | `applyUITheme()` 函数，启动时读取 appStore.uiTheme 应用 |
| `frontend/src/stores/app.ts` | `uiTheme` ref，从 `config.ui_theme` 初始化 |
| `frontend/src/style.css` | `.theme-violet`、`.theme-orange` CSS 变量 |
| `backend/internal/handler/admin/setting_handler.go` | `UITheme` 字段（`teal`/`violet`/`orange` 枚举验证） |
| `backend/internal/service/settings_view.go` & `setting_service.go` | `UITheme` 字段持久化 |

**合并注意**：上游无此字段，合并 `setting_handler.go` 时注意 `UITheme` 字段不被丢弃。

---

### 7. 语言切换（Cookie 持久化）

用户切换语言后通过 Cookie 持久化偏好，刷新页面保持选择。

**文件**

| 文件 | 说明 |
|-----|------|
| `frontend/src/main.ts` | bootstrap 阶段读取 Cookie 语言偏好并应用 |
| `frontend/src/i18n/locales/zh.ts` & `en.ts` | 新增所有 fork 功能的翻译键 |

**合并注意**：i18n 文件差异较大，合并时须保留所有 fork 新增键（`models.*`、`nav.models` 等）。

---

### 8. 网关响应遮蔽 (Response Masking)

账号级功能：当用户询问"你是谁/什么模型"时，网关拦截并返回固定身份回答，不透传给上游。

**文件**

| 文件 | 说明 |
|-----|------|
| `backend/internal/service/gateway_response_masking.go` | 新增文件：身份问题正则、模型名映射、SSE/非流式假响应生成 |
| `backend/internal/service/gateway_service.go` | 调用 `IsResponseMaskingEnabled()` + `isIdentityQuestion()` 分支 |
| `backend/internal/service/account.go` | `IsResponseMaskingEnabled()` 方法 |

**合并注意**：`gateway_response_masking.go` 是纯新增文件，通常不与上游冲突；但 `gateway_service.go` 合并时须确认 masking 调用分支未被覆盖。

---

### 9. GitHub Actions 全部禁用自动触发

**原因**：避免 GitHub Actions 额度被耗尽。

| 文件 | 原触发条件 | 当前 |
|-----|-----------|------|
| `.github/workflows/backend-ci.yml` | push / pull_request | `workflow_dispatch` |
| `.github/workflows/security-scan.yml` | push / pull_request / 每周一 | `workflow_dispatch` |
| `.github/workflows/docker-push.yml` | push zhiguofan 分支 | `workflow_dispatch` |
| `.github/workflows/release.yml` | push `v*` tag | `workflow_dispatch` |
| `.github/workflows/sync-upstream.yml` | 每日定时 | `workflow_dispatch` |
| `.github/workflows/cla.yml` | issue_comment / pull_request_target | `workflow_dispatch` |

**合并注意**：上游会携带自动触发配置，合并后必须逐文件检查 `on:` 段，确保还原为 `workflow_dispatch:`。

---

### 10. 脚本与工具

上游无以下目录/文件，通常不产生冲突，但合并后应确认未被意外删除。

| 文件 | 说明 |
|-----|------|
| `script/dev_local.sh` | 本地开发环境启停（后端 :8082，前端 :3002） |
| `script/sync_upstream_to_zhiguofan.sh` | 上游同步自动化脚本 |
| `tools/openai_quick_test.py` | OpenAI 兼容接口快速测试 |
| `test/api_reconciliation_test.go` | API 一致性测试 |
| `statics/we2ai.com/index.html` | We2AI 静态文档页 |
| `statics/api.cxm.icu/index.html` | API 文档静态页 |

---

## 合并后最低验证清单

```bash
# 1. 后端编译和单元测试
cd backend && go build ./cmd/server/ && go test -tags=unit ./internal/service ./internal/repository ./internal/handler/...

# 2. 关键路由是否存在
grep -r "model-discounts\|/models\|provider-pricing" backend/internal/server/routes/

# 3. wire_gen.go 关键注入点
grep -A2 "NewSettingHandler\|NewAdminService\|providerPricingHandler\|endpointRepository" backend/cmd/server/wire_gen.go

# 4. billing_service 折扣调用
grep "applyDiscount" backend/internal/service/billing_service.go

# 5. 前端路由完整性
grep -E "'/models'|model-discounts|provider-pricing" frontend/src/router/index.ts

# 6. i18n 键完整性
grep -E "nav\.models|models\.|providers\." frontend/src/i18n/locales/zh.ts | head -10

# 7. Workflows 触发条件
grep -A3 "^on:" .github/workflows/*.yml

# 8. 前端测试
cd frontend && pnpm exec vitest run src/views/user/__tests__/ModelsView.spec.ts src/i18n/__tests__/usageServiceTierLocales.spec.ts

# 9. fork 功能守护（MAAS P2-6）
grep -q '/lingjing/v1/video' backend/internal/server/routes/gateway.go && echo "OK: lingjing route" || echo "MISSING: lingjing route"

# 10. MAAS Phase 2 协议分流函数
grep -q 'getGroupInboundProtocol' backend/internal/server/routes/gateway.go && echo "OK" || echo "MISSING"

# 11. MAAS Phase 5 回滚开关
grep -q 'ProtocolBucketEnabled' backend/internal/config/config.go && echo "OK" || echo "MISSING"

# 12. MAAS Phase 0 上游成本快照装配点（上游 0.1.147 拆分后在 gateway_usage_billing.go）
grep -q 'ApplyUpstreamCostSnapshot' backend/internal/service/gateway_usage_billing.go && echo "OK" || echo "MISSING"

# 13. Ent schema 是否需要重跑 generate（有 schema 文件变更时执行）
cd backend && go generate ./ent

# 14. 注册接口用户名必填校验
grep 'username.*binding.*required' backend/internal/handler/auth_handler.go && echo "OK" || echo "MISSING"
```

---

## wire_gen.go 关键注入点速查

```go
// 用户统计 Handler（需传入 usageLogRepo）
adminService := service.NewAdminService(..., usageLogRepo, ...)

// Setting Handler（需传入 pricingService、adminService）
settingHandler := admin.NewSettingHandler(..., pricingService, adminService)

// 路由注册
routes.RegisterAdminRoutes(v1, h, adminAuth)

// 灵境接入（lingjing）— openAIGatewayService 创建之后
lingjingClient := service.NewLingjingClient()
lingjingTaskRepository := repository.NewLingjingTaskRepository(client)
lingjingGatewayService := service.NewLingjingGatewayService(lingjingClient, lingjingTaskRepository, accountRepository, schedulerSnapshotService)
openAIGatewayService.SetLingjingService(lingjingGatewayService)

// lingjing Handler（availableChannelHandler 附近）
lingjingHandler := handler.NewLingjingHandler(lingjingGatewayService, billingCacheService, apiKeyService)

// lingjing 后台轮询 Runner（channelMonitorRunner 附近）
lingjingPollRunner := service.ProvideLingjingPollRunner(lingjingTaskRepository, lingjingGatewayService, accountRepository, billingService, billingCacheService)

// provideCleanup 末尾参数
provideCleanup(..., lingjingPollRunner)

// ===== MAAS 重构新增注入点（feature/maas-refactor） =====

// Phase 0：供应商定价（provider_pricing）
providerPricingRepository := repository.NewProviderPricingRepository(client)
upstreamCostResolver := service.NewUpstreamCostResolver(providerPricingRepository)
providerPricingHandler := admin.NewProviderPricingHandler(providerPricingRepository)
// 注：applyUpstreamCostSnapshot() 通过 upstreamCostResolver 在 gateway_service / openai_gateway_service 内部调用

// Phase 3：协议桥接注册表
protocolBridgeRegistry := service.NewProtocolBridgeRegistry()
// 各桥在 registry 初始化时通过 Register() 注入，无需独立 wire 变量

// Phase 5：Endpoint Repository
endpointRepository := repository.NewEndpointRepository(client)
// schedulerSnapshotService 接收 cfg.Gateway.Scheduling（含 ProtocolBucketEnabled）
schedulerSnapshotService := service.NewSchedulerSnapshotService(..., endpointRepository, cfg.Gateway.Scheduling)

// 功能 24/34：模型定价 Handler（6 参，含 accountTestService 用于 sync-from-upstream + modelRoutingService 用于后台=广场口径）
modelRoutingService := service.NewModelRoutingService(accountRepository, endpointRepository, pricingService)
modelPricingHandler := admin.NewModelPricingHandler(modelPricingService, modelPricingRepository, pricingService, settingService, accountTestService, modelRoutingService)
// 功能 34：usage Handler 也注入 modelRoutingService（广场口径）
usageHandler := handler.NewUsageHandler(..., modelRoutingService)
```

---

### 11. 移除 OAuth 授权流程（前端 UI）

从账号创建/编辑弹窗中移除所有 OAuth 相关 UI 入口，仅保留 API Key / Setup Token 方式。

**改动文件**

| 文件 | 说明 |
|-----|------|
| `frontend/src/components/account/CreateAccountModal.vue` | 删除 OpenAI "OAuth (ChatGPT OAuth)" 账号类型按钮；删除 Anthropic "添加方式" OAuth/Setup Token 单选框；删除所有 OAuth 相关 handler 函数（`handleOpenAIExchange`、`handleGeminiExchange`、`handleAnthropicExchange`、`handleOpenAIBatchRT`、`handleOpenAIValidateRT`、`buildOpenAICodexImportCredentialExtras`、`formatCodexImportMessages`、`OPENAI_MOBILE_RT_CLIENT_ID`） |
| `frontend/src/components/account/EditAccountModal.vue` | 同步修复因删除 OAuth 区块导致的未闭合标签 |
| `frontend/src/i18n/locales/zh.ts` & `en.ts` | `oauthSetupToken` 文本从 `'OAuth / Setup Token'` 改为 `'Setup Token'` |

**合并注意**：上游若新增 OAuth 相关 UI，合并后需重新删除；`CreateAccountModal.vue` 中 `accountCategory` 仍保留 `'oauth-based'` 值用于后端逻辑，仅 UI 入口被移除。

---

### 12. 京东云灵境（Doubao Seedream / Seedance）接入

支持三类能力：Seedream 生图（同步轮询返回）、Seedance 文生视频/图生视频（异步 taskId）。API 全异步任务制，Base URL `https://model.jdcloud.com`。

**后端 - 新增文件**

| 文件 | 说明 |
|-----|------|
| `backend/ent/schema/lingjing_task.go` | Ent Schema：视频任务持久化（gen_task_id、status、result_url、billing 字段等） |
| `backend/internal/service/lingjing_task_port.go` | `LingjingTask` 领域结构体 + `LingjingTaskRepository` 接口 + 任务状态常量 |
| `backend/internal/service/lingjing_client.go` | HTTP 客户端：`SubmitTask` / `QueryTaskResult`（保留官方 URL typo） |
| `backend/internal/service/lingjing_gateway_service.go` | 核心 Service：`SelectAccount`、`ForwardSeedreamImage`（同步轮询 60s）、`SubmitVideoTask`、`GetTaskStatus` |
| `backend/internal/service/lingjing_images.go` | `ForwardImages` 中的 lingjing 分支：尺寸映射 → 提交任务 → 轮询 → 返回 OpenAI 兼容格式 |
| `backend/internal/service/lingjing_poll_runner.go` | 后台轮询 Runner（alitto/pond v2，5s ticker，10 workers，max 240 次），触发计费 |
| `backend/internal/repository/lingjing_task_repo.go` | `LingjingTaskRepository` Ent 实现 |
| `backend/internal/handler/lingjing_handler.go` | `POST /lingjing/v1/video/submit`（HTTP 202）、`GET /lingjing/v1/video/:taskId` |

**后端 - 修改文件**

| 文件 | 改动要点 |
|-----|---------|
| `backend/internal/domain/constants.go` | 追加 `PlatformLingjing = "lingjing"` |
| `backend/internal/service/scheduler_snapshot_service.go` | L484/L786 平台列表追加 `PlatformLingjing` |
| `backend/internal/service/account.go` | 追加 `IsLingjing()`、`GetLingjingAPIKey()` 方法 |
| `backend/internal/service/openai_gateway_service.go` | 追加 `lingjingSvc` 字段 + `SetLingjingService()` setter |
| `backend/internal/service/openai_images.go` | `ForwardImages` 前置 `IsLingjing()` 分支 |
| `backend/internal/handler/handler.go` | `Handlers` struct 追加 `Lingjing *LingjingHandler` |
| `backend/internal/handler/wire.go` | `ProvideHandlers` 追加 `lingjingHandler`；ProviderSet 追加 `NewLingjingHandler` |
| `backend/internal/service/wire.go` | ProviderSet 追加 `NewLingjingClient`、`NewLingjingGatewayService`、`ProvideLingjingPollRunner` |
| `backend/internal/repository/wire.go` | ProviderSet 追加 `NewLingjingTaskRepository` |
| `backend/internal/server/routes/gateway.go` | `/v1/images/generations` 允许 lingjing 平台；新增 `/lingjing/v1/video/*` 路由组 |
| `backend/cmd/server/wire.go` | `provideCleanup` 追加 `lingjingPollRunner` 参数 |
| `backend/cmd/server/wire_gen.go` | 手动补全所有 lingjing 初始化链（见速查表） |
| `backend/cmd/server/wire_gen_test.go` | `provideCleanup` 调用追加 `nil` 占位 |

**前端**

| 文件 | 改动要点 |
|-----|---------|
| `frontend/src/types/index.ts` | `GroupPlatform`、`AccountPlatform` 追加 `'lingjing'`；补齐 `AccountPlatform` 中缺失的 `'antigravity'` |
| `frontend/src/utils/platformColors.ts` | 追加 lingjing 全部颜色映射（红色主题）及 `platformLabel()` case |
| `frontend/src/components/account/CreateAccountModal.vue` | 平台选择器追加灵境按钮；追加 lingjing 账号类型选择区块；切换 lingjing 时 baseUrl 置空、accountCategory 设为 apikey；模型限制区块排除 lingjing |
| `frontend/src/components/account/EditAccountModal.vue` | 占位符/提示文案追加 lingjing 分支；模型限制区块排除 lingjing |

**API 路由**

| 方法 | 路径 | 说明 |
|-----|------|------|
| POST | `/v1/images/generations` | 复用现有路由，lingjing 账号自动分流 |
| POST | `/lingjing/v1/video/submit` | 提交视频任务，返回 HTTP 202 + taskId |
| GET | `/lingjing/v1/video/:taskId` | 查询视频任务状态/结果 |

**关键常量（apiId）**：初版 6 个常量已被功能 28 扩展为 51 个（`lingjing_client.go`），完整对照见 [`claudedocs/lingjing/model_catalog.md`](lingjing/model_catalog.md)。

**合并注意**：`gateway.go` 中 `/v1/images/generations` 的 lingjing 平台判断和 `/lingjing/v1/video/*` 路由组不能丢失；`wire_gen.go` 中 lingjing 初始化链（5 行）、`provideCleanup` 调用（含 `lingjingPollRunner`）不能丢失；上游若修改 `openai_images.go` 的 `ForwardImages` 函数，需确认 `IsLingjing()` 前置分支仍在。

---

### 13. 用户注册/创建必填用户名

注册接口和管理员创建用户接口均要求用户名为必填项。

**改动文件**

| 文件 | 说明 |
|-----|------|
| `backend/internal/handler/auth_handler.go` | `RegisterRequest.Username` 加 `binding:"required"`；`Register()` 传 username 给 service |
| `backend/internal/service/auth_service.go` | `RegisterWithVerification()` 签名加 `username` 参数；创建用户时赋值 `Username` |
| `backend/internal/handler/admin/user_handler.go` | `CreateUserRequest.Username` 加 `binding:"required"` |
| `frontend/src/types/index.ts` | `RegisterRequest` 加 `username: string`（非 optional） |
| `frontend/src/views/auth/RegisterView.vue` | 新增用户名输入框（email 后、password 前）；校验 + 传参 |
| `frontend/src/views/auth/EmailVerifyView.vue` | 从 sessionStorage 读取 `username` 并在注册时传递 |
| `frontend/src/components/admin/user/UserCreateModal.vue` | username input 加 `required` 属性 |
| `frontend/src/i18n/locales/zh.ts` & `en.ts` | auth 节新增 `usernameLabel`、`usernamePlaceholder`、`usernameRequired` |

**合并注意**：上游 `RegisterRequest` 无 `username` 字段，合并后需确认后端 struct 和前端 `RegisterRequest` 类型定义均含必填 `username`。

---

### 14. 上游成本快照（MAAS Phase 0）

为每条 UsageLog 记录附加上游原始成本，支持毛利率查询与对账。

**后端 - 新增文件**

| 文件 | 说明 |
|-----|------|
| `backend/ent/schema/provider_pricing.go` | 供应商单价 Ent Schema（含 `go generate ./ent`） |
| `backend/internal/repository/provider_pricing_repository.go` | `ProviderPricingRepository` CRUD + `FindEffective()` |
| `backend/internal/service/upstream_cost.go` | `UpstreamCostResolver` + `applyUpstreamCostSnapshot()` helper |
| `backend/internal/handler/admin/provider_pricing_handler.go` | 供应商定价 CRUD Admin API（**独立 handler，不进 SettingHandler**） |
| `script/reconcile_upstream_cost.sh` | 每日对账 job（产出 provider/account 维度差异报告） |

**后端 - 修改文件**

| 文件 | 改动要点 |
|-----|---------|
| `backend/ent/schema/usage_log.go` | 新增 9 列：`upstream_unit_price_input/output/cache_creation/cache_read`、`upstream_total_cost`、`provider`、`pricing_source`、`async_task_id`、`cost_finalized_at` |
| `backend/internal/service/gateway_usage_billing.go` | `ApplyUpstreamCostSnapshot()` 调用（装配点 1/3，Claude 主路径；上游 0.1.147 从 `gateway_service.go` 拆出）；命中 provider_pricing 才填成本列，未命中保持 NULL（无独立 env flag） |
| `backend/internal/service/openai_gateway_usage.go` | `ApplyUpstreamCostSnapshot()` 调用（装配点 2/3，OpenAI 路径；上游 0.1.147 从 `openai_gateway_service.go` 拆出） |
| `backend/internal/service/lingjing_poll_runner.go` | `ApplyUpstreamCostSnapshot()` 调用（装配点 3/3，异步路径，flag 强制 ON） |

**API**：`GET/POST/PUT/DELETE /api/v1/admin/provider-pricings`（**复数**，backend admin-API-only，无前端页面）

**门控机制**：上游成本快照由 `ApplyUpstreamCostSnapshot()`（`upstream_cost.go`）驱动——命中 provider_pricing 表则填 9 列，未命中保持 NULL；lingjing 异步路径强制写入。文档曾提的 `USAGE_UPSTREAM_COST_ENABLED` env flag **未实现为 config 字段/env**（仅注释/测试中提及），不要据此查找开关。

**合并注意**：`ent/schema/usage_log.go` 新增 9 列后必须重跑 `go generate ./ent`；上游若修改 `gateway_usage_billing.go` 或 `openai_gateway_usage.go` 中的 UsageLog 构造函数，需确认 `ApplyUpstreamCostSnapshot()` 调用未丢失；`provider_pricing_handler.go` 为独立新文件，不污染 `setting_handler.go`。

---

### 15. 供应商规范化与分布视图（MAAS Phase 1）

DeepSeek / 豆包 / 硅基流动等账号通过 `extra.provider` 元信息写入规范化 provider_key；新增 Provider/Account 跨 group 分布统计图表。

**后端 - 新增文件**

| 文件 | 说明 |
|-----|------|
| `backend/internal/service/provider_normalize.go` | `NormalizeProvider()` 别名表（`DeepSeek` / `deep-seek` → `deepseek` 等） |
| `backend/cmd/migrate_provider/` | 历史回填 job（仅处理 `extra.provider` 非空且账号存活的 UsageLog 行） |

**后端 - 修改文件**

| 文件 | 改动要点 |
|-----|---------|
| `backend/internal/repository/usage_log_repo.go` | 新增 `GetStatsByProvider` + `GetAccountStatsCrossGroup`（**复用已有 `getEntityUsageStats`**，不新建范式） |

**前端**

| 文件 | 说明 |
|-----|------|
| `frontend/src/components/charts/ProviderDistributionChart.vue` | Provider 分布图表（新增） |
| `frontend/src/components/charts/AccountDistributionChart.vue` | Account 分布图表（新增） |
| `frontend/src/components/account/CreateAccountModal.vue` | OpenAI APIKey 模式新增 Provider 预设下拉 + 自定义 base_url（OAuth 入口保持已删除状态） |
| `frontend/src/i18n/locales/zh.ts` & `en.ts` | `admin.accounts.providers.*`、`admin.dashboard.providers.*` 键值 |

**合并注意**：`usage_log_repo.go` 新增方法必须复用 `getEntityUsageStats` 私有方法；上游若修改 `CreateAccountModal.vue`，需确认 OAuth 入口仍处于删除状态（fork 11 约束）；历史回填 job 仅按 `extra.provider` 细粒度写入，**不按 `account.platform` 粗粒度回填**（防止 provider_key 桶污染）。

---

### 16. InboundProtocol / OutboundProtocol 双写（MAAS Phase 2）

Group 新增 `inbound_protocol`，Account 新增 `outbound_protocol`；路由分流改为优先读新字段，旧 `platform` 字段保留为回退 alias。

**后端 - 新增文件**

| 文件 | 说明 |
|-----|------|
| `backend/internal/domain/protocol.go` | 协议常量集中（`InboundProtocol`、`OutboundProtocol` 枚举）+ `ResolveInboundProtocol()` 函数 |

**后端 - 修改文件**

| 文件 | 改动要点 |
|-----|---------|
| `backend/ent/schema/group.go` | 新增 `inbound_protocol` 字段（迁移脚本从 `platform` 推导默认值） |
| `backend/ent/schema/account.go` | 新增 `outbound_protocol` 字段 |
| `backend/internal/service/domain_constants.go` | `PlatformOpenAI` 等改 alias 指向 `domain/protocol.go` 常量 |
| `backend/internal/server/routes/gateway.go` | `getGroupInboundProtocol()` 函数（先 `InboundProtocol` 后回退 `Platform`）；约 10-11 处协议分流 switch 改造；保留 `/lingjing/v1/video/*` 路由组 |

**验收（P2-6 守护检查）**：
```bash
# CI 守护检查，确认 fork 关键路由未被清除
grep -q '/lingjing/v1/video' backend/internal/server/routes/gateway.go
```

**合并注意**：`gateway.go` 是 P2-3 高改动文件（~10-11 处），上游同步后须逐行检查 `getGroupInboundProtocol()` 函数及协议 switch 分支是否完整；`group.go`/`account.go` schema 变更后必须重跑 `go generate ./ent`；`domain/protocol.go` 是纯新增文件，上游无，但 `domain_constants.go` 可能与上游 alias 产生冲突。

---

### 17. 协议桥接注册表 Bridge Registry（MAAS Phase 3）

把 `ForwardAsAnthropic` / `ForwardAsResponses` 等协议转发逻辑抽到注册表；补齐 N×M 协议矩阵，建立流式 SSE 回归测试集。

**后端 - 新增文件**

| 文件 | 说明 |
|-----|------|
| `backend/internal/service/protocol_bridge_registry.go` | `ProtocolBridgeRegistry`（map 注册表）+ `ProtocolBridge` interface（桥数≥3 后抽出） |
| `backend/internal/pkg/apicompat/anthropic_to_chatcompletions.go` | Anthropic Messages → OpenAI Chat Completions 桥（含流式） |
| `backend/internal/pkg/apicompat/chatcompletions_to_anthropic.go` | Chat Completions → Anthropic Messages 桥 |
| `backend/internal/pkg/apicompat/responses_to_chatcompletions.go` / `chatcompletions_to_responses.go` | Responses ↔ Chat Completions 互转桥 |
| `backend/internal/pkg/apicompat/sse_replay.go` | SSE fixture 重放框架（录制工具 + `testdata/sse/` 目录约定） |
| `backend/testdata/sse/` | 每协议 ≥5 个 SSE fixture 录制文件 |

**合并注意**：`apicompat/` 全部为新增文件，上游无，通常不冲突；`protocol_bridge_registry.go` 需在 `wire_gen.go` 中正确初始化；`ProtocolBridge` interface 所有实现（包括 lingjing 桥）须保持接口满足。

---

### 18. Gemini 入站协议桥（MAAS Phase 4）

`/v1beta/*` Gemini 路由可路由到 Anthropic 或 OpenAI 账号；UsageLog 记录 `inbound_protocol='gemini_v1beta'`。

**后端 - 新增文件**

| 文件 | 说明 |
|-----|------|
| `backend/internal/pkg/apicompat/gemini_to_openai.go` | Gemini generateContent → OpenAI Chat Completions 转换 |
| `backend/internal/pkg/apicompat/gemini_to_anthropic.go` | Gemini generateContent → Anthropic Messages 转换 |
| `backend/internal/pkg/apicompat/gemini_types.go` | Gemini 公共类型定义 |

**后端 - 修改文件**

| 文件 | 改动要点 |
|-----|---------|
| `backend/internal/handler/gemini_v1beta_handler.go` | 接入 Bridge Registry（保留 native 路径作回退）；UsageLog `inbound_protocol` 写入 `gemini_v1beta` |

**合并注意**：上游若修改 `gemini_v1beta_handler.go`，需确认 Bridge Registry 接入点和 `inbound_protocol` 写入未丢失；native 路径（直连 Gemini 账号）必须保留，不能被 Bridge 完全替代。

---

### 19. Generic 多 Endpoint + Scheduler 协议桶（MAAS Phase 5）

单账号支持挂多个 endpoint；scheduler 按 endpoint 粒度分桶；双桶并存 4 周验证后切单桶；保留 `ProtocolBucketEnabled` 回滚开关。

**后端 - 新增文件**

| 文件 | 说明 |
|-----|------|
| `backend/ent/schema/endpoint.go` | `Endpoint` Ent Schema（含 `go generate ./ent`） |
| `backend/internal/repository/endpoint_repository.go` | `EndpointRepository` CRUD |
| `backend/internal/service/scheduler_dual_bucket.go` | 双桶影子比较逻辑（`bucketFor` 双写 + 对比 metric） |

**后端 - 修改文件**

| 文件 | 改动要点 |
|-----|---------|
| `backend/ent/schema/account.go` | Account 1→N Endpoint 关联（迁移脚本范围：`WHERE platform != 'lingjing'`） |
| `backend/internal/service/scheduler_snapshot_service.go` | L650 `ProtocolBucketEnabled` 分支（P5-6 切单桶主路径）；`scheduler_dual_bucket.go` 双写集成 |
| `backend/internal/domain/constants.go` | 追加 `PlatformGeneric = "generic"`（L26–L29） |
| `backend/internal/config/config.go` | `Gateway.Scheduling.ProtocolBucketEnabled`（L999，`protocol_bucket_enabled` env var） |

**前端**

| 文件 | 说明 |
|-----|------|
| `frontend/src/components/account/CreateAccountModal.vue` | Generic 账号类型；账号级 API Key + Endpoint 列表表单（详见功能 24） |
| `frontend/src/components/account/EditAccountModal.vue` | Generic Endpoint 列表编辑器（增删端点，单独 `updateAccountEndpoints` 保存）+ 账号级 API Key 编辑；`handleSubmit` apikey 凭证块以 `platform !== 'generic'` 排除 generic（generic 走专用分支写 `credentials.api_key`），详见功能 24 |
| `frontend/src/types/index.ts` | `AccountPlatform` 追加 `'generic'` |

**关键配置**：`PROTOCOL_BUCKET_ENABLED=true`（env var）切换到协议桶单路径；`false`（默认）保持兼容旧路径。

**合并注意（高危）**：
- `scheduler_snapshot_service.go` 是高改动文件，上游同步后须确认 L650 `ProtocolBucketEnabled` 分支完整、`scheduler_dual_bucket.go` 引用未丢失
- lingjing 账号**不参与** endpoint 实体迁移（迁移脚本 `WHERE platform != 'lingjing'` 过滤）；上游若修改 Account schema，须确认排除条款仍生效
- `ent/schema/endpoint.go` 合并后须重跑 `go generate ./ent`
- `PlatformGeneric` 常量追加在 `constants.go`（非 `protocol.go`），两文件语义正交，不要合并

---

### 20. 操作日志与请求详情新增客户端 IP 和 API 密钥字段

管理员在运维操作日志和请求详情弹窗中可查看来源 IP 和 API 密钥名称，便于排查问题和归因。

**后端**

| 文件 | 改动要点 |
|-----|---------|
| `backend/internal/repository/ops_repo.go` | 错误日志列表查询增加 `LEFT JOIN api_keys`，返回 `api_key_name` |
| `backend/internal/repository/ops_repo_request_details.go` | 请求详情查询增加 `client_ip`（可空）和 `api_key_name` 列 |
| `backend/internal/service/ops_models.go` | `OpsErrorLogItem` 追加 `APIKeyName string` |
| `backend/internal/service/ops_request_details.go` | `OpsRequestDetail` 追加 `ClientIP *string` 和 `APIKeyName string` |

**前端**

| 文件 | 改动要点 |
|-----|---------|
| `frontend/src/api/admin/ops.ts` | 类型定义追加 `client_ip?: string \| null`、`api_key_name?: string \| null` |
| `frontend/src/i18n/locales/zh.ts` & `en.ts` | `admin.ops.errorDetail.clientIp`、`admin.ops.errorDetail.apiKey`、`admin.ops.requestDetails.clientIp`、`admin.ops.requestDetails.apiKey` |
| `frontend/src/views/admin/ops/components/OpsErrorDetailModal.vue` | 新增 Client IP 和 API Key 展示区块 |
| `frontend/src/views/admin/ops/components/OpsErrorLogTable.vue` | 新增列展示 |
| `frontend/src/views/admin/ops/components/OpsRequestDetailsModal.vue` | 新增 Client IP 和 API Key 展示 |
| `frontend/src/components/common/BaseDialog.vue` | 扩宽 `full` 尺寸上限以容纳更多字段 |

**合并注意**：上游若修改 `ops_repo.go` / `ops_repo_request_details.go` 的 SQL，须确认 `LEFT JOIN api_keys` 和 `client_ip` 列追加未丢失；i18n 新增 4 个键须保留。

---

### 21. 版本发布基础设施对接 fork 仓库

后台"检查更新"和一键安装脚本统一指向 fork 仓库 `imleoo/tokenpanel`，修复前端将 `1.x` 版本误判为自定义构建（禁用更新按钮）的问题。

**后端**

| 文件 | 改动要点 |
|-----|---------|
| `backend/internal/service/update_service.go` | `githubRepo` 常量由 `"Wei-Shaw/sub2api"` 改为 `"imleoo/tokenpanel"` |

**前端**

| 文件 | 改动要点 |
|-----|---------|
| `frontend/src/components/common/VersionBadge.vue` | `isCustomBuild` 由 `v.startsWith('1.')` 改为 `buildType.value === 'source'`，以 goreleaser ldflags 注入的 `build_type` 字段为准 |

**部署**

| 文件 | 改动要点 |
|-----|---------|
| `deploy/install.sh` | `GITHUB_REPO`、Usage 注释 URL、systemd `Documentation` 字段均改为 fork 地址 |
| `Dockerfile.goreleaser` | `maintainer` 和 `org.opencontainers.image.source` label 改为 fork |

**合并注意**：上游若修改 `install.sh` 的 `GITHUB_REPO`，须还原为 `imleoo/tokenpanel`；`VersionBadge.vue` 中 `isCustomBuild` 逻辑不可被上游覆盖（上游无此字段，会直接覆盖成按版本号判断）。

---

### 22. 文档精简与 Release 安装说明增强

统一文档中的数据库/Redis 版本要求，精简三语 README，并在 GitHub Release 页面提供完整安装和升级指南。

**改动文件**

| 文件 | 改动要点 |
|-----|---------|
| `.goreleaser.yaml` / `.goreleaser.simple.yaml` | Release footer 从简单几行扩展为完整安装指南：区分 Docker Compose（含内置 PG+Redis）和外部数据库（Standalone）两种方式；.env 必填项表格（`POSTGRES_PASSWORD`/`DATABASE_HOST`/`REDIS_HOST`）；强烈建议设置项说明（`JWT_SECRET`/`TOTP_ENCRYPTION_KEY` 留空的后果）；升级步骤（含数据库备份建议） |
| `README.md` / `README_CN.md` / `README_JA.md` | 删除与系统无关内容：赞助商（❤️ Sponsors）、在线演示（上游演示站）、官方域名声明、Trendshift badge、生态项目（Ecosystem）、Star History、底部 Star 号召；保留并整理：项目概述、功能列表、技术栈、部署方式、开发模式/代码生成注意事项、项目结构 |
| `deploy/DOCKER.md` | 示例 compose 镜像 `postgres:15-alpine` → `postgres:18-alpine`，`redis:7-alpine` → `redis:8-alpine` |
| `README.md` / `README_CN.md` / `README_JA.md` / `CODEBUDDY.md` / `claudedocs/系统架构设计文档.md` | 版本要求统一：PostgreSQL 15+/16 → PostgreSQL 18+，Redis 7+/7 → Redis 8+ |

**合并注意**：上游 README 包含大量赞助商和演示站内容，每次 merge 上游后需重新清理；goreleaser footer 改动在 `.goreleaser.yaml` 中维护，上游同步时注意 `release.footer` 段不被覆盖。

---

### 23. 全量文档同步更新（代码与文档对齐）

一次性文档维护（2026-05-21，针对 v1.1.129）：`repo-wiki.md`、`系统架构设计文档.md`、`admin-manual.md`、`DEV_GUIDE.md`、`API_DOCUMENTATION.md` 等 6 个文档与代码对齐——补全 MAAS 各阶段路由/handler 清单、修正 ENV 变量全名（`GATEWAY_SCHEDULING_PROTOCOL_BUCKET_ENABLED`）、统一 PostgreSQL 18+/Redis 8+ 与 golangci-lint v2.9、新增 `/v1/responses` API 文档。

**合并注意**：纯文档改动，无合并风险。将来文档再漂移时按"先确认代码，再更新文档"操作。

---

### 24. 从上游导入模型到定价表（独立工具）+ 通用渠道账号级密钥

「模型定价」页提供"从上游导入"工具：填 Base URL + API Key + Provider（+ 可选鉴权头/方案），请求上游 `/v1/models`，把模型写入 `model_pricings` 表（统一定价源），管理员随后设置折扣率（`discount_rate`）/ 自定义价格。兼容 newapi / OpenAI 风格变体（`{data:[]}` / `{models:[]}` / 裸数组、`id`/`name`/`owned_by`）。

> 设计沿革：模型导入已从添加账号弹窗解耦为「模型定价」页独立工具；generic 账号为**账号级单 API Key（`credentials.api_key`）+ N 个协议 endpoint**。与功能 5 的关系：依赖统一 `model_pricings` 表（`discount_rate` 即新折扣），替代旧 `settings.model_discounts`（迁移进行中）。

**后端**

| 文件 | 改动要点 |
|-----|---------|
| `backend/internal/service/upstream_models.go` | `FetchModelsByConfig(ctx, baseURL, apiKey, authHeader, authScheme, proxyURL)`：无账号依赖，复用 `buildOpenAIModelsURL` + `extractUpstreamModelIDs`；authHeader 空回退 `Authorization`，scheme 空且 header 为 Authorization 时默认 `Bearer`，自定义 header（如 `x-api-key`）用裸 key。`upstreamModelEntry` 追加 `owned_by` |
| `backend/internal/handler/admin/model_pricing_handler.go` | `accountTestService` 字段 + `NewModelPricingHandler` 第 4 参数；`SyncFromUpstream`（拉取 → `repo.SeedIfNotExists` 仅插新、不覆盖 → 返回 `{models, fetched}`） |
| `backend/internal/server/routes/admin.go` | `POST /api/v1/admin/model-pricings/sync-from-upstream` |
| `backend/cmd/server/wire_gen.go` | `NewModelPricingHandler(...)` 追加 `accountTestService` 实参（手动补） |
| `backend/internal/service/upstream_models_test.go` | `FetchModelsByConfig` 覆盖三种 newapi 变体 / 自定义 header 无 scheme / 缺参 / 错误体不泄露 |

**前端**

| 文件 | 改动要点 |
|-----|---------|
| `frontend/src/api/admin/modelPricings.ts` | `syncModelPricingsFromUpstream` + 类型 |
| `frontend/src/views/admin/ModelPricingsView.vue` | "从上游导入"按钮 + 弹窗（Base URL / API Key / Provider 必填 / 可选鉴权头方案）；导入成功刷新列表 |
| `frontend/src/components/account/GenericEndpointModelsField.vue` | **新增（fork 独有）** 共享组件：端点 `supported_models` 编辑器——拉取上游模型 → 带搜索/全选/清空的复选清单（拉取结果 ∪ 已选）+ 折叠手动输入兜底。Create/Edit 两模态共用 |
| `frontend/src/components/account/CreateAccountModal.vue` | generic 块改为**账号级 API Key 输入 + 端点列表**；apikey 区块加 `platform !== 'generic'` 排除（修双份输入）；提交发 `credentials.api_key`；接入 `GenericEndpointModelsField`；**generic 段新增别名映射编辑器**，提交写 `credentials.model_mapping` |
| `frontend/src/components/account/EditAccountModal.vue` | generic 块加账号级 API Key 输入（留空不改）；submit 增加 generic 分支写 `credentials.api_key`；接入 `GenericEndpointModelsField`；**generic 段新增别名映射编辑器**，加载回填 + 提交写 `credentials.model_mapping` |
| `frontend/src/types/index.ts` | `AccountEndpointInput` 不含 api_key（密钥在账号级，非端点级） |
| `frontend/src/i18n/locales/zh.ts` & `en.ts` | `admin.accounts.generic.apiKey*`、`modelPicker*`、`modelMappingHint` 键值 |
| `backend/internal/service/model_routing_service.go` | generic 分支折入账号级 `model_mapping`（别名进可路由集，与 supported_models 并存） |
| `backend/internal/service/generic_gateway.go` | 新增 `genericEndpointSupportsModel`：generic 配了 model_mapping 后，各准入点 OR 上它补回 supported_models 直连放行（单调放宽） |
| `backend/internal/service/{openai_account_scheduler,gateway_service,gemini_messages_compat_service}.go` | 3 个准入点对 generic 改用 `genericEndpointSupportsModel`（openai `isOpenAIAccountEligibleForRequest` 因 generic 早返回不受影响） |
| `backend/internal/service/gateway_service.go` `GetAvailableModels` | 网关 `/v1/models` 模型来源**按运行模式分口径**：标准模式委托 `ModelRoutingService.routableFromAccounts`（与广场收敛，catalog 交集只留已启用）；simple 模式 raw（generic supported_models ∪ mapping，未定价也列出）。依据：标准模式计费对未定价 fail-closed。含 `modelRouting` 字段 + `SetModelRoutingService` setter（wire 手动注入，见合并注意） |
| `backend/internal/service/generic_gateway.go` `genericEndpointModelIDs` | **唯一口径**：generic 账号 supported_models 并集（+openEndpoint 标志）。`routableFromAccounts`/`GetAvailableModels`(simple 路径)/`genericEndpointSupportsModel`/`admin_service.GetGroupModelsListCandidates` 四处共用，防 generic 模型来源漂移 |
| `backend/internal/service/admin_service.go` `GetGroupModelsListCandidates` | 分组自定义 /v1/models 列表候选：generic 账号补 endpoint `supported_models`（走唯一口径）；`defaultModelsListCandidateIDs` 对 generic 返回 nil（不 seed claude 默认）。`NewAdminService` 新增 `endpointRepo` 构造参数（wire 末位实参 `endpointRepository`；改签名后同步 `api_contract_test`） |
| `frontend/src/components/account/__tests__/GenericEndpointModelsField.spec.ts` | **新增（fork 独有）** `GenericEndpointModelsField` 组件单测（拉取/勾选/全选清空/手动输入/校验，5 用例） |
| `backend/internal/handler/gateway_handler.go` `Models()` / `defaultModelIDsForPlatform` | handler 层 generic 回退收敛：`/v1/models` 无可路由模型时 generic 返回**空列表**（不再回退 claude 默认列表误导）；`defaultModelIDsForPlatform` 对 generic 返回 nil（与 admin 侧 `defaultModelsListCandidateIDs` 同口径） |
| `backend/internal/service/gateway_service.go` `GetAvailableModels`（raw 路径） | 空白名单 endpoint（=支持全部）在 raw 口径用已启用 catalog 兜底（此前丢弃 `openEndpoint` 标志，simple 模式下该类账号一个模型都列不出） |

**API**：`POST /api/v1/admin/model-pricings/sync-from-upstream`，body `{base_url, api_key, auth_header?, auth_scheme?, provider?, mode?}`，返回 `{models: string[], fetched: number}`。

**关键设计**
- 写入用 `is_custom=true` + `SeedIfNotExists`：仅插新 model_id，已设价格/折扣不被覆盖，且不被 LiteLLM 同步删除。
- Provider 必填、用渠道 slug（如 `wanjie`），避免多上游混入同一桶；`ModelWhitelistSelector` 按 provider 过滤即可正确归类。
- generic 数据模型：一个账号一个 `credentials.api_key`，挂多个不同 `outbound_protocol` 的 endpoint（base_url + auth_header + auth_scheme），同一 key 由各端点的鉴权头/方案携带。**万界方舟所有端点都用 `Authorization: Bearer`**（与 OpenAI 官方约定一致，不是 x-api-key/x-goog-api-key——这是 newapi/maas 中转的通用做法）；端点表单默认值 `auth_header=Authorization, auth_scheme=Bearer` 直接对应。

**运行时转发**：见功能 25（openai + anthropic + gemini 三协议直通已实现，flag 守卫）。

**合并注意**：`NewModelPricingHandler` 第 4 参数（`accountTestService`）须在 `wire_gen.go` 同步；`upstream_models.go`/`model_pricing_handler.go` 高改动，上游同步后确认未被覆盖；路由 `sync-from-upstream` 须保留。

---

### 25. generic 渠道运行时转发（MAAS Phase 5 收尾，flag 守卫，进行中）

让 generic 账号真正转发请求：按"入站协议 → 匹配同 `outbound_protocol` 的健康 endpoint"直通，用账号级 `credentials.api_key` + 端点鉴权头转发。全程受 feature flag 守卫。

**Feature flag**：`Gateway.Scheduling.GenericRuntimeEnabled`（env `GATEWAY_SCHEDULING_GENERIC_RUNTIME_ENABLED`，**当前默认 `true`**，开发期曾默认 false）。作回滚保险：出问题设 `false` 立即恢复旧行为。此 flag 只影响 `platform=generic` 账号，无 generic 账号时与改造前完全等价。

**已实现：openai（chat/completions + responses）+ anthropic（messages）+ gemini（v1beta generateContent / streamGenerateContent）三协议直通**

| 文件 | 改动要点 |
|-----|---------|
| `backend/internal/service/account.go` | `IsGeneric()`、`GetGenericAPIKey()` |
| `backend/internal/service/generic_gateway.go`（新） | `selectGenericEndpointForProtocols`（健康过滤+优先级）、`resolveGenericEndpointVia`（跨服务共用）；各网关侧 base_url 解析（openai/anthropic/gemini）；`genericProtocolsForPlatform`（anthropic→messages，gemini→v1beta） |
| `backend/internal/service/openai_gateway_service.go` | `endpointRepo` + `SetEndpointRepository`；`listSchedulableAccounts` 并入 generic 候选（flag 守卫）；eligibility 放行 generic；token 用 `GetGenericAPIKey`；responses/passthrough builder base_url 覆盖 |
| `backend/internal/service/openai_gateway_chat_completions_raw.go` | **chat/completions 原生直通**（generic 走此路）：apiKey/base_url 用账号 key + 端点 base_url |
| `backend/internal/service/gateway_service.go` | `endpointRepo` + `SetEndpointRepository`；`listSchedulableAccounts` 包一层并入 generic（anthropic + gemini）；Forward 路由 generic → anthropic API Key 直通；`buildUpstreamRequestAnthropicAPIKeyPassthrough` base_url 用端点 |
| `backend/internal/service/gemini_messages_compat_service.go` | `endpointRepo` + `SetEndpointRepository`；`ForwardNative` APIKey 路径 base_url 对 generic 用端点 base_url（`GetCredential("api_key")` 已天然支持 generic 的账号级 key） |
| `backend/cmd/server/wire_gen.go` | 三个网关服务各 `SetEndpointRepository(endpointRepository)` |
| `backend/internal/config/config.go` | `GenericRuntimeEnabled` flag |
| `backend/internal/service/generic_gateway_test.go` | 端点选择/优先级/健康、协议映射、`IsGeneric`/`GetGenericAPIKey` 单测 |

**关键设计**
- generic + {openai_chat, anthropic, gemini} ≈ 对应平台 apikey + 自定义 base_url：仅覆盖 base_url（openai 还需 key），复用既有转发机器。
- 单一守卫点（各网关 `listGeneric*` flag 判断 / `listSchedulableAccounts` 包装器）控制全部 generic 运行时。
- chat/completions 入站走 `forwardAsRawChatCompletions` 原生直通（不转 Responses，匹配 newapi 上游）；gemini 入站走 ForwardNative APIKey 路径（`{endpoint.base_url}/v1beta/models/{model}:{action}`，`x-goog-api-key`）。

**收尾完成项**（均已落地）
- UsageLog `endpoint_id` 归因：两个 RecordUsage 解析端点写 `endpoint_id` 列；`usage_log_repo` 全链路（select/insert×3/CTE×2/scan/arg-types/sqlmock）同步。
- GET /v1beta/models：`listSchedulableAccountsOnce` 并入 generic；`ForwardAIStudioGET` 用端点 base_url。
- 端点 `supported_models`：schema JSON 数组 + **迁移 `144_endpoint_supported_models.sql`**；前后端表单/`GetAvailableModels`/用户侧 `/v1/models` 全链路用端点 supported_models 并集。
- 修复 EditAccountModal 端点列表恒空（API 客户端缺 `await + .data` 解包）。
- 测试：离线单测 `generic_gateway_test.go`；在线脚本 `script/test_generic_runtime.sh`（需真实 maas key 实跑）。

**合并注意**：改动落在调度+网关热路径，但全部 flag 守卫（可一键回滚）；上游同步后确认 `openai_gateway_service.go`/`gateway_service.go`/`openai_gateway_chat_completions_raw.go` 的 generic 分支未丢失。**`wire_gen.go` 的 `SetEndpointRepository(endpointRepository)` 注入共 4 处**（`gatewayService`/`openAIGatewayService`/`geminiMessagesCompatService`/`accountTestService`），是 fork 手改的 setter 装配点，`go generate ./cmd/server` 重生成会**整体丢失**（wire 只生成构造器注入）。每次 generate 后必须 `grep -c '\.SetEndpointRepository(' backend/cmd/server/wire_gen.go` 确认为 **4**，否则通用渠道账号测试报 `Endpoint repository is not configured`、generic 运行时转发失效（2026-07-08 曾因此回归，见 CHANGELOG）。**另有 `gatewayService.SetModelRoutingService(modelRoutingService)` 一处手动 setter**（标准模式 `/v1/models` 与广场收敛用），同样会被 generate 丢失——`grep -c '\.SetModelRoutingService(' backend/cmd/server/wire_gen.go` 应为 **1**，丢失则标准模式 `/v1/models` 退回 raw 口径（未定价 generic 模型会重新出现在 Playground）。

---

### 26. 模型定价 SSOT 重构（DB 为唯一真源）

将计费核心从"64 条硬编码 fallbackPrices + 三套内存 map"迁移到以 `model_pricings` DB 表为唯一真源（SSOT），内存层降级为定时刷新的只读 catalog + alias 索引。**共 8 个 PR，已全部完成（2026-05-28）**。

**新增文件**

| 文件 | 说明 |
|-----|------|
| `backend/migrations/145_model_pricings_ssot_columns.sql` | 14 列 `ADD COLUMN IF NOT EXISTS` + `pricing_status` 回填 |
| `backend/internal/service/model_catalog_seed.go` | `BootstrapPricingSeeds()` 21 条启动 seed（含 16 条 billing fallback + 5 条灵境）；`RunBootstrapSeed` 启动时 `SeedIfNotExists` |
| `backend/internal/service/model_catalog_seed_test.go` | 种子内容 + 价格字面值回归测试 |
| `backend/internal/service/model_catalog_service.go` | `UpsertModels(ctx, batch, source)` 统一写路径，按 `source` 决定覆盖规则 |
| `backend/internal/service/billing_service_golden_test.go` | 14 模型 × 7 场景 × 6 token 边界黄金值测试（精确到 1e-12） |
| `backend/internal/service/pricing_service_match_test.go` | `matchByModelFamily` 10 个家族锚点命中固化 |
| `backend/scripts/verify_pricing_coverage/main.go` | 定价覆盖验证 CLI（exit 0 = 0 miss）；**注意：`backend/scripts/` 被 .gitignore 忽略，此文件仅存在于本地工作区、未入库**，新 clone 无此文件 |

**核心修改文件**

| 文件 | 改动要点 |
|-----|---------|
| `backend/ent/schema/model_pricing.go` | 新增 14 个字段（PR-1） |
| `backend/internal/service/pricing_service.go` | 新增 `catalog map[string]*DBModelPricing` + `aliasIdx map[string]string`；删除 `pricingData`/`discounts`/`customPrices` 及所有专用方法 |
| `backend/internal/service/billing_service.go` | `GetModelPricing` 重写：catalog → aliasIdx → matchByModelFamily → fail-closed（unpriced 5xx）；删除 `fallbackPrices` 64 条静态价格 |
| `backend/internal/service/openai_codex_transform.go` | 新增 `CodexAliasPairs()` 导出，供 aliasIdx 构建 |
| `backend/internal/repository/model_pricing_repository.go` | `UpsertBatch` / `SeedIfNotExists` 覆盖规则按 `source` 决策 |
| `backend/internal/handler/admin/model_pricing_handler.go` | 写路径改调 `ModelCatalogService.UpsertModels` |
| `backend/internal/handler/admin/account_handler.go` | 账号拉取模型落 endpoint + catalog（写路径统一） |
| `backend/internal/handler/usage_handler.go` | `collectWhitelistedModelsForAccounts` generic 分支改用 catalog is_enabled |

**新架构计费路径**

```
BillingService.GetModelPricing(model)
    ├─ catalog[model]                           // 直接命中
    ├─ aliasIdx[normalize(model)] → catalog[…] // 别名/归一化命中
    ├─ matchByModelFamily(catalog)              // 家族模糊匹配
    └─ pricing_status=unpriced → 5xx           // fail-closed
```

**已删除的旧机制**：`billing_service.go::fallbackPrices`（64 条静态价格）+ `getFallbackPricing()`；`pricing_service.go::pricingData`/`discounts`/`customPrices` 三套内存 map 及 `syncPricingToDB`/`loadDiscounts`/`migrateOldDiscounts`/`injectCustomPricingModels`；`downloadPricingData` 文件写入路径（JSON 只读路径保留作冷启动兜底）。

**新增字段**：14 列详见 `migrations/145_model_pricings_ssot_columns.sql`（核心：`source`、`pricing_status`（unpriced → fail-closed）、长上下文阈值/倍率、priority tier 单价、`output_cost_per_image`）。**灰度开关已终态**：`UseCatalogPath=true`、`LegacyFallbackEnabled=false`、`BootstrapSeedEnabled=true`、`ShadowCompare` 已删除。

**合并注意**：
- 上游不得重新引入 `fallbackPrices`、`pricingData`、`discounts`、`customPrices`
- `model_catalog_service.go` 和 `model_catalog_seed.go` 上游无此文件，合并时确认未被意外删除
- 迁移文件 `145_*.sql` 必须保留并执行
- 合并后运行：`go test -tags=unit ./internal/service ./internal/server ./internal/handler/...` 黄金值必须全绿

**完整报告**：[`docs/model-pricing-ssot-refactor-completed.md`](../docs/model-pricing-ssot-refactor-completed.md)

---

### 27. Bill-Request-ID 下游对账标识

引入专用响应头 `Bill-Request-ID`，让下游系统能用自己持有的订单号直接反查账单，闭合对账链路。与 `X-Request-Id`（上游服务商覆盖）完全隔离，互不干扰。

**设计背景与兜底规则**：网关转发时 `X-Request-Id` 会被上游覆盖，下游无法用自己的 ID 反查。`Bill-Request-ID` 由下游上传（≤64 字符原样回写+落库），未携带或超长时回退为本次 X-Client-Request-ID（UUID）。

**新增文件**

| 文件 | 说明 |
|-----|------|
| `backend/migrations/148_usage_log_bill_request_id.sql` | `ALTER TABLE usage_logs ADD COLUMN IF NOT EXISTS bill_request_id VARCHAR(64)` + 索引 |

**修改文件**

| 文件 | 改动要点 |
|-----|---------|
| `backend/internal/pkg/ctxkey/ctxkey.go` | 新增 `BillRequestID Key = "ctx_bill_request_id"` |
| `backend/internal/server/middleware/client_request_id.go` | 新增 `billRequestIDHeader` 常量；在两个代码路径（快速返回 + 新 UUID）均读取请求头 → 校验长度 → 兜底 → 回写响应头 → 存入 ctx |
| `backend/ent/schema/usage_log.go` | 新增 `bill_request_id` 字段（`Optional().Nillable().MaxLen(64)`），执行 `go generate ./ent` |
| `backend/internal/service/usage_log.go` | `UsageLog` 结构体追加 `BillRequestID string` |
| `backend/internal/service/gateway_service.go` | `buildRecordUsageLog` 从 ctx 读取 `BillRequestID` 并写入 `UsageLog` |
| `backend/internal/service/openai_gateway_service.go` | `RecordUsage` 从 ctx 读取 `BillRequestID` 并写入 `UsageLog`（新增 ctxkey import） |
| `backend/internal/repository/usage_log_repo.go` | `usageLogSelectColumns` + `usageLogInsertArgTypes` + 4 处 INSERT SQL + `prepareUsageLogInsert` args + `scanUsageLog` + `ListWithFilters` 过滤条件均补全 `bill_request_id` |
| `backend/internal/pkg/usagestats/usage_log_types.go` | `UsageLogFilters` 新增 `BillRequestID string` |
| `backend/internal/handler/dto/types.go` | `UsageLog` DTO 新增 `BillRequestID *string \`json:"bill_request_id,omitempty"\`` |
| `backend/internal/handler/dto/mappers.go` | `usageLogFromServiceUser` 补 `BillRequestID` 映射 |
| `backend/internal/handler/usage_handler.go` | `List` 方法解析 `bill_request_id` 查询参数并传入 filters |
| `backend/internal/repository/usage_log_repo_request_type_test.go` | INSERT mock expectations 补 `sqlmock.AnyArg()`；scan value slices 补 `sql.NullString{}` |
| `backend/internal/server/middleware/client_request_id_test.go` | 新增 4 个测试：上传值回写、无头兜底、超长兜底、与 X-Request-Id 隔离 |

**合并注意**：`usage_log_repo.go` 是 🔴 高风险（4 处 INSERT SQL + CTE + scan 全链路，arg 顺序错会数据错位）；上游同步后确认 INSERT SQL arg 顺序（`bill_request_id` 位于 `video_seconds` 之后、`created_at` 之前）未被覆盖；`client_request_id.go` 的两条代码路径均包含 BillRequestID 处理逻辑；migration 148 已执行。2026-07 同步上游 `#2007 feature/user-usage-admin-parity` 时实测：`usage_handler.go` 的 `List()` 若被上游重构为 `parseUserUsageFilters()` 统一解析入口，`bill_request_id` 解析需迁移为对 `parsed.Filters.BillRequestID` 赋值（而非独立构造 `UsageLogFilters{}` 字面量），否则冲突后容易整段丢失。

**API 使用**：参见 [`docs/RECONCILIATION_API_CN.md` — 附录：Bill-Request-ID](../docs/RECONCILIATION_API_CN.md#附录bill-request-id-下游对账标识)

---

### 28. 灵境模型目录全量化与官方刊例价落库

把京东云灵境平台官方支持的全部模型（可灵 / 海螺 / Vidu / 拍我 / 豆包 / Happy_Horse / 数字人 6 系列共 26 个独立 model_name + 3 新增 + 6 别名）一次性注册到 `model_pricings`，并把 JoySpace 内部刊例价（2026-06）换算为 USD 写入 `output_cost_per_image`。

**背景**：功能 12 仅接入了豆包 Seedream/Seedance，账号测试时遇到 `kling-v2-master` 等其它模型走默认 `Seedream-4.5` apiId 触发"参数值不在允许的范围内"；视频模型用前端默认 `"hi"` prompt 会被京东云排队挂起 5min 直接 deadline 超时。

**新增文件**

| 文件 | 说明 |
|-----|------|
| `backend/migrations/151_seed_lingjing_models.sql` | 26 个官方模型 INSERT，ON CONFLICT DO NOTHING（不覆盖管理员自定义） |
| `backend/migrations/152_update_lingjing_prices.sql` | UPDATE 23 个 canonical 模型的 USD 价格 + INSERT 3 个新模型（`viduq2-turbo`/`v5.6`/`OmniHuman1.5`）+ INSERT 6 个别名行（`kling-v3`/`kling-O1`/`kling-2.6`/`seedream4.0`/`seedream4.5`/`doubao-seedance-1-5-pro`） |
| `claudedocs/lingjing/raw_docs.json` | 22 个 docs.jdcloud.com/cn/lingjing 子页面原始抓取 |
| `claudedocs/lingjing/extracted.md` | 上述 JSON 的可读 markdown 版本 |
| `claudedocs/lingjing/model_catalog.md` | 26 个唯一 model_name 的对照总表（apiId / mode / 备注） |

**修改文件**

| 文件 | 改动要点 |
|-----|---------|
| `backend/internal/service/lingjing_client.go` | apiId 常量从 6 个扩到 **51 个**：可灵 13 个（550-554/560-567）、海螺 7 个（456-462）、Vidu 13 个（0/2/4/5/7/16/17/19-24）、拍我 8 个（400-402/501-505）、Happy_Horse 3 个（200202-200204）、数字人 `LingjingAPIIDOmniHuman=703` 等（精确清单以 `lingjing_client.go` 为准） |
| `backend/internal/service/lingjing_gateway_service.go` | 新增 `lingjingModelCatalogEntry` 结构 + `lingjingModelCatalog` 中心化映射表（27 条 canonical 条目，每条携带 `TextToVideoAPIID`/`ImageToVideoAPIID`/`RefToVideoAPIID`/`PictureAPIID`/`ModelKey`/`SupportedTestAPIID`）；新增 `lingjingModelAliasToCanonical`（6 条别名 → 官方名映射）；`lookupLingjingModel` 三段式解析（精确→别名→大小写忽略）；`buildLingjingTestSubmitRequest` 全量重写按模型类型分发参数模板；`lingjingModelToAPIID` / `isLingjingVideoModel` 改为走 catalog |
| `backend/internal/service/account_test_service.go` | 新增 `testLingjingAccountConnection`（97 行）；视频模型**强制覆盖前端 prompt** 为 `"蓝天白云，微风轻拂"`（绕开 "hi" 引发的上游排队 5min 卡死）；超时常量 `lingjingVideoTestMaxWait` 从 5min 提到 8min |

**模型总览与 apiId 对照**：26 canonical + 3 新增 + 6 别名，完整对照表见 [`claudedocs/lingjing/model_catalog.md`](lingjing/model_catalog.md)（唯一真源，本文档不再节选）。

**价格约定**：`output_cost_per_image` 对 video_generation = USD/秒、对 image_generation = USD/张；汇率 `1 USD = 7.28 CNY`；代表性档位 5s+720p+单镜头+非音画同步。**未列价（保持 `unpriced`）**：`v6`、`HappyHorse-1.0`、`cinema-generate-2.0`、`S2V-01`、`Doubao-Seedream-5.0-lite`、`Kling-V3` 图片专用变体。

**合并注意**：
- 上游同步若引入新的灵境模型，须 **同时** 在 `lingjingModelCatalog` 添加条目和 `151_seed_lingjing_models.sql` 同名 INSERT，且 model_id 严格一致
- 别名 6 条（`kling-v3` / `kling-O1` / `kling-2.6` / `seedream4.x` / `doubao-seedance-1-5-pro`）通过 `lingjingModelAliasToCanonical` 解析到 canonical，**不应** 在 `lingjingModelCatalog` 重复登记
- `output_cost_per_image` 字段对 video_generation 模式的"per-second"语义是项目历史约定（见 `model_catalog_seed.go:194-198` 注释），不要误改为 per-task 否则计费偏差 5-10 倍
- 待管理员后续在京东云控制台确认 `viduq2-turbo` 和 `v5.6` 的 apiId 后，需把它们加入 `lingjingModelCatalog`（目前只入了价格行未入路由表）

**完整模型/价格抓取与换算记录**：[`claudedocs/lingjing/model_catalog.md`](lingjing/model_catalog.md)

---

### 29. 手机号注册 + 短信验证码登录（火山引擎）

新增手机号 + 短信验证码注册/登录链路，验证码经火山引擎 SMS 下发并入 Redis 缓存做节流和校验。受管理员设置 `phone_register_enabled` 全局开关守卫。

**设计要点**
- E.164 归一化（`+8613xxxxxxxxx`）：中国大陆 11 位 `1` 开头自动补 `+86`，其它号段要求带 `+` 国家码。
- Redis 缓存 5 min TTL，60 s 重发节流；路由层附加请求级限流（send-sms-code 5/min、phone-register 5/min、phone-login 20/min，`FailureMode: RateLimitFailClose`）。
- 验证码下发与校验解耦：注册/登录共用 `SendSmsCodeForAuth`，复用 `RegisterWithVerification` 走和邮箱注册一致的用户创建/绑定路径。

**后端 — 新增文件**

| 文件 | 说明 |
|-----|------|
| `backend/internal/pkg/sms/volcengine.go` | 火山引擎 SMS HMAC-SHA256 签名 + 模板发送实现（209 行） |
| `backend/internal/repository/sms_cache.go` | Redis 验证码缓存（set/get/del + 节流键） |
| `backend/internal/service/sms_service.go` | `SmsService.SendVerifyCode`、`VerifyCode`；后续被功能 30 抽象为多服务商 |
| `backend/internal/service/auth_phone.go` | `NormalizePhone`、`SendSmsCodeForAuth`、`RegisterWithPhone`、`LoginWithPhone`（196 行） |
| `backend/migrations/149_users_add_phone.sql` | `users.phone` 列 |

**后端 — 修改文件**

| 文件 | 改动要点 |
|-----|---------|
| `backend/ent/schema/user.go` | 已有的 `phone` 字段从 Optional 升为业务必填的归一化值（注释更新） |
| `backend/internal/repository/user_repo.go` | `FindByPhone` 查询；创建/更新 phone 列 |
| `backend/internal/handler/auth_handler.go` | `SendSmsCode`/`PhoneRegister`/`PhoneLogin` 三 handler（共 130 行） |
| `backend/internal/server/routes/auth.go` | 三条新路由 + 限流 |
| `backend/internal/handler/admin/setting_handler.go` | `PhoneRegisterEnabled` + Volcengine 5 项凭证（AccessKeyID/Secret/AccountID/Sign/TemplateID） |
| `backend/internal/handler/dto/settings.go` | 同上 DTO |
| `backend/internal/service/setting_service.go` | `IsPhoneRegisterEnabled`、`LoadVolcengineConfig` |
| `backend/internal/service/domain_constants.go` | `SettingKeyPhoneRegisterEnabled`、`SettingKeyVolcengine*` 5 项常量 |
| `backend/internal/service/auth_service.go` | 注入 `smsService`、暴露 phone 路径 |
| `backend/cmd/server/wire_gen.go` | 注入 `smsCache` + `smsService` 到 `AuthService` |

**前端**

| 文件 | 改动要点 |
|-----|---------|
| `frontend/src/views/auth/LoginView.vue` | 「短信验证码登录」Tab + 倒计时按钮 |
| `frontend/src/views/auth/RegisterView.vue` | 「手机号注册」Tab + 验证码 + 用户名 |
| `frontend/src/views/admin/SettingsView.vue` | 手机号注册开关 + Volcengine 凭证表单 |
| `frontend/src/api/auth.ts` | `sendSmsCode`/`phoneRegister`/`phoneLogin` |
| `frontend/src/stores/auth.ts` | 手机号登录状态 |
| `frontend/src/utils/featureFlags.ts` | `phoneRegisterEnabled` flag |
| `frontend/src/i18n/locales/{zh,en}.ts` | `auth.phone*`、`admin.settings.volcengine*` 29 + 29 键 |

**API**：`POST /auth/send-sms-code`、`POST /auth/phone-register`、`POST /auth/phone-login`

**完整方案文档**：[`claudedocs/手机号注册短信登录方案.md`](手机号注册短信登录方案.md)（481 行）

**合并注意**：`user.go` schema 变更后必须 `go generate ./ent`；`149_*.sql` 必跑；`wire_gen.go` 中 `smsCache → smsService → authService` 注入链不能丢；上游若修改 `setting_handler.go`/`auth.go`，须确认本功能新增字段/路由未被覆盖。

---

### 30. 短信服务商抽象（腾讯云 + 阿里云）

把功能 29 的单一火山引擎扩展为「按设置切换」的多服务商架构：新增 `smsProvider` 接口，腾讯云和阿里云走各自签名算法，管理员在设置页选 `sms_provider` 字段切换。

**设计要点**
- 接口抽象：`SmsService.loadSmsClient(ctx)` 按 `sms_provider`（`volcengine` / `tencent` / `aliyun`）动态构造客户端；缺省值仍是 `volcengine`，向后兼容功能 29 单服务商部署。
- `signup_source` CHECK 约束加入 `phone`：迁移 `150_users_signup_source_add_phone.sql` 修正 user schema 的 `signup_source` 校验范围。

**后端 — 新增文件**

| 文件 | 说明 |
|-----|------|
| `backend/internal/pkg/sms/tencent.go` | 腾讯云 SMS v20210111 TC3-HMAC-SHA256 签名 + 接口实现（162 行） |
| `backend/internal/pkg/sms/aliyun.go` | 阿里云 SMS 2017-05-25 RPC POP 签名 + 接口实现（170 行） |
| `backend/migrations/150_users_signup_source_add_phone.sql` | `signup_source` CHECK 增加 `phone`、`sms` 值 |

**后端 — 修改文件**

| 文件 | 改动要点 |
|-----|---------|
| `backend/internal/service/sms_service.go` | 抽出 `smsProvider` 接口（`SendVerifyCode`）；`loadSmsClient` 三家分支 |
| `backend/internal/service/setting_service.go` | `SmsProvider`、`Tencent*`、`Aliyun*` 字段持久化 |
| `backend/internal/service/settings_view.go` | 同上视图 |
| `backend/internal/service/domain_constants.go` | `SettingKeySmsFrontend`、`SettingKeyTencent*`（5 项）、`SettingKeyAliyun*`（4 项） |
| `backend/internal/handler/admin/setting_handler.go` | DTO 与校验扩展三家配置（共 97 行新增） |
| `backend/internal/handler/dto/settings.go` | 同上 DTO |
| `backend/internal/service/auth_phone.go` | 错误分支细化（`ErrSmsNotConfigured` 等） |

**前端**

| 文件 | 改动要点 |
|-----|---------|
| `frontend/src/views/admin/SettingsView.vue` | 服务商下拉（火山/腾讯/阿里） + 各自凭证表单分组（208 行新增） |
| `frontend/src/api/admin/settings.ts` | DTO 字段扩展 |
| `frontend/src/i18n/locales/{zh,en}.ts` | `admin.settings.{tencent,aliyun,smsProvider}*` 24 + 24 键 |

**合并注意**：`sms_service.go` 的 `smsProvider` 接口和 `loadSmsClient` 三家分支不能退回单一服务商实现；新增三家的 SettingKey 常量须保留；`signup_source` 迁移须执行。

---

### 31. 密码登录开关 + 手机号 + 密码登录方式

新增管理员设置 `password_login_enabled`（默认 true）控制是否允许密码登录；同时把"手机号 + 密码"作为新登录方式加入（在已有手机号 + 短信验证码登录基础上扩展）。

**两次提交**：`0e9162aa`（核心实现）+ `89ed49e4`（设置项收尾 1 行补丁）。

**后端 — 修改文件**

| 文件 | 改动要点 |
|-----|---------|
| `backend/internal/handler/admin/setting_handler.go` | `PasswordLoginEnabled` 字段（DTO + GET/PUT） |
| `backend/internal/handler/setting_handler.go` | 公开设置端点暴露 `password_login_enabled`（89ed49e4 补全） |
| `backend/internal/handler/dto/settings.go` | `PasswordLoginEnabled bool` |
| `backend/internal/handler/dto/types.go` | DTO 类型扩展 |
| `backend/internal/handler/dto/mappers.go` | DTO 映射 |
| `backend/internal/service/setting_service.go` | `PasswordLoginEnabled` 读取（默认 `!= "false"`，即默认 true） |
| `backend/internal/service/settings_view.go` | 视图字段 |
| `backend/internal/service/domain_constants.go` | `SettingKeyPasswordLoginEnabled` 常量 |

**前端**

| 文件 | 改动要点 |
|-----|---------|
| `frontend/src/views/auth/LoginView.vue` | 手机号模式下出现「使用密码登录」切换链接（仅当 `phone_register_enabled` && `password_login_enabled`）；密码登录提交支持 phone 字段 |
| `frontend/src/views/admin/SettingsView.vue` | 「允许密码登录」开关 |
| `frontend/src/components/user/profile/Profile{InfoCard,IdentityBindingsSection,AccountBindingsCard}.vue` | 个人中心显示手机号绑定状态 |
| `frontend/src/views/user/ProfileView.vue` | 同步显示 |
| `frontend/src/api/admin/settings.ts` | DTO 字段 |
| `frontend/src/types/index.ts` | `password_login_enabled` 字段 |
| `frontend/src/i18n/locales/{zh,en}.ts` | 4 + 4 键 |

**合并注意**：`password_login_enabled` 默认必须保持 true，否则全站升级后无人能登录；上游若改 `setting_handler.go` 公开设置端点须确认本字段未丢失。

---

### 32. 模型定价表「只显示可见模型」过滤

> **⚠️ v6（2026-06-08）推翻**：「只显示可见模型」复选框已**删除**，后台模型折扣**默认即广场口径**（可路由 ∩ 海外开关 ∩ 版本下限）。可见集计算从 repo 的 CTE JOIN 收敛到 `service.ModelRoutingService` + `service.FilterVisibleModels`（与广场单一实现）。详见功能 34。下文为历史记录。
>
> **例外（2026-07-08）**：`List` 在 query 带显式 `provider` 时跳过广场过滤，展示该 provider 下全部同步模型（含 `unpriced`/未路由）。解决「新 provider（如 NVIDIA）从上游同步后，因未被账号路由而在广场口径下永不可见 → 无法补价+启用」的鸡生蛋。默认（无 `provider`）口径不变。见 CHANGELOG 2026-07-08。

历史实现（存档）：「模型定价」页"只显示可见模型"复选框 + repo 层 CTE JOIN（账号 `model_mapping` keys 并集）。v6 后：复选框与 `visible_only` query 已删，`ModelPricingListFilter.VisibleOnly` 字段保留但由 handler 固定为 true 且可见集计算改走 `ModelRoutingService`（见风险表 `model_pricing_repository.go` 两行与功能 34(C)）。`ModelPricingService` 包装层与 wire 注入仍在。

---

### 33. 模型定价计费单位 `pricing_unit`（token vs second）

> **⚠️ v6（2026-06-08）扩展**：`pricing_unit` enum 从二值扩为**四值**（增 `image_generation`/`video_generation`，迁移 154）；价格**分列**（按张 `output_cost_per_image` / 按 token `output_cost_per_image_token`，每行只填其一）；视频改为**真 token 计费**（官方火山公式 + `tier_pricing` 分档列，迁移 158）。前端添加/编辑自定义模型新增「图片（按张）」计价单位入口。详见功能 34。

`model_pricings` 表新增 `pricing_unit` enum 字段（`token` / `second`，默认 `token`），用于把"按秒计费"的视频/语音类模型与"按 token 计费"的对话模型在 schema 层区分。配合灵境视频模型（功能 28）的 USD/秒 计价语义落地。

**后端 — schema/migration**

| 文件 | 改动要点 |
|-----|---------|
| `backend/ent/schema/model_pricing.go` | `field.Enum("pricing_unit").Values("token", "second").Default("token")` |
| `backend/migrations/153_model_pricing_pricing_unit.sql` | `ADD COLUMN pricing_unit varchar(20) NOT NULL DEFAULT 'token'` + `CHECK IN ('token','second')` |
| `backend/ent/{modelpricing,mutation,migrate/schema,runtime/runtime}.go` | `go generate ./ent` 产物 |

**后端 — 修改文件**

| 文件 | 改动要点 |
|-----|---------|
| `backend/internal/handler/admin/model_pricing_handler.go` | Create/Update 接收 `pricing_unit` 字段 |
| `backend/internal/repository/model_pricing_repository.go` | 写入读取均带上 `pricing_unit` |
| `backend/internal/service/model_pricing_repository.go` | `DBModelPricing.PricingUnit` 字段 + 常量 `ModelPricingUnitToken`/`ModelPricingUnitSecond` |
| `backend/internal/service/pricing_service.go` | `ModelInfo.PricingUnit` JSON 暴露；`ListEnabledCatalogModels`：当 `PricingUnit=second` 且有 `CustomInputCost` 时用作 `InputCostPerToken` 实际值（保留 token 命名但承载"USD/秒"语义） |

**前端**

| 文件 | 改动要点 |
|-----|---------|
| `frontend/src/views/admin/ModelPricingsView.vue` | 编辑表单新增"计费单位"下拉（149 行新增） |
| `frontend/src/views/user/ModelsView.vue` | 用户端模型广场按 `pricing_unit` 显示单位（USD/1K tokens vs USD/秒） |
| `frontend/src/api/admin/modelPricings.ts` & `frontend/src/api/models.ts` | DTO 字段 |
| `frontend/src/i18n/locales/{zh,en}.ts` | 11 + 11 键 |
| `frontend/src/views/{admin,user}/__tests__/*.spec.ts` | 单测覆盖 token/second 渲染分支 |

**与功能 28 的关系**：功能 28 把视频模型的"USD/秒"价格直接写入 `output_cost_per_image`（沿用旧字段名借语义），功能 33 是把这一约定显式化到 schema 字段；后续应把灵境视频模型的 `pricing_unit` 批量更新为 `second`，并把 `output_cost_per_image` 字段语义注释同步收紧（未在本提交完成，遗留项）。

**合并注意**：`ent/schema/model_pricing.go` 变更后必须 `go generate ./ent`；迁移 `153_*.sql` 必跑；上游若改 `ModelInfo` 结构须保留 `PricingUnit` 字段；前端 `ModelsView.vue` 的单位显示逻辑不能退回固定 "USD/1K tokens"。

---

### 34. 定价通用化（绝对不内置）+ 后台=广场口径 + 视频真 token 计费 + 图片按张表单（v6）

2026-06-08 落地的一组定价系统重构，整体目标：**价格完全由运营管理、系统不内置任何厂商价**，且后台所见 = 用户所见。是功能 26（SSOT）/28（灵境目录）/32/33 的演进收尾，并**推翻了**「后台始终看全集」与「内置 doubao/lingjing seed」两条旧约定。设计文档：`claudedocs/可见模型口径统一方案.md` v6。

**(A) 定价通用化、删除全部内置厂商价**

| 文件 | 改动要点 |
|-----|---------|
| `backend/internal/service/wanjie_sync.go` | 通用化：`ParseMaasFromBytes(data, cnyRate, source)` / `FetchAndParseMaasModels(...)` 接受任意 `source`；`convertMaasModel` 收集视频 token 分档写 `TierPricing` |
| `backend/internal/service/doubao_sync.go` `lingjing_sync.go` | **删除**（含各自 `//go:embed *.json` 副本）；豆包/灵境价不再内置 |
| `backend/internal/service/model_catalog_seed.go` | **删除灵境/豆包 seed**，仅保留 16 条 Claude/GPT/Gemini 基础兜底 |
| `backend/internal/handler/admin/model_pricing_handler.go` | 三个平台专用同步端点合并为单一 `SyncMaas`（`source` 必填，JSON 上传或 URL+Token；凭证按 `maas_url:{source}`/`maas_token:{source}` 存；无内置兜底） |
| `backend/internal/server/routes/admin.go` | `POST /admin/model-pricings/sync-maas`（替换 sync-from-doubao/lingjing/wanjie） |
| `frontend/src/views/admin/ModelPricingsView.vue` `api/admin/modelPricings.ts` | 三按钮+两弹窗合并为一个「同步 MaaS 定价」通用弹窗；`syncModelPricingsFromMaas` 替换三个旧函数 |

> **副作用**：全新部署在运营上传 JSON 前无任何 MaaS 价，视频计费 fail-closed 返回 0。

**(B) 视频按官方火山真 token 计费**

| 文件 | 改动要点 |
|-----|---------|
| `backend/ent/schema/model_pricing.go` + `backend/migrations/158_model_pricing_tier_pricing.sql` | 新增 `tier_pricing` text 列（视频分档 JSON：`[{spec, cny_per_m_token}]`） |
| `backend/internal/service/lingjing_video_pricing.go` | 重写为 tier-based：`lingjingVideoTokens`（`⌈W/32⌉×⌈H/32⌉×fps×duration`，720p=858 token/帧×24fps 已核）、`CalculateVideoTokenCostUSD`、`selectVideoTier`（在线/有声无声） |
| `backend/internal/service/billing_service.go` | `CalculateSeedanceVideoCost` 从 catalog 读 `TierPricing` → `CalculateVideoTokenCostUSD` |

**(C) 可见模型口径单一实现 + 后台=广场（推翻功能 32 + §3 原则3）**

| 文件 | 改动要点 |
|-----|---------|
| `backend/internal/service/model_routing_service.go` | 新增：`RoutableModelInfos(accountIDs)`（广场）/ `OperatorRoutableModelInfos()`（后台全 active）/ `OperatorRoutableModelIDs()`；原 usage_handler 的 `collectWhitelistedModelsForAccounts` 等下沉至此 |
| `backend/internal/service/model_visibility.go` | 新增：`FilterVisibleModels(infos, showOverseas)` + 版本下限（claude≥4.5/gpt≥5.2/gemini≥3，o 系列）从前端 `shouldShowModel` 下沉 |
| `backend/internal/handler/usage_handler.go` | `ListModels` = `RoutableModelInfos(用户账号)` ▸ `FilterVisibleModels`；`UsageHandler` 持 `modelRouting` 字段 |
| `backend/internal/handler/admin/model_pricing_handler.go` | `List` 默认 `VisibleOnly=true` + `FilterVisibleModels(OperatorRoutableModelInfos, show_overseas_models)`（路由不可用退回全集）；不再解析 `visible_only` query；`routable` 原始集仍供 `pricing_health` orphan 判定 |
| `frontend/src/views/admin/ModelPricingsView.vue` `api/admin/modelPricings.ts` | 删「只显示可见模型」复选框 + `visibleOnly` state + `visible_only` 入参 |
| `backend/cmd/server/wire_gen.go` | `modelRoutingService` 注入 `NewUsageHandler`（8 参）+ `NewModelPricingHandler`（6 参） |

> **取舍**：leoobai 拍板优先「后台=广场」直观一致，牺牲原则1「后台 ⊇ 广场」。未被任何 active 账号路由 / 低版本海外模型不再出现在后台列表，给这类模型配价改走搜索框或 `sync-maas` 上传。

**(D) 图片按张计费表单入口（补功能 33）**

| 文件 | 改动要点 |
|-----|---------|
| `frontend/src/views/admin/ModelPricingsView.vue` | 添加/编辑自定义模型计价单位增「图片（按张）」选项，写 `output_cost_per_image`（图片单位时清空 token/秒占位字段）；选图片时联动 `mode=image_generation`；create/edit 表单 + handleCreate/handleSave 同步 |
| `frontend/src/i18n/locales/{zh,en}.ts` | `pricingUnitImage`、`upstreamPricePerImage` |

**(E) pricing_unit 扩四值 + 分列（补功能 33）**

| 文件 | 改动要点 |
|-----|---------|
| `backend/ent/schema/model_pricing.go` + `backend/migrations/154_model_pricing_unit_add_image_video.sql` | enum 扩 `image_generation`/`video_generation`；CHECK 重建 + 按 mode 回填 |
| `backend/migrations/155_pricing_data_governance_cleanup.sql` | chat 污染清理 + 单位归一 |
| `backend/internal/handler/admin/model_pricing_handler.go` + `repository/model_pricing_repository.go` | `normalizePricingUnit` 四值；DTO 透传 `output_cost_per_image[_token]` |

**合并注意**：
- `doubao_sync.go`/`lingjing_sync.go` 及 `internal/service/*.json` embed 副本已删除，上游同步若引回内置价须再删；`sync-from-doubao/lingjing/wanjie` 路由已合并为 `sync-maas`。
- `model_routing_service.go` / `model_visibility.go` 为纯新增文件，`wire_gen.go` 须注入 `modelRoutingService`（usage + model_pricing 两个 handler 各 6 参）。
- 迁移 154/155/158 必跑；schema 变更后 `go generate ./ent`。
- 后台 `List` 不要退回「全集 + visible_only 开关」；`billing_service.CalculateSeedanceVideoCost` 必须走 `tier_pricing`（勿退回写死 ¥/秒）。
- `backend/data/{doubao,lingjing,wanjie}.json` 保留为运营上传模板，非 embed。

---

### 35. 逆向订阅清理（删除 antigravity / OAuth / Codex 整链）

2026-06 落地的一项重大 fork 差异：**彻底删除从上游继承的「订阅逆向」能力**（拿他人 Claude Code / Codex / Gemini CLI / Antigravity 订阅账号 OAuth 登录换 token、在网关层伪装官方客户端转卖额度），改为只走正规 API 接入（apikey / bedrock / vertex / service_account / generic / lingjing）。方案与执行证据：`claudedocs/逆向代码清理执行方案_v6.md`、`claudedocs/逆向清理_执行报告_v6.md`。

**已删除（P1-P7 + 收尾，均已提交）**

| 范畴 | 内容 |
|-----|------|
| 平台 | `antigravity` 整平台（`pkg/antigravity` 仅迁出 `GeminiRequest`/`DummyThoughtSignature` 共享类型到 `pkg/gemini`，其余删；`service/antigravity_*.go`、handler、`PlatformAntigravity` ~94 处引用） |
| OAuth 逆向链 | Claude / Gemini / OpenAI / Codex 的 OAuth provider/service/handler、`pkg/openai/oauth.go`、`pkg/geminicli` OAuth/CodeAssist 部分、共享 token 刷新设施（`TokenRefreshService`/`OAuthRefreshAPI`/`token_refresher`） |
| 枚举/字段 | `AccountTypeOAuth`/`AccountTypeSetupToken` 常量、`groups.require_oauth_only` + `mcp_xml_inject`（migration 160 DROP COLUMN）、`account.IsOAuth()`（恒 false）整方法及全部死分支 |
| 路由/入口 | 逆向网关路由、账号 `apply-oauth-credentials`/`:id/refresh`/`/batch-refresh` 端点与 stub、前端 OAuth composable/UI、刷新/setPrivacy 死按钮 |
| 收尾死代码 | thinking-enabled context plumbing（读取侧随 OAuth 删除已无消费）、`token_version.go`、`applyClaudeCodeOAuthMimicryToBody` 等孤儿、gemini compat 的 `isOAuth` 死分支链 |

**保留（误删即破坏，见方案 §0.3）**：用户登录社交 OAuth（LinuxDo/GitHub/Google/WeChat/DingTalk/OIDC、`pkg/oauth` PKCE 工具、`refresh_token_cache`）、面板自有订阅/配额、正规云接入（Vertex/Bedrock/Gemini service_account/`GeminiTokenCache`/apikey 直通）。

**合并注意（最高优先）**：同步上游 `Wei-Shaw/sub2api` 会**整套重新引入**逆向订阅代码。合并后必须按 `逆向清理_执行报告_v6.md` 重新执行删除，并用风险表「逆向订阅代码」行的 grep 门禁校验只剩用户登录 OAuth 白名单。

**剩余 follow-up（inert 死代码）✅ 已于 2026-06-16 全部完成**（Docker 恢复可达后专项执行，逐类独立提交）：① 网关 `tokenType=="oauth"` 死分支 + constants.go OAuth 符号 → commit `8327fcd0`；② `RequirePrivacySet`/`IsPrivacySet` 死功能（Ent schema + 迁移 161）→ commit `8355b78f`；③ openai_*.go unused 孤儿函数海 → commit `dfe1fe40`。验收：build/vet/unit/e2e-compile/integration(Docker)/golangci-lint unused 全绿，rg 死符号清零。唯一遗留：类①「真实 apikey/vertex 转发冒烟」需 live 上游凭证手动补做。详见 `claudedocs/逆向清理_后遗症收尾_follow-up.md` + `claudedocs/待办任务列表.md` T1。

---

### 36. 全功能自包含 E2E 测试套件

2026-06-16 新增的 fork 测试资产（上游无）：一套**自起服务 + admin API 自动 seed** 的端到端测试，对着真实上游 apikey 验证网关多平台转发与计费/限额闭环。源于逆向清理后对「真实转发冒烟」的验证需求，沉淀为可复跑套件。

**新增文件（上游无，合并不冲突）**

| 文件 | 作用 |
|-----|------|
| `script/e2e-test.sh` | 自包含 runner：`dev_local` 起服务 → 等 health → 加载 `script/e2e.env`（gitignored 凭证）→ 跑 e2e → trap 拆环境；设 `E2E_BASE_URL` 可指向已运行实例跳过自起；`E2E_RUN=""` 跑全部 e2e |
| `script/e2e.env.example` | 上游凭证模板；真实 `script/e2e.env` 已在 `.gitignore`（勿提交） |
| `backend/internal/integration/e2e_full_provision_test.go` | provisioning 助手：admin 登录、建 apikey 账号/分组/网关 key、建用户设余额、余额/用量查询、Claude/OpenAI/Gemini 数据面调用。build tag `e2e` |
| `backend/internal/integration/e2e_full_test.go` | `TestE2EFull_*`：三平台转发（含流式/count_tokens、`TestE2EFull_GeminiForwarding`）+ 计费扣减 + 配额(quota)/余额不足/限流(rate_limit_5h) 拦截 + API Key 生命周期 + admin 账号/分组 CRUD |
| `backend/internal/integration/e2e_full_protocol_test.go` | Claude 协议深度套件（`TestE2EFull_ClaudeToolUse`/思维链等），131d3656 新增 |
| `backend/internal/integration/e2e_full_batchedit_repro_test.go` | 批量改账号模型限制 repro 用例（对应已知陷阱#1 修复 bd0bf3c4） |

**运行**：`cp script/e2e.env.example script/e2e.env`（填真实 key）→ `./script/e2e-test.sh`（或 `cd backend && make test-e2e`）。环境不全（无 admin / 无上游 key）各测试优雅 `t.Skip`；上游不支持 count_tokens / gemini 不可用时 skip（网关侧已证转发正确）。

**合并敏感（上游改契约会让 e2e 失败——loud，非静默）**：provisioning 依赖 admin API 请求体形状（`CreateAccountRequest`/`CreateGroupRequest`/建 key/`POST /admin/users/:id/balance`）、网关路由（`/v1/messages`、`/v1/messages/count_tokens`、`/v1/chat/completions`、`/v1beta/models/{m}:generateContent`）、余额预检语义（`server/middleware/api_key_auth.go` 的 `Balance <= 0` → 403）、key status 合法值（`active|inactive`）。build tag `e2e` 隔离，不影响常规 build/vet/unit。历史黑盒套件（`e2e_gateway_test.go`/`e2e_user_flow_test.go`）用旧 `CLAUDE_API_KEY`/`GEMINI_API_KEY`（网关 key）约定 + 写死模型列表，跑全量时仍 skip（与本套件并存、不冲突）。

---

### 37. 账号级协议适配（Bedrock Converse 兼容 + Kiro 归一 + 能力兜底）

账号级机制，与功能 8（Response Masking）同源（账号 flag + 选号后判断 + 响应侧处理），抽象为统一的 `AccountProtocolAdapter`。两端始终走标准 `/v1/messages`，不新增路由。设计与实现落地状态见 `claudedocs/账号级协议适配-bedrock与kiro-设计方案.md` §14。

**三个能力**
- **Bedrock Converse 兼容**（新开关 `accounts.extra.bedrock_compat`）：把返回客户端的响应改写为 AWS Bedrock Converse 形态——非流式 JSON 重映射（表 A）+ 流式二进制 EventStream（usage 末尾 metadata 帧）。与账号是否 AWS Bedrock 类型无关；与 Kiro 兼容互斥。
- **Kiro 响应归一**（复用现有 `accounts.extra.response_masking`，行为扩展）：把 Kiro 上游响应归一为标准 Anthropic——补缺失的 `stop_reason`、移除非标准 `usage.inference_geo`。归一与现有身份遮蔽 `needMask` 正交（不移除、不双重）。
- **Kiro 能力跨组兜底**：Kiro 遇 image/document（静默忽略的能力）→ reroute 到组配置的兜底组（如 Claude 官方组）。**复用现有预留字段 `groups.fallback_group_id_on_invalid_request`**（DB/CRUD/前端 UI 已有，此前网关未消费）。

**文件**

| 文件 | 说明 |
|-----|------|
| `backend/internal/service/account.go` | `IsBedrockCompatEnabled()`（仿 `IsResponseMaskingEnabled` :1443，读 `extra.bedrock_compat`） |
| `backend/internal/service/account_adapter.go` | 新增：`AccountProtocolAdapter` 接口 + `pickAdapter` + `RequestAction`（Pass/Reject/Reroute）+ Bedrock/Kiro 两 adapter + `streamAdapterFromCtx`/`applyAdapterNonStream` helper |
| `backend/internal/pkg/claude/conversecompat/` | 新包：`mapping.go`（Anthropic→Converse 非流式）+ `eventstream.go`（AWS EventStream 二进制帧编/解码，逆推自 `bedrockEventStreamDecoder`）+ `stream.go`（native SSE→Converse 事件流） |
| `backend/internal/pkg/claude/kirocompat/` | 新包：`normalize.go`（Kiro→标准 Anthropic 归一） |
| `backend/internal/service/gateway_service.go` | Forward gating（InspectRequest switch：Reject 早返回 / Reroute 换组 / Pass 存 ctx）；`RequestRerouteError` 类型；`invalidRequestFallbackGroupID`；三路非流式接 `applyAdapterNonStream`；通用 + APIKey 直通两条流式路径 adapter 接管 |
| `backend/internal/service/bedrock_stream.go` | AWS Bedrock 流式路径 adapter 接管（Content-Type 覆盖 + EmitStreamEvent + FinishStream） |
| `backend/internal/handler/gateway_handler.go` | 选号循环捕获 `RequestRerouteError` → 换 `currentGroupID` + 重置 failover + 重进循环（防循环 max=1，计费按原 key 不变） |
| `frontend/.../CreateAccountModal.vue` `EditAccountModal.vue` | 「Bedrock Converse 兼容」开关（与「响应遮蔽」互斥，watch 实现）；i18n `bedrockCompat*` |
| `backend/internal/integration/e2e_full_{bedrock_compat,kiro_compat,kiro_reroute}_test.go` | 真实上游 e2e：Bedrock 非流式/流式 Converse、Kiro 归一、vision/document 跨组兜底 |
| `script/capture-kiro-fixtures.sh` | Kiro 样本采集（基础 + 能力探测）；fixtures 落 `claudedocs/fixtures/kiro/` |

**合并注意**：
- `account_adapter.go` / `conversecompat/` / `kirocompat/` 是纯新增，通常不与上游冲突。
- `gateway_service.go` 合并时确认：Forward 的 adapter gating（在 `response_masking` gate 之后、`anthropic_passthrough` 分支之前）、三条流式路径的 `streamAdapter` 接管分支未被覆盖。
- `gateway_handler.go` 选号循环的 `RequestRerouteError` 捕获 + `currentGroupID` 用于选号/sticky（计费仍 `currentAPIKey`）须保留。
- 复用 `groups.fallback_group_id_on_invalid_request`（既有字段），无新 migration。
- 前端 `bedrock_compat` 开关与 `response_masking` 互斥逻辑、`GroupsView.vue` 的 `fallback_group_id_on_invalid_request` 编辑控件（既有）须保留。

---

### 38. API Playground（用户对话/API 试玩页）

用户端可视化对话/请求测试页：用自己的网关 Key 直接向网关发消息、查看响应（流式/非流式），用于自助验证账号可用性与模型行为。上游无此功能。

**文件**

| 文件 | 说明 |
|-----|------|
| `frontend/src/views/user/PlaygroundView.vue` | 试玩主页面（对话输入、模型选择、流式渲染）；含 `__tests__/PlaygroundView.spec.ts` |
| `frontend/src/components/playground/MessageBubble.vue` | 消息气泡组件；含 `__tests__/MessageBubble.spec.ts` |
| `frontend/src/api/playground.ts` | 前端 API 客户端；含 `__tests__/playground.spec.ts` |
| `frontend/src/router/index.ts` | `/playground` 路由（`requiresAuth`，`titleKey: playground.title`） |
| `frontend/src/i18n/locales/zh.ts` & `en.ts` | `playground.*` 翻译键 |

**合并注意**：纯前端功能，复用现有网关 API（`/v1/messages` 等），无新增后端路由。上游改动路由守卫或 i18n 时确认 `/playground` 路由与 `playground.*` 键未丢失。

---

### 39. 双桶调度统计看板（Dual-Bucket Stats）

功能 P5 双桶影子比较（`scheduler_dual_bucket.go`，见风险表 🟢 行与「双桶」段）的**观测面**：新增 admin API + 服务，聚合双桶（协议桶 vs 单桶）在指定平台/时间窗内的调度指标差异，供 4 周切单桶前的验证决策。上游无此功能。

**文件**

| 文件 | 说明 |
|-----|------|
| `backend/internal/service/dual_bucket_stats.go` | 双桶指标聚合服务 |
| `backend/internal/handler/admin/dual_bucket_stats_handler.go` | `DualBucketStatsHandler`（`NewDualBucketStatsHandler(schedulerSnapshotService)`）；须在 `wire_gen.go` 注入并进 `ProvideAdminHandlers` |
| `backend/internal/server/routes/admin.go` | 路由 `GET /api/v1/admin/scheduler/dual-bucket-stats?platform=&days=` |

**合并注意**：与 P5 `scheduler_dual_bucket.go`、`ProtocolBucketEnabled` 回滚开关配套。`DualBucketStatsHandler` 依赖 `schedulerSnapshotService`；上游改动 admin 路由/wire 时确认注入链与路由未丢失。

---

### 40. 上游巨型文件拆分后的 fork 落位（0.1.147 同步产物）

上游 0.1.147 对 `gateway_service.go` / `openai_gateway_service.go` / `usage_log_repo.go` /
`setting_handler.go` / `admin_service.go` 做了「纯移动拆分」。fork 语义按函数三方归并回上游拆分文件后，
两类 fork 代码在上游拆分结构中无对应位置，落到两个新文件：

| 文件 | 说明 |
|-----|------|
| `backend/internal/service/gateway_claude_body.go` | 上游 `gateway_claude_oauth_body.go` **混装**了 OAuth 拟态（逆向）与共享工具（非逆向）。fork 只保留后者：`replaceModelInBody`、`sanitizeSystemText`、`deleteJSONPathBytes`、`buildStableSessionSeed`/`sessionContextDiscriminator`/`generateSessionUUID`、`cacheControlPath`/`collectCacheControlPaths`/`enforceCacheControlLimit`/`injectAnthropicCacheControlTTL1h`/`forceEphemeralCacheControlTTL`、`shouldInjectAnthropicCacheTTL1h`/`shouldNormalizeClientDateline`/`normalizeClientDatelineIfEnabled` |
| `backend/internal/service/gateway_fork_support.go` | `listSchedulableAccountsBase`（功能 25）、`invalidRequestFallbackGroupID`（功能 37）、`calculateVideoCost`（功能 34）、`resolveProviderKey`/`resolveUpstreamModelForCost`（功能 14）、`claudeUsageToTokens` |

**合并注意**：上游同步会重新引入 `gateway_claude_oauth_body.go`（整包含 OAuth 拟态）。合并后须：
① 删除该文件；② 确认本文件两处的符号未与上游重复声明；③ `grep -c "buildOAuthMetadataUserID\|normalizeClaudeOAuthRequestBody" backend/internal/service/` 为 0。

另外注意 fork 与上游对同名函数的两处**签名融合**（勿被上游整体覆盖）：
- `isOpenAICompatibleAccountEligibleForRequest`：上游加 `platform` 形参（grok），fork 加 generic 早返回分支；另有窄化包装 `isOpenAIAccountEligibleForRequest` 供 generic 调度路径复用
- `OpenAIGatewayService.listSchedulableAccounts(ctx, groupID, platform)`：上游加 `platform` 形参，fork 在 `platform == PlatformOpenAI` 时并入 generic 账号

---

### 41. i18n 语言包模块化 + fork 键深合并（0.1.147 同步产物）

上游把单体 `frontend/src/i18n/locales/{zh,en}.ts`（7877/7703 行）拆成模块目录
（`landing`/`common`/`dashboard`/`admin/*`/`misc`）。fork 的 276（zh）/ 278（en）个自定义键
被提取到 `locales/{zh,en}/fork.ts`，并由新增的 `locales/forkMerge.ts::deepMergeLocale`
在 `locales/{zh,en}/index.ts` 末尾深合并覆盖上游模块。

| 文件 | 说明 |
|-----|------|
| `frontend/src/i18n/locales/forkMerge.ts` | `deepMergeLocale(base, patch)` 递归深合并（数组整体替换） |
| `frontend/src/i18n/locales/{zh,en}/fork.ts` | fork 自定义键：`models.*`、`nav.models`、`playground.*`、`auth.phone*`、`admin.settings.{volcengine,tencent,aliyun,smsProvider}*`、`admin.accounts.generic.*`、`version.*`、`onboarding.*` 等 |
| `frontend/src/i18n/locales/{zh,en}/index.ts` | 末尾 `export default deepMergeLocale({...上游模块}, fork)` |

**合并注意**：上游改语言包模块时不会碰 `fork.ts`；新增 fork 键**直接改 `fork.ts`**，不要写进上游模块文件。
上游若新增同名键，fork 键优先（深合并后置覆盖）。

---

## 版本历史

> 仅作时间线索引，功能细节见对应功能节，不在此重复。

| fork 版本 | 上游版本 | 摘要 |
|----------|---------|-----------|
| 1.1.108 | 0.1.108 | 模型广场、用户统计、词云（功能 2/3/4；词云 2026-06-08 已移除） |
| 1.1.121 | 0.1.121 | 模型折扣、人民币定价、UI 主题、语言 Cookie（功能 5/6/7） |
| 1.1.123 | 0.1.123 | 响应遮蔽、Actions 禁用、货币模式（功能 8/9）；移除 OAuth 前端 UI（功能 11） |
| 1.1.125 | 0.1.125 | 同步上游（Airwallex 多币种、支付 NaN 修复等） |
| 1.1.126 | 0.1.126 | 同步上游；灵境接入（功能 12）；注册必填用户名（功能 13） |
| feature/maas-refactor | 0.1.126 | MAAS 重构 P0-P5 全量（功能 14-19） |
| 1.1.129 | 0.1.129 | 运维日志 IP/密钥字段（功能 20）；同步上游 0.1.128-129；首个 GitHub Release |
| maas-refactor（已并入） | 0.1.129 | 发布基础设施对接 fork（功能 21）；文档精简（功能 22）；全量文档同步（功能 23） |
| maas-refactor（已并入） | 0.1.129 | 模型导入工具 + generic 账号级密钥（功能 24）；generic 运行时转发（功能 25） |
| maas-refactor（已并入） | 0.1.129 | 模型定价 SSOT 重构 8 PR（功能 26，2026-05-28）；Bill-Request-ID 对账（功能 27，migration 148） |
| maas-refactor（已并入） | 0.1.129 | 灵境目录全量化 + JoySpace 刊例价（功能 28，migration 151/152） |
| maas-refactor（已并入） | 0.1.129 | 手机号注册/短信登录（功能 29）；短信服务商抽象（功能 30）；密码登录开关（功能 31）；migration 149/150 |
| maas-refactor（已并入） | 0.1.129 | 可见模型过滤（功能 32，后被 v6 推翻）；pricing_unit（功能 33）；定价通用化 v6（功能 34，migration 153-158）；词云整体移除（功能 4，migration 159） |
| 1.1.136（已提交，2026-06-16 同步上游 0.1.136） | 0.1.136 | P1/P2 修复加固（SSE buffer 池复用、`BulkUpsertMaas` 事务化、Gemini 用量并行+缓存、token 估算零拷贝、帧边界 Flush、`io.ReadAll` 限流；7.6 判误报、7.7 待产品决策未改），依据 `claudedocs/代码修复报告-2026-06-09.md` |
| 1.1.136（后续提交） | 0.1.136 | `data/model_pricing.json` 补 deepseek 参考定价（冷启动兜底模板，非 seed 内置，commit eb6cf4ae）；功能 36 扩展：Claude 协议深度 E2E（`e2e_full_protocol_test.go`，131d3656）+ Gemini 转发用例（4c96fd1a） |
| feature/reverse-cleanup-v6 | 0.1.x | 逆向订阅清理（功能 35）：删 antigravity/OAuth/Codex 整链 + `IsOAuth`/thinking plumbing/孤儿端点等收尾死代码，migration 160；后遗症 T1 三类已收尾（2026-06-16，migration 161 drop `require_privacy_set`）；新增全功能 E2E 自包含套件（功能 36） |
| feature/bedrock-converse-compat（2026-06-28） | 1.1.x | 功能 37：账号级协议适配——Bedrock Converse 兼容（非流式+流式三路接管）、Kiro 响应归一（补 stop_reason/移除 inference_geo）、Kiro image/document 跨组兜底（复用 `fallback_group_id_on_invalid_request`，无新 migration）；新增 `account_adapter.go` + `conversecompat`/`kirocompat` 包；真实上游 e2e 全验证。本文档由 `claudedocs/` 移至根目录 |
| zhiguofan（2026-07-01 同步上游 0.1.141） | 0.1.141 | 同步上游 `#2007 feature/user-usage-admin-parity`（用户端用量统计与管理员口径对齐）；`usage_handler.go`/`usage_log_types.go`（功能27 Bill-Request-ID）、`UsageView.vue`+费用展示共享组件（功能5 人民币定价 formatUSD）冲突手动解决；e2e 全量验证 19/19 PASS |
| zhiguofan（2026-07-06 回炉 0.1.145） | 0.1.145 | 原始合并「未清理·不编译」回炉：后端/前端逆向残留清理（199 编译错误）、货币口径修复（`OrderTable`/`PaymentQRDialog`/`UsageView` CSV，功能 5）、守护脚本 ForcePlatform 规则 3→1；根目录新增 `CHANGELOG.md` |
| zhiguofan（2026-07-07 同步上游 0.1.146） | 0.1.146 | 同步 47 提交：采纳 apikey 请求头覆写（`account_header_override.go`，剥离 OAuth 分支）、入站端点归一化+responses/compact 区分、Redis SCAN 优化、gpt-5.6 新模型、`ProvideAPIKeyService` 新增 concurrencyService 参；逆向回炉（grok/codex/antigravity/oauth）；新增 `TestE2EFull_BillRequestIDWriteback`（功能 27 显式断言）；E2E 21/21 PASS |
| zhiguofan（2026-07-10 同步上游 0.1.147） | 0.1.147 | 同步 147 提交：上游拆分 5 个巨型文件，fork 语义逐函数三方归并（自动 64 个 + 人工 3 处）；**决策保留 Grok 官方 xAI API、删除 Grok 订阅逆向**（守卫改 apikey、`isOpenAIAccount` 纳入 grok、恢复迁移 157/158）；逆向链再清理（antigravity/Claude Code 拟态/codex CLI 限制/spark 影子/`ListOAuthRefreshCandidates`）；修复上游 `ListCRSAccountIDs` 查 `parent_account_id` 的运行时地雷；新增功能 40（`gateway_claude_body.go`+`gateway_fork_support.go`）与功能 41（i18n fork 键深合并）；采纳上游 response.failed 透传、上下文超限不 failover、流式漏计费修复、鉴权绕过与 XSS 修复 |
| zhiguofan（2026-07-08 一致性审计） | 0.1.146 | 补录未记录 fork 功能：功能 38 API Playground、功能 39 双桶统计看板、功能 5 补 CurrencySetupModal；风险表补功能 25/37 拆分 helper 行 |
| zhiguofan（2026-07-13 同步上游 0.1.152） | 0.1.152 | 同步 40 提交：**采纳 Grok 官方 xAI API key 账号全链**（前端平台入口/类型/颜色/图标此前缺失一并补齐 + 后端 group oneof 加 grok + endpoint 分流 + 429→rate-limit 持久化 + cache identity 裁剪版）与 **Codex alpha/search 按次计费**（migration 174，OAuth 分支剥离）；修复 0.1.151 合并误删的 forward.go grok Responses 分流；继续删除 grok OAuth 订阅逆向整链（oauth/quota service、chat 桥、useGrokOAuth、CodexModels）；上游 grok 内置兜底价测试删除（fork fail-closed 政策）；详见 `CHANGELOG.md` [1.1.152] |

---

*最后更新：2026-07-13（同步上游 0.1.152：Grok 官方 xAI API key 全链采纳 + alpha/search 按次计费 + grok OAuth 逆向链再删除；修复 0.1.151 合并误删的 grok Responses 分流；要点见 `CHANGELOG.md` [1.1.152]）*

*历史更新：2026-07-10（同步上游 0.1.147：新增功能 40「上游巨型文件拆分后的 fork 落位」与功能 41「i18n 语言包模块化 + fork 键深合并」；Grok 保留官方 API、删除订阅逆向；风险表补 3 行）*

*历史更新：2026-07-08（同步上游 0.1.146 后代码-文档一致性审计：补录 3 项此前未记录的 fork 功能——**功能 38 API Playground**（用户对话/API 试玩页，`PlaygroundView.vue`+`MessageBubble.vue`+`playground.ts`+`/playground` 路由）、**功能 39 双桶调度统计看板**（`dual_bucket_stats.go`+handler+`/admin/scheduler/dual-bucket-stats` 路由，此前只记了 `scheduler_dual_bucket.go` 比较逻辑、缺观测面）、**功能 5 补 `CurrencySetupModal.vue`**（首次货币设置向导）；风险表补上述文件 + 功能 25/37 拆分 helper 行（endpoint_selector/types、bridge_capabilities、gemini_compat_helpers/tier、protocol_consistency_check、account_schedulable、gateway_temp_unschedule、credentials_merge）；审计仅覆盖 fork 新增源码文件，改动文件行为审计另行进行）*

*历史更新：2026-07-11（同步上游至 0.1.151，61 提交无结构重构：成本快照装配点路径校准——`ApplyUpstreamCostSnapshot()` 调用在上游 0.1.147 拆分后实际位于 `gateway_usage_billing.go:985`/`openai_gateway_usage.go:337`/`lingjing_poll_runner.go`，非旧记的 `gateway_service.go`/`openai_gateway_service.go`；风险表 line 38、装配点表 3 行、验证清单 grep、合并注意共 5 处路径更正，装配点编号 4/4→3/3；`gateway_service.go`/`openai_gateway_service.go` 仍存在且保留 masking/generic/adapter 等其它 fork 功能。本次合并冲突解决要点见 `CHANGELOG.md` [1.1.151]）*

*历史更新：2026-07-01（同步上游至 0.1.141：风险表补 `frontend/src/views/user/UsageView.vue` 及其费用展示共享组件行，记录该文件曾被上游整体重写、合并时需保留 `formatUSD`/`formatCost` 人民币换算；功能 27 合并注意补充 `usage_handler.go` 的 `List()`→`parseUserUsageFilters()` 重构冲突模式；版本历史加 2026-07-01 同步条目）*

*历史更新：2026-06-28（新增功能 37「账号级协议适配」：风险表加 `account_adapter.go`/`conversecompat`/`kirocompat`/`bedrock_compat`/`gateway_handler` 行 + `gateway_service.go` 行补功能 37 标注；本文档由 `claudedocs/自定义开发功能列表.md` 移至根目录 `自定义开发功能列表.md`，同步更新所有引用路径）*

*历史更新：2026-06-21（代码-文档一致性审计校准：①功能 5 折扣独立页/路由已被功能 24/34 取代——`ModelDiscountsView.vue` 删除、`/admin/model-discounts` 改重定向、后端 `settings/model-discounts` 路由+handler 移除，折扣并入 `model_pricings.discount_rate`；②`billing_service.go` 无 `applyDiscount()` 方法，折扣走 inline `discountRate`；③`USAGE_UPSTREAM_COST_ENABLED` 未实现为 env flag，快照由 `ApplyUpstreamCostSnapshot` 命中 provider_pricing 驱动；④provider-pricing 路由名更正为复数 `provider-pricings` 且无前端页面；⑤`NewUsageHandler` 6→8 参、灵境 apiId 40→51；⑥`model_pricing.json` 含 deepseek 等参考定价已澄清为冷启动模板（非内置）；⑦功能 36 补 `e2e_full_protocol_test.go`/`e2e_full_batchedit_repro_test.go`；⑧版本历史 1.1.136 标注更正为已提交）*

*历史更新：2026-06-16（①逆向清理后遗症 T1 三类全部收尾：功能 35 follow-up 行标 ✅ + 记三个 commit + migration 161 drop `require_privacy_set`；②新增功能 36「全功能自包含 E2E 测试套件」：`script/e2e-test.sh` + `e2e_full*_test.go`，三平台转发/计费/配额/限流/CRUD 断言，凭证走 gitignored `script/e2e.env`，记合并敏感契约点；③变更表 reverse-cleanup-v6 行同步更新）*

*历史更新：2026-06-15（新增功能 35「逆向订阅清理」：风险表加「逆向订阅代码」高危行 + 修 types/index.ts 的 antigravity/oauth 残留描述，记录 antigravity/OAuth/Codex 整链删除与收尾死代码、保留白名单、上游同步重新引入须再删的合并风险，剩余 3 类 follow-up 指向 `逆向清理_后遗症收尾_follow-up.md` + `待办任务列表.md`）*

*历史更新：2026-06-12（①文档瘦身：风险表去重合并、版本历史压缩为索引、功能 12/23/24/25/26/27/28/32 叙事精简，所有合并风险要点保留；②事实修正：VERSION 不再硬编码、`script/check_fork_integrity.sh` 不存在已删、`verify_pricing_coverage` 标注未入库、seed 数 21→16、功能 25 flag 默认值矛盾修正为 true；③记录 P1/P2 修复加固，依据代码修复报告-2026-06-09）*
